# Context Map

## Contexts

- [Fábrica de Agentes de IA](./docs/domain/CONTEXT.md) — linguagem do produto, conceitos do runtime gerado, builder, flow spec, persistência, LLM e safety.

## Relationships

- O roteamento ICM da raiz fica em `CONTEXT.md`.
- A linguagem de domínio fica em `docs/domain/CONTEXT.md`.
- Decisões arquiteturais ficam em `docs/adr/`.
- O plano consolidado fica em `docs/plan.md`.
- As aplicações do builder ficam em `apps/` e têm roteamento próprio.
- Verificadores transversais ficam em `tools/` e têm roteamento próprio.
