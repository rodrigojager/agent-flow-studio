# Status de Implementação

Última atualização: 2026-07-07.

Resumo atual: o MVP principal está 100% verificado no estado atual do workspace e não deve ser rebaixado por pendências do plano completo. O plano completo expandido continua em evolução para colaboração distribuída mais profunda, integrações externas reais e artefatos operacionais avançados que dependem de ambiente/provedor.

## Implementado

- Estrutura ICM do workspace.
- Plano consolidado em `docs/plan.md`.
- ADRs de arquitetura de `0001` a `0018`.
- Baseline manual em `examples/reference-interview-runtime/`.
- Contrato FastAPI baseado em `/sessions`.
- Idempotência por `Idempotency-Key` com fallback `idempotency_key`.
- Transcript separado de eventos.
- LangGraph real no baseline.
- Persistência pública com SQLAlchemy.
- Checkpointer LangGraph com fallback in-memory para testes e opção Postgres para ambiente real.
- Cache quente Redis com fallback in-memory.
- LLMClient OpenAI/OpenAI-compatible/OpenRouter com mock determinístico.
- Safety Gate de entrada e saída com fallback determinístico e primeira camada de Safety Harness configurável por nó: `safetyMode`, `safetySeverityThreshold`, `safetyFallbackResponse` e `safetyRules` com match `contains`/`regex`, categoria, severidade e ação `warn`/`safe_redirect`/`block`.
- API key por header com compatibilidade legada `AGENT_API_KEY` e autenticação local avançada por `AGENT_API_KEYS`/`AGENT_API_KEYS_PATH`, incluindo múltiplas chaves, arquivo JSON local rotacionável sem rebuild/restart, expiração local por `expires_at`/`expiresAt`, revogação local persistente por `AGENT_API_REVOKED_KEY_IDS`/`AGENT_API_REVOKED_KEY_IDS_PATH`, inventário seguro em `GET /auth/keys`, scopes globais `metadata:read`, `auth:read`, `sessions:read`, `sessions:write`, `jobs:read`, `jobs:write`, wildcard `*`, prefixos como `sessions:*`, scopes por agente em bundles (`agents:<agent_id>:metadata:read`, `agents:<agent_id>:sessions:*`, `agents:<agent_id>:jobs:*`, `agents:<agent_id>:auth:read` ou `agents:<agent_id>:*`), validação por rota, suporte SSE de eventos/WebSocket por header ou query `api_key`, rate limit local opcional por credencial/escopo e auditoria por `GET /auth/audit`, com janela em memória e persistência local opcional em JSONL por `AUTH_AUDIT_PATH`, sem registrar valores brutos de chave.
- Studio Local usa `STUDIO_RUNTIME_API_KEY` ou `AGENT_API_KEY` de `Secrets locais` como `X-Agent-API-Key` nas chamadas HTTP ao runtime, adiciona `api_key` na URL de SSE/WebSocket de eventos, possui painel `Chaves de auth` para carregar `/auth/keys`, resumir chaves ativas/expiradas/revogadas, exportar inventário `.afauthkeys.json` governado sem valores brutos/caminhos locais e preparar `AGENT_API_REVOKED_KEY_IDS` para o próximo start sem mostrar valores brutos de chave, e possui painel `Auditoria de auth` para carregar `/auth/audit`, resumir status e listar eventos por `key_id`/escopo/rota sem mostrar o valor da chave.
- Runtime manual e runtime gerado expõem streaming de eventos por SSE em `GET /sessions/{session_id}/events/stream` e por WebSocket em `/sessions/{session_id}/events/ws`, com `from_seq`, `max_events`, timeout controlado e fechamento explícito por `stream_closed`.
- Runtime manual e runtime gerado expõem streaming do turno por SSE em `POST /sessions/{session_id}/turn/stream` e por WebSocket em `/sessions/{session_id}/turn/stream/ws`, preservando idempotência, emitindo `turn_started`, `token`, `turn_completed` e `stream_closed`, usando callback incremental do grafo/LLM quando disponível e fallback por resposta final quando o provider não entrega chunks.
- Runtime manual e runtime gerado emitem spans nativos durante a execução do grafo como eventos `span_started` e `span_completed`, persistidos em `/events`, SSE e WebSocket enquanto a chamada roda, com `span_id`, `node_id`, `node_type`, `started_at`, `finished_at`, `duration_ms`, `status`, `source=runtime_native_span`, `source_message_id` quando o span vem de um turno e `payload.span` compatível com o painel `Execução ao vivo`.
- Runtime manual e runtime gerado possuem primeira camada de jobs pós-finalização: tabela `agent_jobs`, criação de job `post_finish_summary` no `finish`, `max_attempts`, `last_error`, `next_run_at`, claim/lease multiworker por `worker_id`, `locked_until` e `WORKER_LEASE_SECONDS`, retomada de job abandonado após lease expirado, métricas agregadas e operacionais em `GET /jobs/metrics?window_hours=...` (status/tipo, pendências, tentativas, taxa de sucesso, duração média/mínima/máxima/p95, janela configurável, throughput, próxima pendência, leases ativos/expirados, finalizações na última hora e último término), retenção governada em `POST /jobs/cleanup` com `dry_run=true` por padrão e remoção explícita só de jobs terminais antigos, eventos `post_finish_pending`/`post_finish_completed`/`post_finish_scheduled`/`post_finish_retry_scheduled`/`post_finish_failed`/`post_finish_retry_requested`/`jobs_cleanup_completed`/`job_schedule_created`/`job_schedule_enqueued`/`job_schedule_event_triggered`/`job_schedule_disabled`, endpoints `GET /jobs`, `GET /jobs/{job_id}`, `POST /jobs/{job_id}/run`, `POST /jobs/{job_id}/retry`, `POST /jobs/{job_id}/schedule`, `POST /jobs/{job_id}/recurrence`, `GET /job-schedules`, `POST /job-schedules/run-due`, `POST /job-schedules/trigger-event`, `POST /job-schedules/{schedule_id}/disable`, `POST /jobs/run-pending`, `POST /jobs/retry-failed` e `POST /jobs/cleanup`, tabela `agent_job_schedules` para recorrência simples por intervalo, cron básico ou evento com `trigger_type`/`cron_expression`/`event_type`, botões visuais `Evento fim`, `Disparar fim`, `Prévia limpeza 7d` e `Limpar 7d` no Studio, worker CLI opcional `python -m app.worker --once`, serviço `worker` no Docker Compose final configurável por `WORKER_INTERVAL_SECONDS`, `WORKER_LIMIT`, `WORKER_RETRY_DELAY_SECONDS`, `WORKER_LEASE_SECONDS` e `WORKER_CLEANUP_*`, cleanup automático governado desligado por padrão, teste no baseline, teste gerado e paridade OpenAPI/comportamental.
- `/metadata` do runtime manual, runtime gerado e bundle gerado expõe `operations.jobs` com comando do worker, intervalo, limite, retry delay, lease, suporte multiworker, política de retenção/cleanup automático, status terminais e suporte a schedules por intervalo, cron básico e evento, sem retornar chaves, tokens, URLs sensíveis ou valores brutos de `.env`.
- Runtime manual e runtime gerado aplicam migrações aditivas leves no `init_db()` quando `AUTO_CREATE_TABLES=true`, preservando volumes existentes e adicionando colunas novas como `agent_id` e campos de `agent_jobs` em tabelas legadas antes de usar o ORM.
- O `init_db()` gerado serializa criação/migração de schema em PostgreSQL com `pg_advisory_xact_lock`, evitando corrida entre `api` e `worker` no primeiro `docker compose up`.
- Dockerfile, Docker Compose com `api`, `worker`, `postgres` e `redis`, `.env.example` e migration SQL do baseline. O compose gerado aceita `.env` ausente, mantém `MOCK_LLM=true` por padrão e deixa `LANGSMITH_TRACING=false` no `.env.example` para o caminho local não tentar enviar traces ao LangSmith Cloud.
- Smoke Docker real passou em 2026-07-03 e foi validado novamente em 2026-07-04 em `generated/reference-interview-runtime`; o gate está codificado em `npm run test:docker-runtime-smoke`: valida compose sem `.env` obrigatório, executa `docker compose up -d --build`, `/health`, `/metadata`, `POST /sessions`, `start`, `turn`, `transcript`, `events`, `finish`, `GET /jobs?session_id=...`, com job `post_finish_summary` processado como `succeeded` e logs de API/worker sem `Traceback`, `IntegrityError`, `LangSmith` ou `Unauthorized`.
- Gate `npm run test:mvp-main-path` passou em 2026-07-03 e novamente em 2026-07-04 cobrindo draft, validação, evidência de debug, geração/aprovação do sandbox LangGraph, handoff LangSmith governado sem token/secrets, registro local de deploy/verificação cloud com URL sanitizada, runtime ZIP aprovado, setup de modelo local, smoke Docker simulado e bundle aprovado.
- Flow de referência em `flows/reference-interview/agent.flow.json`.
- Flow Spec inicial em Zod/TypeScript.
- Flow Spec expõe catálogo canônico de adapters LLM com Codex CLI, OpenAI, OpenAI-compatible, OpenRouter, Ollama local e opencode Go/Zen, incluindo base URLs oficiais `https://opencode.ai/zen/go/v1` e `https://opencode.ai/zen/v1`.
- Flow Spec, Builder API, Builder UI e Codegen LangGraph possuem primeira camada de modelo local via adapter `ollama`, com default `qwen3:8b`, base URL `http://localhost:11434/v1`, chave dummy `OLLAMA_API_KEY=ollama`, sem exigência de chave real quando `MOCK_LLM=false`, nota visual no inspector, presets iniciais por perfil de hardware (`llama3.2:3b`, `qwen3:8b`, `qwen3:14b`), healthcheck local protegido contra hosts remotos, detecção visual de modelos instalados por `/api/tags`, chips clicáveis para aplicar um modelo já instalado ao flow, sugestão guiada de `ollama pull` quando o modelo do flow não está instalado, `.env.example` usando `LLM_MODEL` com compatibilidade de leitura para `OPENAI_MODEL`, defaults de base URL/chave aplicados no cliente LLM gerado e `docker-compose.yml` gerado com serviço `ollama`, volume `ollama_models`, `OLLAMA_BASE_URL=http://ollama:11434/v1`, tuning local por `OLLAMA_KEEP_ALIVE`/`OLLAMA_NUM_PARALLEL`/`OLLAMA_MAX_LOADED_MODELS`/`OLLAMA_MAX_QUEUE`/`OLLAMA_CONTEXT_LENGTH`, serviços one-shot `ollama-pull-*` no profile explícito `model-setup`, override `docker-compose.model-image.yml` com `ollama-models/Dockerfile` para imagem local pré-carregada, tag separada `OLLAMA_MODEL_IMAGE` para distribuição versionada e override `docker-compose.gpu.yml` para reserva NVIDIA GPU em hosts compatíveis. O painel `API Docker final` detecta os serviços de setup no status, mostra os modelos exigidos, inclui `Modelos locais` no checklist, executa `/docker-runtime/setup-models` pela UI sem exigir terminal, expõe `Build imagem` para construir a imagem Ollama pré-carregada via `/docker-runtime/build-model-image`, registra essa operação no histórico como `build_model_image`, expõe `Exportar imagem` para salvar `OLLAMA_MODEL_IMAGE` como `.tar` por `/docker-runtime/export-model-image`, registra essa operação como `export_model_image`, expõe `Publicar imagem` para executar `docker image push` por `/docker-runtime/push-model-image` quando a tag aponta para registry, registra essa operação como `push_model_image`, expõe `Registrar catálogo` para persistir tag/modelos/comandos em `.agent-flow/model-images/catalog.afmodelimages.json` via `/model-image-catalog/register-runtime`, permite consultar e mesclar esse catálogo por `/model-image-catalog` e `/model-image-catalog/merge`, descobre pacotes `.afmodelimages.json` por `/model-image-catalog/discovery` em `.agent-flow/model-images/imports` e em `AGENT_FLOW_MODEL_IMAGE_CATALOG_PATHS`, sincroniza os descobertos por `/model-image-catalog/sync-discovered`, salva registries remotos curados sem credenciais em `.agent-flow/model-images/remote-registries.afmodelregistry.json` por `/model-image-catalog/remote-registry`, consulta registries remotos HTTP(S) salvos ou configurados em `AGENT_FLOW_MODEL_IMAGE_CATALOG_REMOTE_URLS` por `/model-image-catalog/remote`, mescla-os por `/model-image-catalog/sync-remote` com timeout de 5s, payload máximo de 1 MB e sem enviar credenciais Docker ou valores de `.env`, detecta GPU NVIDIA local por `nvidia-smi`, inspeciona runtimes Docker por `docker info --format "{{json .Runtimes}}"`, recomenda GPU apenas quando há override GPU, GPU local e runtime `nvidia` no Docker, permite escolher visualmente `Up CPU` ou `Up GPU` sem editar `.env`, usando `docker-compose.gpu.yml` quando o usuário escolhe GPU, e expõe a ação `Testar GPU` via `/docker-runtime/check-gpu`, executando `docker run --rm --gpus all nvidia/cuda:12.4.1-base-ubuntu22.04 nvidia-smi -L` para confirmar dispositivo dentro de container e registrar histórico `check_gpu`. Quando o runtime usa adapter `ollama` e o provider retorna modelo ausente, o LLMClient devolve fallback prescritivo com `ollama pull ...` e `docker compose --profile model-setup up ...`, sem transformar o turno em erro 500, e registra `fallback_reason=local_model_missing` no payload `llm_called`.
- Codegen em TypeScript gerando runtime Python executável em `generated/reference-interview-runtime/`.
- Validação mínima de equivalência do baseline: o runtime manual e o runtime gerado exercitam o mesmo contrato `/sessions`, idempotência, transcript, eventos, safety e fluxo LangGraph de referência.
- Verificação automatizada de paridade estrutural em `tools/verify_runtime_parity.py`, comparando flow spec, OpenAPI, schemas principais, metadata e cenários normalizados entre baseline manual e runtime gerado.
- Builder API em `apps/builder-api/` para criar, listar, ler, validar e gerar flows versionáveis.
- Builder UI inicial em `apps/builder-ui/` com canvas React Flow, lista de flows, criação de flow em branco, criação explícita por templates do Catálogo, inspector, preview JSON, edição básica de propriedades, salvamento do `agent.flow.json` e ações de validar/gerar via Builder API.
- Builder API persiste flows versionáveis com `PUT /flows/{flowId}`, valida Flow Spec antes de gravar e bloqueia divergência de `id`.
- Builder API cria novos workspaces de flow com canvas em branco e mantém o template de conversa guiada no Catálogo para criação explícita de exemplo funcional com safety, switch, LLM, human input, gate determinístico e finish.
- Flow Spec expõe análise estruturada de flows com diagnósticos de grafo, referências, nós, arestas e compatibilidade inicial de codegen.
- Builder API cria, lê, salva e remove prompts Markdown e schemas JSON referenciados pelo flow, com validação de path dentro do diretório do flow, validação JSON para schemas e bloqueio de remoção quando o asset ainda está em uso.
- Builder API retorna validação visual rica com diagnósticos estruturados, contagem de erros/avisos e checagem de assets referenciados.
- Builder API expõe catálogo de adapters LLM via `/llm-adapters` e catálogo de modelos por provider via `/llm-adapters/{adapterId}/models`, consultando Codex CLI local, `/models` de gateways OpenAI-compatible/OpenRouter/opencode e `/api/tags` do Ollama.
- Builder API exporta e importa pacotes JSON versionados de workspace de flow, contendo `agent.flow.json`, prompts e schemas referenciados, com proteção contra conflito e path traversal.
- Builder API lista, lê e empacota artefatos gerados dentro de `generated/` via `/artifacts`, `/artifacts/file` e `/artifacts/archive`.
- Builder UI possui aba `Arquivos` para criar, remover e editar prompts e schemas referenciados pelo flow antes de validar, gerar ou iniciar sandbox.
- Flow Spec, Builder API e Builder UI preservam metadados visuais de prompts/schemas no `agent.flow.json`: versão, descrição, tags e variáveis de prompt, com formulário dedicado na aba `Arquivos` e salvamento pelo mesmo fluxo dirty do workspace.
- Builder UI permite exportar e importar workspace de flow pela toolbar, salvando alterações pendentes antes de exportar.
- Builder UI possui aba `Artefato` para pré-visualizar arquivos do runtime ou bundle gerado e baixar um zip do projeto.
- Builder UI possui aba `Validação` para exibir diagnósticos estruturados e navegar para nós, arestas, prompts ou schemas afetados.
- Builder UI permite editar adapter/modelo/env vars do LLM padrão do flow e adapter/modelo de nós LLM.
- Builder UI permite criar, remover, mover, conectar e reconectar nós/arestas no canvas, com posições persistidas no `agent.flow.json`.
- Builder API expõe catálogo local reutilizável via `/catalog`, `/catalog/items`, `/catalog/items/{kind}/{itemId}/export`, `/catalog/items/import`, `/catalog/items/restore-revision`, `/catalog/agent-templates/create-flow` e `/flows/{flowId}/catalog/apply`, persistindo itens do workspace em `.agent-flow/catalog/registry.json`, carregando seeds locais de prompt/schema/tools/templates de agente/skills, mantendo versão/revisão/hash de conteúdo por item e snapshots anteriores em `history`, criando novas revisões ao restaurar snapshots antigos, exportando/importando pacotes versionados `agent-flow-builder.catalog-item.v1`, criando flows completos a partir de templates, aplicando prompts/schemas como assets reais, tools como patch/criação de nó ou bundle composto de nós/arestas e skills como bundle de prompt/schema/patch de nó selecionado ou subgrafo composto com remapeamento de prompt/schema, além de biblioteca compartilhável em `.agent-flow/catalog/shared-library.afcataloglibrary.json` com rotas `GET /catalog/shared-library`, `POST /catalog/shared-library/load`, `POST /catalog/shared-library/merge`, `POST /catalog/shared-library/conflicts/:conflictId/curation`, `POST /catalog/shared-library/conflicts/:conflictId/resolve`, `GET /catalog/central` e `POST /catalog/sync-central`, incluindo detecção inicial de conflitos por `kind/id`, snapshots compactos sem conteúdo bruto, curadoria governada local com lease configurável por `AGENT_FLOW_CATALOG_CONFLICT_CURATION_LEASE_HOURS`, liberação automática de lease vencido, resolução governada local com `resolvedBy`, `resolution=keep_library` e nota de resolução, `resolutionHistory` compacta com snapshot mantido/descartado sem conteúdo bruto e sync central opcional por `AGENT_FLOW_CATALOG_CENTRAL_URL` com token somente no header e status sem URL/token.
- Builder UI possui aba `Catálogo`, com busca textual, filtros por tipo/origem/tag, refresh, cards de itens locais/built-in com versão/revisão/hash visíveis, resumo visual de blocos/templates com etapas, conexões, assets e preview JSON, curadoria visual de itens locais com edição de metadados, criação/reordenação/remoção de etapas, alteração de IDs com remapeamento de conexões e patch alvo, refs de prompt/schema, criação/remoção de conexões internas, condições, prompts/schemas internos com ID/path/conteúdo editáveis, validação guiada antes de salvar e painel `Schema guiado` para schemas internos com métricas de campos/required/`$defs`/refs/composições, ações de campo, `$defs`, `oneOf`, bloqueio de `additionalProperties` e validação semântica, histórico de revisões, comparação selecionável contra a revisão atual, diff compacto e ação de restaurar revisão, exportação/importação de pacotes `.afcatalog.json`, painel de governança por item com checks de versionamento/metadados/conteúdo/reuso/histórico/portabilidade e export `.afcataloggovernance.json` sem conteúdo bruto ou valores de secrets, governança agregada da biblioteca com export `.afcataloglibrarygovernance.json`, ações visuais `Carregar compartilhado`/`Sincronizar compartilhado`, `Central`/`Sync central` da biblioteca do catálogo, curador local persistido em `localStorage` com papel `Owner`/`Reviewer`/`Viewer`, resumo de conflitos compartilhados, cards compactos com decisão `Manter biblioteca` bloqueada para Viewer e painel `Histórico de resolução` da biblioteca compartilhada, além de ações para salvar o prompt/schema atual, salvar o nó selecionado como tool ou skill local, salvar subgrafos selecionados como tool/skill composta, criar flow a partir de template de agente, criar blocos de tool/skill composta e adicionar/aplicar prompt, schema, tool ou skill no flow ou no nó selecionado.
- Builder API e Builder UI possuem primeira camada central de governança compartilhável do workspace por `.agent-flow/governance/workspace.afgovernance.json`, com formato `agent-flow-builder.workspace-governance.v1`, participantes, papéis `owner`/`reviewer`/`operator`/`viewer`, políticas por área, conflitos de papel, resolução `keep_existing`/`use_incoming`, auditoria compacta e flags de governança sem secrets, env values ou runs brutas. A API também expõe `/workspace-governance/authorize` com decisão `agent-flow-builder.workspace-governance-decision.v1`, calculando ação permitida/bloqueada por ator, área, papel e política em modo consultivo local por padrão. Quando `AGENT_FLOW_WORKSPACE_GOVERNANCE_ENFORCE=true`, mutações críticas de governança, catálogo, schemas, annotation queue, replay, safety, experimentos, Docker/runtime delivery, sandbox local e geração/aprovação de runtime exigem autorização por `x-agent-flow-actor` ou ator do payload. Quando a auth local do Builder está ativa, as mesmas rotas protegidas também exigem papel/área compatíveis da chave autenticada, com decisão `agent-flow-builder.builder-auth-area-decision.v1` sem payload bruto ou secrets. A aba `Governança` permite carregar, salvar, sincronizar, exportar `.afgovernance.json`, resolver conflitos abertos usando a identidade local de revisor e verificar acesso por ator/área/ação.
- A aba `Governança` também possui visão agregada de pendências de colaboração por `/collaboration/conflicts`, carregando apenas revisões governadas de workspace, catálogo, padrões de schema, cenários/datasets/evaluators, annotation queue, pins de nós, camadas de debug e replay governado. O pacote `agent-flow-builder.collaboration-conflict-overview.v1` expõe totais globais, totais filtrados, opções de triagem por flow, área, severidade, responsável, papel e status, `reviewPath`, `sourceActions` da área de origem para revisão/diff/curadoria/resolução, bloqueio de mutação por `viewer` e hash do pacote, e a UI exporta `.afcollaboration-conflicts.json` e compara revisões por `/collaboration/conflicts/diff`, gerando `agent-flow-builder.collaboration-conflict-overview-diff.v1`/`.afcollaboration-conflicts-diff.json` apenas com deltas, hashes e refs governadas, sem schema bruto, prompt bruto, input/output bruto, headers, tokens, payloads, candidatos completos ou secrets.
- Builder API possui primeira camada opcional de autenticação local própria, separada da auth do runtime final: `AGENT_FLOW_BUILDER_AUTH_REQUIRED=true` protege rotas não públicas, aceita chave por `X-Agent-Flow-Builder-Key`, `X-Agent-Builder-API-Key`, `X-API-Key` ou `Authorization: Bearer`, carrega chaves de `AGENT_FLOW_BUILDER_API_KEY`, `AGENT_FLOW_BUILDER_API_KEYS` ou `AGENT_FLOW_BUILDER_API_KEYS_PATH`, aceita actor/role/groups/areas/scopes/expiração/desabilitação, aceita JWT local assinado por `AGENT_FLOW_BUILDER_AUTH_JWT_SECRET` (`HS256`), `AGENT_FLOW_BUILDER_AUTH_JWT_PUBLIC_KEY` (`RS256`) ou JWKS por `AGENT_FLOW_BUILDER_AUTH_JWT_JWKS_PATH`/`AGENT_FLOW_BUILDER_AUTH_JWT_JWKS_URL`, e descobre `jwks_uri`/`end_session_endpoint` via `AGENT_FLOW_BUILDER_AUTH_OIDC_ISSUER_URL` ou `AGENT_FLOW_BUILDER_AUTH_OIDC_DISCOVERY_URL`, com seleção por `kid`, cache local por URL/discovery, validação opcional de issuer/audience e claims configuráveis de ator/papel/grupos/áreas/scopes, enriquece grupos por ator via diretório local `AGENT_FLOW_BUILDER_AUTH_GROUP_DIRECTORY`/`AGENT_FLOW_BUILDER_AUTH_GROUP_DIRECTORY_PATH` e diretório corporativo HTTP opcional `AGENT_FLOW_BUILDER_AUTH_GROUP_DIRECTORY_URL` com token fora de status/export, aplica política local por grupo de `AGENT_FLOW_BUILDER_AUTH_GROUP_POLICIES`/`AGENT_FLOW_BUILDER_AUTH_GROUP_POLICIES_PATH` para role/áreas/scopes efetivos, expõe `/builder-auth/status` com inventário sem valores brutos e status JWT/JWKS/OIDC/grupos/diretório/políticas/sessões/auditoria sem segredo/chave bruta, cria primeira sessão local curta por `/builder-auth/session` com token `Bearer` armazenado apenas por hash em memória, persiste sessão local em arquivo central hash-only por `AGENT_FLOW_BUILDER_AUTH_SESSION_PATH`, sincroniza lifecycle hash-only de sessão com serviço HTTP opcional por `AGENT_FLOW_BUILDER_AUTH_SESSION_SERVICE_URL`, renova a sessão por `/builder-auth/session/refresh` rotacionando o token e revogando o anterior, encerra por `/builder-auth/session/logout` sem retornar token bruto, registra auditoria local em `/builder-auth/audit` sem headers nem valores brutos, com persistência/reload JSONL opcional por `AGENT_FLOW_BUILDER_AUTH_AUDIT_PATH` e sink HTTP central opcional por `AGENT_FLOW_BUILDER_AUTH_AUDIT_SINK_URL`, enviando payload versionado sem chave bruta, token de sessão ou headers da requisição, token de sink apenas por header e falha não bloqueante, filtros por status/ator/chave/rota/busca, resumo agregado por status/ator/chave/rota e metadados de carga do arquivo persistido, permite gerar chave nova por `/builder-auth/keys` e desativar chave file-backed por `/builder-auth/keys/:keyId/disable` quando `AGENT_FLOW_BUILDER_API_KEYS_PATH` está configurado, salvando apenas `keyHash` no arquivo e retornando o valor bruto uma única vez, e injeta o ator autenticado em `x-agent-flow-actor` para o enforcement de governança. A Builder UI envia `VITE_BUILDER_API_KEY`, a sessão local válida ou a chave salva localmente no navegador, mostra grupos corporativos da sessão quando presentes, e a aba `Governança` possui campo mascarado para salvar/limpar essa chave local, status de JWT/JWKS/OIDC/grupos corporativos, diretório local/HTTP externo de grupos, políticas por grupo, persistência central local e serviço corporativo da sessão, status JSONL/sink central da auditoria, criação/renovação/encerramento de sessão local removendo a chave bruta após criar sessão, inventário seguro, eventos recentes de auditoria, filtros visuais de auditoria, export governado `.afbuilderauthaudit.json` sem headers/valores brutos, formulário de geração com presets de escopo, expiração em 7/30 dias, opção sem expiração, resumo visual da política e botão de desativação para chaves file-backed sem incluí-las em exports com secrets.
- O gate dedicado `npm run test:builder-auth-corporate` valida o contrato corporativo local do Builder com serviços HTTP simulados para session service, introspecção central obrigatória, audit sink e diretório de grupos, cobrindo token somente no header, ausência de body em probes `HEAD`, `GET` do diretório sem body, sessão local hash-only, refresh/revogação, decisão central fail-closed, auditoria sanitizada e respostas sem URL/token/chave bruta.
- Sessões `Bearer afbs_*` do Builder também suportam introspecção/decisão central por `AGENT_FLOW_BUILDER_AUTH_SESSION_INTROSPECTION_URL`, com token opcional, timeout, modo fail-closed por `AGENT_FLOW_BUILDER_AUTH_SESSION_INTROSPECTION_REQUIRED=true`, envio apenas de hash/metadados locais, override seguro da identidade efetiva, rejeição central mesmo quando a sessão existe localmente e status visual sem URL/token.
- Builder API/UI expõe probe governado de integrações corporativas em `POST /builder-auth/external-probe` e botão `Testar integrações` na aba `Governança`, verificando serviço corporativo de sessão, introspecção central, sink central de auditoria e diretório corporativo de grupos com `HEAD` sem corpo para serviços/sinks e `GET` para diretório. O resultado retorna apenas status, código HTTP, contagens e razões sanitizadas, com tokens somente no header e sem URL, token, header, JWT, chave bruta, token de sessão ou token de provedor.
- Builder API/UI expõe homologação corporativa local em `POST /builder-auth/corporate-homologation` e botão `Homologar auth`, gerando `.agent-flow/builder-auth/corporate-homologation.afbuilderauthhomologation.json` com status `blocked`/`verified`/`homologated`, nível de homologação, evidências pendentes, snapshot sanitizado de auth/JWT/OIDC/sessões/auditoria/grupos, componentes testados do probe e flags de governança sem URL, token, header, JWT, chave bruta, token de sessão, token de provedor ou path absoluto.
- Builder API e Builder UI agora completam login OIDC local por authorization code + PKCE, refresh OIDC de sessão e logout federado avançado: `POST /builder-auth/oidc/login-url` cria state/nonce/code challenge, `GET /builder-auth/oidc/callback` troca code por `id_token`, valida state/nonce/assinatura/issuer/audience, cria sessão local `afbs_*` e mantém `id_token`/`refresh_token` apenas em memória como hint de logout e refresh de sessão; `POST /builder-auth/oidc/session/refresh` troca o refresh token no token endpoint, valida o novo `id_token`, rotaciona a sessão local e não retorna refresh token ao navegador; `POST /builder-auth/oidc/logout-url` gera a URL do `end_session_endpoint` descoberto ou configurado por `AGENT_FLOW_BUILDER_AUTH_OIDC_END_SESSION_ENDPOINT`, com `AGENT_FLOW_BUILDER_AUTH_OIDC_POST_LOGOUT_REDIRECT_URI` opcional, `id_token_hint` quando a sessão local possui hint e state hash-only; `GET /builder-auth/oidc/logout-callback` valida state, rejeita replay/estado inválido e não retorna tokens do provedor; a aba `Governança` habilita `Entrar OIDC` e `Sair OIDC`, mostra callback validado/hint/refresh por sessão e remove a sessão local depois de abrir logout.
- Flow Spec aceita `position` opcional em nós para preservar layout visual sem afetar o runtime gerado.
- Codegen LangGraph monta o grafo gerado a partir dos nós e arestas do `agent.flow.json`, com handlers por tipo de nó e eventos baseados nos nós realmente executados.
- Codegen LangGraph executa nós dedicados de `switch` e `human_input`, incluindo condições simples com `and`, comparações de estado e eventos operacionais específicos.
- Flow Spec, Builder UI e Codegen LangGraph possuem suporte inicial a nós avançados determinísticos `http_request`, `transform_json`, `database_query`, `database_save`, `file_extract` e `rag_retrieval`, com configuração visual, execução no runtime gerado, suporte a `mock://echo` para testes sem rede, tabela genérica `agent_node_records` para gravações JSON, cópia de assets de `files/` para `app/files/`, extração de texto/Markdown/PDF opcional via `pypdf`, busca lexical RAG local e eventos próprios em `/events`.
- Flow Spec, Builder UI e Codegen LangGraph possuem suporte inicial a nós `approval_gate`, `scoring` e `analytics`, com configuração visual, execução no runtime gerado e eventos próprios em `/events`.
- Codegen LangGraph valida adapters LLM pelo catálogo, gera runtime apenas com o adapter selecionado, respeita overrides de adapter/modelo em nós LLM, deriva envs/defaults de runtime a partir do adapter selecionado e roteia OpenCode Go/Zen por família de modelo conforme endpoints documentados: `responses`, `chat/completions`, `messages` e `models/{model}` para Gemini.
- Codegen possui testes end-to-end com flow simplificado sem `deterministic_gate`, flow com `switch`/`human_input`, flow com `http_request`/`transform_json`, flow com `database_query`/`database_save`, flow com `file_extract`/`rag_retrieval`, flow com `approval_gate`/`scoring`/`analytics`, primeira camada de adapter local Ollama e bundle multiagente, gerando runtimes temporários e executando pytest nos artefatos gerados quando aplicável.
- Flow Spec define `RuntimeManifest` para agrupamento monoagente ou multiagente, com agentes referenciando `agent.flow.json` por `flowPath`.
- Codegen gera bundle a partir de `runtime.manifest.json`, com metadados, README, contrato de storage compartilhado por `DATABASE_URL`/Postgres, contrato de isolamento operacional por agente em `.runtime-manifest/agent-isolation.json`, `agentIsolation` em `.agent-flow/generated-meta.json` e `bundle.json`, teste opcional de integração com `AGENT_FLOW_TEST_POSTGRES_URL`, `.agent-flow/generated-meta.json` no pacote raiz e um runtime independente por agente em `generated/reference-runtime-bundle/agents/`.
- Codegen gera app FastAPI raiz para manifestos `multiagent`, montando os agentes em um único processo pelos `routePrefix`, preservando idempotência por prefixo de rota e publicando em `/metadata`/`/health` a política de isolamento operacional: prefixos únicos, import isolado de módulos `app`/`app.*`, restauração de `sys.path`, storage por `agent_id` e scopes `agents:<agent_id>:...`.
- Runtime baseline, runtime gerado e bundles multiagente agora expõem `agent_id` em `/metadata`, sessão e eventos; o Studio envia `agent_id` na criação de sessão, persiste `agentId` nos runs locais, mostra resumo por agente e filtra runs/timeline por agente.
- Builder API lê, salva, valida e gera bundles por manifesto via rotas `/runtime-manifest`, `/runtime-manifest/validate`, `/runtime-manifest/generate` e `/runtime-manifest/generate-approved`; a geração aprovada bloqueia bundles sem sandbox aprovado por agente, embarca aprovação agregada em `.agent-flow/langgraph-sandbox-approval.json`, copia a aprovação para cada agente do bundle e a validação retorna flow, resourceName e contrato por agente para orientar endpoints reais do bundle.
- Builder UI possui aba `Runtime` para editar visualmente `runtime.manifest.json`, incluindo metadata, LLM padrão, empacotamento, agentes, prefixos de rota, composição assistida multiagente, orquestração declarativa por modo/entrada/handoffs, validação, geração de bundle preliminar e geração de bundle aprovado por manifesto via Builder API; a mesma aba mostra mapa operacional do bundle multiagente com app raiz, `/health`, `/metadata`, runtime por agente, rotas montadas e endpoint de sessão por resourceName.
- Sandbox local inicial: Builder API inicia/para o runtime gerado, lista runtimes em memória, acompanha status/logs e aceita porta configurável; Builder UI permite iniciar/parar/atualizar, acompanhar logs recentes, escolher porta e acionar criação de sessão, turnos, finalização, transcript e events.
- Codegen gera um artefato separado de sandbox LangSmith/LangGraph em `generated/reference-interview-langgraph-sandbox/`, com `langgraph.json`, `app/langgraph_app.py:graph`, `.env.example` com variáveis LangSmith, README próprio, testes de entrypoint LangGraph e dependência `langgraph-cli[inmem]` isolada do runtime FastAPI.
- Artefatos gerados incluem `.agent-flow/generated-meta.json` com target e hash determinístico do projeto do agente, cobrindo `agent.flow.json`, prompts, schemas, arquivos em `files/` e código customizado referenciado por `codePath`.
- Builder API expõe `/flows/{flowId}/generate-langgraph-sandbox`, `/flows/{flowId}/approve-langgraph-sandbox` e `/flows/{flowId}/generate-approved-runtime`, recusando gerar runtime aprovado quando o sandbox não foi gerado/aprovado ou quando o hash do flow/assets mudou.
- Builder UI expõe a sequência visual `LangGraph` -> `Aprovar` -> `API Docker`, separando o pacote de validação LangSmith/LangGraph do runtime final FastAPI/Docker.
- Builder API expõe controle operacional do runtime Docker final aprovado e do bundle multiagente por `/docker-runtime/status`, `/docker-runtime/prepare-env`, `/docker-runtime/setup-models`, `/docker-runtime/configure-ports`, `/docker-runtime/build`, `/docker-runtime/up`, `/docker-runtime/down`, `/docker-runtime/smoke`, `/docker-runtime/smoke-all` e `/docker-runtime/inspect`, validando que o artefato está em `generated/`, possui target `fastapi-runtime` ou `runtime-manifest-bundle`, `Dockerfile`, `docker-compose.yml` e metadados necessários antes de executar comandos.
- Builder API aceita `runtimeUrl` local para status, comandos e smoke test, permitindo testar uma porta customizada do runtime final sem alterar o Builder.
- Builder API persiste histórico operacional local do runtime Docker em `.agent-flow/docker-runtime-history/`, expõe `/docker-runtime/history` e mantém esse histórico fora do artefato exportável.
- Builder UI mostra controles `Status`, `Inspecionar`, `Preparar .env`, `Modelos`, `Build imagem`, `Exportar imagem`, `Publicar imagem`, `Testar GPU`, `Build`, `Up CPU`/`Up GPU`, `Smoke`, `Smoke todos` e `Down` no painel `Artefato` quando o pacote carregado é a API Docker final ou bundle multiagente Docker, com checklist guiado de entrega final (`Aprovar -> .env -> Modelos -> Build -> Up -> Smoke -> Exportar`), próxima ação derivada do status/histórico, diferenciação entre `Baixar zip preliminar` e `Baixar zip final`, auditoria estrutural de exportabilidade do ZIP, runbook para rodar fora do Builder com rotas reais por agente, porta real do `docker-compose.yml`, comando `docker compose --profile model-setup up ...` quando o Compose declara modelos locais, comando `docker image save ...` quando existe imagem Ollama pré-carregada exportável, comando `docker image push ...` quando a tag `OLLAMA_MODEL_IMAGE` é publicável e comando `docker run --rm --gpus all nvidia/cuda:12.4.1-base-ubuntu22.04 nvidia-smi -L` para probe manual de GPU, relatório JSON de prontidão/exportação incluindo `modelSetup` e `agentOperations` para runtime monoagente ou bundle multiagente sem valores de `.env`, ZIP exportado com `.agent-flow/export-manifest.json` para identificar tipo/target do pacote, contrato de exportação e passo `model_setup`, input de Runtime URL, seletor visual CPU/GPU para modelos locais, seletor de agente para smoke em bundles, cards operacionais por agente com rota, metadata, endpoint de sessões, link direto de metadata, status/evidência de smoke por agente, resumo de `operations.jobs` observado no smoke para worker/retry/claim multiworker/retenção/schedules, detecção de smoke desatualizado após porta/build/up/setup de modelos, ação direta `Smoke agora`, ação `Usar no smoke`, atalho `Histórico` com filtro `operation=smoke`/`agentId` e export `.afagentrunbook.json` por agente com URLs, comandos manuais, checklist, evidência de smoke, política operacional de jobs com labels derivadas e objeto bruto `jobsOperations`, e flags sem `.env`/secrets, edição visual das portas API/Postgres/Redis do `docker-compose.yml`, links para `/docs` e `/openapi.json`, serviços de `docker compose ps`, logs recentes, histórico operacional, auto-atualização opt-in, status da última operação, painel de progresso de build, resumo do smoke test individual e smoke agregado por agente com a mesma política operacional de jobs, e resumo do probe GPU com dispositivos detectados ou erro do container.
- Plano mestre de implementação documentado em `docs/master-implementation-plan.md`, consolidando objetivo final, base já implementada, decisões, superfícies de produto, arquitetura alvo, fases, critérios de aceite e checklist anti-regressão.
- Paridade ProUp documentada em `docs/proup-capability-parity.md`, registrando as capacidades que a ferramenta precisa conseguir recriar e a politica de escape hatch para evitar engessamento visual. O catálogo built-in inclui o template `pro-up-parity-complex-agent`, que cria um flow validável/gerável com conversa `sessions-v1`, consulta de conteúdo por arquivo/RAG, escape hatch HTTP, geração estruturada de perguntas, estado por código, scoring, analytics, approval gate e input humano, sem editar manualmente o runtime exportado.
- Flow Spec, Builder UI e Codegen LangGraph possuem contrato inicial para nós `code` customizados, com linguagem, modo de execução, arquivo, entry point, dependências, input path, result path, código inline, executor HTTP, MCP stdio, sidecar local e `runtime_adapter`. O codegen copia assets de `codePath` para `app/code/`, inclui esses arquivos no hash de aprovação, executa Python nativo por arquivo/inline, JavaScript/TypeScript por arquivo/inline via runner Node, Bash/Shell nativo por arquivo/inline via processo com contrato JSON stdin/stdout, `codeExecution: "http"` por contrato externo JSON, `codeExecution: "mcp"` por tool MCP local via stdio, `codeExecution: "sidecar"` por subprocesso local com JSON via stdin/stdout e `codeExecution: "runtime_adapter"` por endpoint de adapter com payload `input/context/contract/adapter` ou pelo runner VM local quando `sandboxIsolation="vm"` e o adapter declara `codeInline`/`codePath`, materializa `codeDependencies` em `app/code/package.json`, instala Bash/Node/NPM no Dockerfile final e registra `custom_code_executed`, `custom_code_declared` ou `custom_code_failed` em `/events`, com `execution_log` e `span` estruturados para cada execução customizada.
- Flow Spec, Builder UI e Codegen LangGraph possuem primeira camada de Safety Harness local no nó `safety_gate`, com modo padrão/custom, threshold de severidade, resposta segura padrão e regras JSON versionáveis. O inspector possui biblioteca local de políticas de Safety com salvar/aplicar/remover/exportar/importar pacote `.afsafety.json`, permitindo reutilizar modo, severidade, fallback e regras entre nós/workspaces. O runtime gerado aplica regras custom antes/depois do LLM conforme `stage`, preserva as regras padrão em `default_and_custom`, bloqueia antes do LLM por `safety.blocked`, registra rule id/label/severity/action/match/source/provider no payload de safety, mantém compatibilidade com o Safety Gate simples existente e pode chamar provider HTTP externo por env vars `SAFETY_PROVIDER_*` quando as regras locais permitem.
- Nós `code` com execução externa (`http`, `runtime_adapter`, `sidecar` e `mcp`) possuem primeira política visual/canônica de segurança de payload: `retryAttempts`, `payloadAllowPaths`, `redactPaths` e `maxPayloadBytes`. O runtime gerado filtra o `context.state` por allowlist antes de enviar, aplica redaction em `input`, `context` ou caminhos de estado, bloqueia payload acima do limite configurado, executa retry simples até 5 tentativas extras e registra `attempts`, `retry_attempts`, `payload_bytes` e `payload_policy` no resultado e no `execution_log`.
- O inspector de nós `code` possui biblioteca local de perfis de sandbox/payload, com salvar/aplicar/remover/exportar/importar pacote `.afcodesandbox.json`, reutilizando `sandboxIsolation`, `sandboxEnvAllowlist`, imagem gerenciada por executor, preset gerenciado de VM, `sandboxContainerImage`/`sandboxContainerEngine`, `sandboxVmRunner`/`sandboxVmImage`, `retryAttempts`, `payloadAllowPaths`, `redactPaths`, `maxPayloadBytes` e `timeoutSeconds` entre nós e workspaces sem guardar secrets ou URLs.
- O runtime gerado registra metadados de sandbox/isolamento para cada execução customizada, diferenciando `runtime_process`, `dedicated_process`, `node_runner_process`, `subprocess_stdio`, `external_endpoint`, `container`, `vm` e `declared_external`, com boundary, executor, transporte, tentativas e payload no `custom.sandbox` e no `execution_log`. O Studio Local exibe essa informação no Tool manager, nos logs estruturados, no filtro por sandbox e no export JSON de logs do nó.
- Nós `code` process-backed (`sidecar`, `mcp`, Bash/Shell e JavaScript/TypeScript via runner Node) possuem primeira camada executável de isolamento por workspace temporário: `sandboxIsolation: "ephemeral_workspace"` cria uma cópia descartável de `app/code`, executa o subprocesso com `cwd` nessa cópia, permite restringir env vars por `sandboxEnvAllowlist`, registra `ephemeral_workspace`/`process_workspace` no trace e evita que escritas do executor contaminem o `app/code` persistente do runtime gerado. Isso ainda não substitui isolamento por container/VM.
- Nós `code` Python `native`/`inline`/`file` também aceitam `sandboxIsolation: "dedicated_process"` como primeira camada de worker dedicado: o runtime gerado copia `app/code` para workspace temporário, chama outro processo Python por stdin/stdout JSON, aplica `sandboxEnvAllowlist` quando configurada, captura stdout do código do usuário sem quebrar o canal JSON, registra `dedicated_process`/`process_workspace` no trace e descarta escritas do worker após a execução. Isso ainda não substitui isolamento por container/VM.
- Nós `code` Python, JavaScript, TypeScript e Bash/Shell `native`/`inline`/`file` aceitam a primeira camada de `sandboxIsolation: "container"`: o Flow Spec, Builder UI, perfis `.afcodesandbox.json` e codegen carregam `sandboxContainerImage`/`sandboxContainerEngine`, e o Studio possui catálogo inicial de imagens gerenciadas por executor/linguagem com provenance em `sandboxContainerImageId`, incluindo preset `bash:5.2`; o runtime gerado executa `docker` ou `podman` com `--network none`, monta workspace temporário em `/workspace`, aplica `sandboxEnvAllowlist`, registra imagem/engine/rede/política no `custom.sandbox` e no `execution_log`, e falha de forma rastreável quando a imagem ou engine não está configurada/disponível. Em JavaScript/TypeScript, o container reaproveita o `code_runner.mjs` gerado e mantém transporte stdin/stdout JSON com executor `node`; em Bash/Shell, mantém transporte stdin/stdout JSON com executor `bash`/`sh`. A primeira camada de orquestração avançada de container também já existe: `sandboxContainerProfile` aceita `baseline`/`hardened`, com `sandboxContainerMemory`, `sandboxContainerCpus`, `sandboxContainerPidsLimit`, `sandboxContainerReadOnlyRootfs`, `sandboxContainerDropCapabilities` e `sandboxContainerNoNewPrivileges`, aplicando `--memory`, `--cpus`, `--pids-limit`, `--read-only`, `--tmpfs`, `--cap-drop ALL` e `--security-opt no-new-privileges` quando configurado.
- Nós `code` Python, JavaScript, TypeScript e Bash/Shell `native`/`inline`/`file` aceitam a primeira camada de `sandboxIsolation: "vm"`: o Flow Spec, Builder UI, perfis `.afcodesandbox.json` e codegen carregam `sandboxVmImageId`, `sandboxVmRunner`, `sandboxVmArgs`, `sandboxVmRunnerManifest`, `sandboxVmImage`, `sandboxVmImageManifest`, `sandboxVmEngine`, `sandboxVmProfile`, `sandboxVmMemory` e `sandboxVmCpus`; o Studio possui presets gerenciados iniciais de VM para Python e Node, aplicando runner, args, imagem, engine, manifestos, perfil e limites de forma atômica, além de uma verificação local de prontidão do runner/imagem/manifestos que resolve comando, manifestos e arquivo de imagem sem executar código do usuário, validando protocolo, engine, imageId, tamanho declarado, SHA-256 opcional e capabilities hardened quando declarados; o runtime gerado cria workspace temporário, envia contrato `agent-flow-vm-runner.v1` por stdin/stdout JSON para o runner local configurado por nó ou por `AGENT_FLOW_CODE_VM_RUNNER`, preservando `language`/`inlineSource`/`sourcePath` para o runner executar Python, Node ou Bash/Shell, registra `vm`/`microvm`, runner, imagem, manifestos, engine, image id, perfil e política no `custom.sandbox` e no `execution_log`, falha de forma rastreável quando o runner não está configurado/disponível e também falha fechado com `vm_runner_unverified_isolation` quando o runner retorna `ok=true` sem declarar `providesVmIsolation=true`, salvo override explícito `AGENT_FLOW_CODE_VM_ALLOW_UNVERIFIED_ISOLATION`. Isso é uma ponte local para QEMU/Firecracker/Cloud Hypervisor ou outro runner próprio; também já existe scaffold local de imagem QEMU por `vm-image:scaffold` com cloud-init, guest agent, scripts de build/boot, transportador SSH, manifestos e governança sem secrets/paths locais, scaffold microVM direct-kernel para Firecracker/Cloud Hypervisor com preparo local de rootfs/kernel ou firmware/seed, manifestos e preflights, contrato de homologação `.afvmhomologation.json`, receita oficial local `vm-image:microvm-recipe` para preparar, homologar, empacotar e publicar localmente imagem microVM, além do empacotador local `.afvmimagebundle` com manifestos sanitizados, cópia de imagem e artefatos obrigatórios de boot como `kernel`, `firmware`, `initrd` e `seed.iso`, SHA-256 verificado para imagem/artefatos e `runner-kit` portátil com verificador local, scripts de ambiente relativos ao pacote, runner de referência opt-in para validar o contrato Python localmente, runner QEMU de preflight para validar binário, imagem/artefatos de boot/hash e plano Q35/microVM sem executar código, runner Firecracker/Cloud Hypervisor de preflight direct-kernel para validar binário, rootfs, kernel/firmware, seed e plano de comando/config sem executar código, suporte a transporte externo explícito para encaminhar o contrato ao guest agent e `agent-flow-vm-guest-agent.py` para embutir na imagem VM como executor guest-side do contrato por stdin/stdout JSON. O runner de referência declara `providesVmIsolation=false`; os runners QEMU/microVM falham fechado sem transporte e só devem declarar `guest_vm` quando o transporte realmente alcança a VM; o smoke real Docker/QEMU já provou build/boot/SSH/guest agent neste host, e o gate `test:vm-microvm-real-smoke` cobre dry-run, preflight real com artefatos fornecidos e boot launch smoke opt-in para Firecracker/Cloud Hypervisor. O artefato binário microVM pronto/homologado para distribuição depende da execução da receita com rootfs/kernel reais e da publicação escolhida pelo operador.
- Plano do Studio Local 100% local documentado em `docs/local-studio-plan.md`, cobrindo fluxo contínuo `Builder Visual -> Studio Local -> Aprovar Versão -> API Docker`, tema claro/escuro, grafo, runs, state inspector, node IO, timeline, logs e traces locais.
- Especificações de UI/UX do Studio Local documentadas em `docs/ux/`, cobrindo pesquisa de referências LangSmith/LangGraph Studio e n8n, varredura navegada com `agent-browser`, análise crua de screenshots logados, decisões de produto/UX para evitar regressões, regras visuais/comportamentais observadas, matriz de inputs e elementos de IA, interface alvo, modelo de interação, design system claro/escuro e roadmap visual.
- Builder UI possui tema claro/escuro persistente por `localStorage`, com tokens CSS para superfícies principais, canvas, inspector, controles, timeline e nós executados.
- Painel `Studio` inicial integrado ao Builder UI substitui o antigo painel cru de sandbox, mantendo start/stop/refresh do runtime e adicionando visão de run, métricas da sessão, timeline de eventos, seleção de evento, Node IO inferido, payload bruto, state inspector, transcript, eventos brutos, runtimes ativos e logs.
- Painel `Studio` possui faixa `Status global do Studio` no topo, resumindo sessão atual, runs locais, eventos visíveis/totais, nó em foco e falha mais recente, com CTA primária contextual para iniciar o Studio, criar sessão, enviar turno ou abrir o nó/falha relevante.
- Painel `Studio` possui gate de aprovação integrado ao fluxo operacional, mostrando status da aprovação LangGraph/API Docker, versão, hash, cobertura `flow/assets`, artefato, evidência de run/sessão/eventos e ações guiadas para preparar sandbox, registrar aprovação e gerar runtime final somente quando a aprovação estiver válida; o endpoint de aprovação aceita evidência resumida do Studio, sanitiza para manter apenas IDs/contagens/tipo de evento/nó e flags sem payload bruto/secrets, grava no `.agent-flow/langgraph-sandbox-approval.json` e copia a evidência para o runtime final aprovado.
- Painel `Execução ao vivo` mantém o progresso por nó calculado sobre todos os eventos do agente filtrado, sem perder o restante do grafo quando a timeline é filtrada por nó; ao selecionar um evento da timeline, o nó correspondente fica destacado, expõe evento/phase e o clique no nó filtra a timeline e abre o evento mais recente daquele nó.
- Timeline do Studio possui empty state acessível com CTA única contextual: `Mostrar todos` quando filtros escondem os eventos, `Iniciar Studio` sem runtime, `Criar sessão` com runtime ativo sem sessão ou `Enviar turno` quando a sessão já existe.
- Builder API persiste snapshots iniciais de runs locais do Studio em `flows/{flowId}/.agent-flow/studio-runs/`, expõe `/flows/{flowId}/studio-runs` e `/flows/{flowId}/studio-runs/{runId}`, salvando sessão, transcript, events, logs, métricas, snapshots derivados de state por evento e diffs incrementais.
- Builder UI lista runs locais no painel `Studio`, salva snapshots após avanço de sessão/turno/finalização e permite recarregar um run persistido para replay básico da timeline, Node IO, transcript, state inspector e diff do evento selecionado.
- Comparação de runs do Studio ganhou diffs semânticos por nó (state/output), filtro por nó e metadados de cenário para comparação (esquerda/direita/alterado).
- Comparação de runs do Studio agora agrega modo `live/mock/pinned`, eventos pinados/mock, tokens, custo estimado e resumo de regressão (`pass/warn/fail`) com motivos, indicação de comparação pinado vs real e inbox local/exportável/compartilhada file-backed de alertas de regressão em `.afregressionalerts.json` e `.agent-flow/regression-alerts/inbox.afregressionalerts.json`, reconhecimento governado com `acknowledgedBy`/`acknowledgedRole` e bloqueio backend/UI de `viewer`, sync central opcional por `AGENT_FLOW_REGRESSION_ALERTS_CENTRAL_URL`/`AGENT_FLOW_REGRESSION_ALERTS_CENTRAL_TOKEN` com ações visuais `Central`/`Sync central`, token somente no header, status sem URL/token e sem runs brutas, payloads de nó ou secrets.
- Cenários do Studio carregam thresholds de regressão para crescimento de tokens, custo e duração, além de limites explícitos por tipo de nó para nós alterados e diffs de state/output; a UI persiste esses limites, fixtures preservam o mapa por tipo, a execução envia na metadata da sessão e a comparação aplica os valores do candidate.
- Cenários do Studio possuem `sourceContext` normalizado para origem por tipo (`manual`, `checkpoint_fork`, `node_debug`, `debug_replay`, importações), agente, run principal, run base/candidato, sessão, nó e evento; a UI mostra essa origem no card/lista, filtra cenários por tipo/agente/run, executa lote filtrado, permite selecionar os cenários filtrados para dataset, fixtures e metadata preservam o contexto, e cenários promovidos de replay/fork/debug ficam rastreáveis por agente/run sem depender apenas de tags.
- Cenários do Studio possuem critérios locais reutilizáveis com modos textuais `contém`, `não contém`, `começa com`, `termina com`, `igual`, `regex` e modos estruturais `JSON path existe`/`JSON path preenchido`, persistidos no cenário, enviados na metadata, preservados em fixture de replay e aplicados ao resumo do lote; a UI possui biblioteca local de evaluators com salvar/aplicar/remover/exportar/importar pacote `.afevaluators.json`, permitindo reaproveitar rubricas simples ou compostas entre cenários. Evaluators compostos suportam múltiplas regras com operador `todas`/`qualquer`, e o cenário salva uma cópia das regras aplicadas para fixture/dataset não dependerem da biblioteca local.
- Cenários do Studio possuem primeira camada de templates visuais aplicáveis ao formulário (`Caminho feliz`, `Bloqueio de safety`, `Replay com pins` e `Contrato JSON`), preenchendo input, tags, evaluator local, pins e thresholds sem persistir automaticamente antes da revisão do usuário.
- Studio Local também possui evaluators HTTP externos/LLM-as-judge no mesmo painel de cenários: a UI configura endpoint, paths de `pass`/`reason`/`score`/`verdict`, score mínimo, timeout e rubrica; a Builder API expõe `POST /evaluators/external`, o cenário/fixture/metadata preserva `evaluatorKind` e `evaluatorExternal`, e a execução individual, fixada, em lote e por dataset aguarda o judge antes de marcar pass/fail.
- Studio Local possui datasets locais por flow com seleção multi-cenário no painel, versão do conjunto, execução como lote filtrado, histórico local de execuções com resumo/hash, versão/hash do flow, agregação experimental de taxa média de sucesso, tendência contra execução anterior, melhor/pior execução, contagem de drift de flow, painel compacto de histórico experimental, dashboard agregado por flow com datasets/runs/OK global/regressões/drift, painel histórico dedicado com tendência entre snapshots, delta de OK/pass/runs, melhor/pior snapshot, drift de flow e janela histórica, export `.afexperiments.json`/`.afexperiment-dashboard.json` e snapshots históricos backend em `flows/{flowId}/.agent-flow/experiment-dashboard-history/history.json` via `GET`/`POST` em `/flows/{flowId}/experiment-dashboard-history`; export/import `.afdataset.json` preserva cenários e histórico.
- Cenários do Studio agora diferenciam cenário fixado, favorito e último cenário executado: o seletor mostra badges, o card explica os atalhos e o painel possui ações rápidas para executar o selecionado, o fixado, o favorito ou repetir o último cenário com `lastUsedAt` atualizado após execução individual, em lote ou dataset.
- Studio Local possui fila de anotação/revisão por flow, alimentada por resultados de lote, com status `pendente`/`aprovado`/`reprovado`, responsável, revisor, nota local opcional, filtros por status/responsável, ações de assumir, aprovar e reprovar pendências visíveis em lote pelo revisor local, identidade local configurável de revisor, policy local `permissionPolicy` (`open` ou `assignee_only`) com papéis locais `owner`/`reviewer`/`viewer`, enforcement backend dessa policy quando a requisição traz `x-agent-flow-actor` ou ator injetado pela auth local do Builder, bloqueando `viewer`, bloqueando reviewer fora do item atribuído em `assignee_only` e exigindo `owner` para alterar política, deduplicação por run/lote, persistência em `localStorage`, sincronização backend file-backed em `flows/{flowId}/.agent-flow/annotation-queue/queue.afannotations.json` via `GET`/`PUT`/`POST merge` em `/flows/{flowId}/annotation-queue`, sync central multiusuário opcional por `GET /flows/{flowId}/annotation-queue/central` e `POST /flows/{flowId}/annotation-queue/sync-central` usando `AGENT_FLOW_ANNOTATION_QUEUE_CENTRAL_URL`/`AGENT_FLOW_ANNOTATION_QUEUE_CENTRAL_TOKEN`, ações visuais `Central`/`Sync central`, status sem URL/token, token somente no header, request sem token no corpo e sem envio de runs brutas, export/import com merge de `.afannotations.json` no formato `agent-flow-builder.annotation-queue.v1`, contadores de responsáveis/revisões, trilha de auditoria `auditEntries`/`auditCount`, histórico compacto no Studio, snapshots históricos locais/exportáveis em `.afannotationhistory.json` no formato `agent-flow-builder.annotation-queue-history.v1` sem payload bruto dos itens e conflitos de revisão (`conflictCount`, `openConflictCount`, `conflicts`) quando revisores diferentes alteram o mesmo item, guardando `existingSnapshot`/`incomingSnapshot` completos apenas no pacote operacional da fila, thread/atribuição de curadoria por conflito com ações `Assumir`/`Liberar`, lease configurável por `AGENT_FLOW_ANNOTATION_CONFLICT_CURATION_LEASE_HOURS`, liberação automática de lease vencido, histórico compacto `curationThread.events`, diferenças compactas para restaurar a versão compartilhada ou recebida sem perder veredito, saída observada, razões e metadados do item, export/diff governado de conflitos por `GET /flows/:flowId/annotation-queue/conflicts-review` e `POST /flows/:flowId/annotation-queue/conflicts-review/diff` no formato `.afannotation-conflicts.json`/`.afannotation-conflicts-diff.json` sem itens completos, snapshots, vereditos, razões, saídas observadas ou secrets, além de `resolutionHistory` compacta com hashes da saída observada, refs descartadas sem payload bruto e prevenção de reabertura quando a mesma revisão descartada volta por sync.
- Cenários do Studio importam/exportam fixture JSON de replay com input, thresholds, checkpoint, pins ativos/stale e metadata de execução pronta para reaproveitamento.
- Cenários/fixtures do Studio podem ser executados em lote sequencial com resumo por cenário, sessão, duração, erro e comparação automática com o run anterior do mesmo cenário.
- O lote de cenários agora gera relatório JSON exportável (`agent-flow-builder.scenario-batch-report.v1`) com resumo, hash determinístico, resultados por cenário e aprovação local do lote quando não há erro de execução/comparação nem regressão `fail`.
- Cenários forkados agora enviam `restore.state` na metadata e o runtime FastAPI gerado restaura o estado inicial do novo thread usando checkpointer real por `sourceSessionId` quando disponível, com fallback para o snapshot serializado do Studio.
- Studio Local mostra a estratégia esperada de restauração do cenário forkado (`checkpointer -> snapshot` ou `snapshot`) e a origem observada emitida pelo runtime (`checkpointer` ou `snapshot`) no card do cenário e no `State inspector`.
- Checkpoints forkados do Studio agora registram assinatura de compatibilidade com `flowId`, versão, hash local do flow, hash de projeto/assets quando disponível e hash do nó; a UI mostra `ok/parcial/incompatível`, fixtures exportam essa assinatura e a execução bloqueia restore quando versão/hash/nó divergem.
- Causality do Studio foi incorporada ao fluxo persistido e ao grafo da UI (`upstream`, `impact`, `cascata`) com destaque de eventos/nós no replay, incluindo trilha visual no painel de grafo.
- Studio Local possui entrada guiada por schema para turnos, derivando `user_message`, campos top-level adicionais, grupos aninhados de objetos tipados e arrays de itens simples ou estruturados do schema de estado carregado, filtrando campos operacionais comuns, validando string/enum/number/boolean/JSON, exibindo o payload real enviado ao runtime e preservando payload extra em cenários/fixtures.
- Studio Local possui bloco `Saída do turno` com visão renderizada da última resposta do agente, visão raw do transcript/evento/output inferido associado e primeira camada de schema guiado de saída com aderência por campo.
- Studio Local possui primeira camada de `Tool manager`, derivada do flow e dos eventos atuais, inventariando nós de código, HTTP, MCP/sidecar, banco, arquivo, RAG e transformações com status observado, último evento, contrato input/output, pin e ação de debug por nó, além de painel agregado `Falhas de sandbox` por nó/modo/sandbox/executor com contagem, último evento, causa provável, próxima ação e navegação direta para o evento/nó.
- Studio Local possui painel `Secrets locais`, derivado das env vars de LLM do flow, com campos mascarados/reveláveis, secrets customizados, persistência local por flow em `localStorage`, envio dos valores ao processo de sandbox apenas no start e exportação manual controlada de `.env` local, incluindo somente envs configuradas e não protegidas pelo sandbox.
- Inspector de nós Safety possui Safety Harness com avaliação local contra a política atual do nó, provider HTTP externo opcional, histórico backend file-backed em `flows/{flowId}/.agent-flow/safety-harness/runs.json`, rotas `GET /flows/{flowId}/safety-harness/runs`, `POST /flows/{flowId}/safety-harness/evaluate`, `PUT /flows/{flowId}/safety-harness/runs/{runId}/review` e `POST /flows/{flowId}/safety-harness/diff`, dashboard local por flow/nó com taxa de bloqueio, pendências, origem local/externa, categorias recentes e último run, além de revisão humana simples (`pendente`/`aceito`/`rejeitado`) por run usando a identidade local de revisor e papel `owner`/`operator`/`reviewer`/`viewer` no UI e no histórico, bloqueio backend de `viewer` mesmo em chamada direta, ação rápida para aceitar/rejeitar o próximo pendente do nó, export `.afsafetyhistory.json` com flags de governança sem input bruto, headers externos ou secrets, comparação visual `Comparar histórico` gerando `.afsafetyhistory-diff.json` sem `inputPreview`, `matchedText`, headers externos, payloads brutos de provider ou secrets, sync central opcional por `AGENT_FLOW_SAFETY_HARNESS_CENTRAL_URL`/`AGENT_FLOW_SAFETY_HARNESS_CENTRAL_TOKEN` com ações visuais `Central`/`Sync central`, token somente no header, status sem URL/token, pacote sanitizado sem `inputPreview`/`matchedText` e `resolutionHistory` compacta para colisões do mesmo run, mantendo a versão mais recente por `updatedAt` com `resolvedRole` sem expor input bruto, matched text, headers externos ou secrets.
- Runtime manual, runtime gerado, sandbox LangGraph e bundle multiagente gerado aceitam provider HTTP externo de safety por `.env`: `SAFETY_PROVIDER_ENABLED`, `SAFETY_PROVIDER_URL`, `SAFETY_PROVIDER_TIMEOUT_SECONDS`, `SAFETY_PROVIDER_FAIL_CLOSED` e `SAFETY_PROVIDER_HEADERS_JSON`. A chamada envia `text`, `stage`, `nodeId`, política do nó e decisão local, preserva fallback local, usa fail-open por padrão e grava metadados externos no payload de safety.
- Studio Local possui primeira camada de governança runtime/secrets, derivada dos secrets locais e env vars do flow, mostrando prontidão de envs obrigatórias, pendências, envs customizadas, valores enviados no start, políticas fixas aplicadas pelo sandbox local, contagem de envs exportáveis e perfis compartilháveis de política de secrets com seleção por flow, padrão de workspace, export/import `.afsecrets.json`, export/import junto do workspace e bloqueio de envs protegidas no `.env` local.
- Studio Local possui painel `Eventos ao vivo`, com transporte selecionável WebSocket/SSE, conexão/desconexão do runtime, status visual, contador de eventos recebidos, último `seq` e mesclagem incremental dos eventos por `seq` na timeline atual.
- Studio Local possui painel `Execução ao vivo`, derivado da timeline atual, com progresso por nó do flow, último evento, status visual por nó, spans observados, duração agregada, tokens/custo e uso por provider/modelo.
- Studio Local possui painel `Telemetria histórica`, carregado do backend a partir de `.agent-flow/studio-runs`, agregando runs/eventos com usage, tokens, custo, erros, provider/modelo, último run/evento observado por provider, janela configurável, alertas locais por limite de tokens/custo via rota `/flows/{flowId}/studio-runs/provider-telemetry`, dashboard histórico dedicado por provider via `GET`/`POST`/`POST merge`/`POST diff`/`GET central`/`POST sync-central` em `/flows/{flowId}/provider-telemetry-dashboard-history`, snapshots persistidos em `.agent-flow/provider-telemetry-dashboard-history/history.json`, tendência/deltas/maior custo/maior uso, export/comparação visual `.afprovidertelemetryhistory.json`/`.afprovidertelemetryhistory-diff.json`, sync central opcional por `AGENT_FLOW_PROVIDER_TELEMETRY_DASHBOARD_HISTORY_CENTRAL_URL` com token somente no header/status sem URL ou token e diff governado sem payload de telemetria bruto, eventos brutos, state bruto, sink URLs/tokens ou secrets, além de inbox local/exportável/compartilhada file-backed por flow para registrar, reconhecer, reter, reabrir por nova ocorrência, sincronizar e exportar alertas em `.aftelemetryalerts.json`, com reconhecimento governado por `acknowledgedBy`/`acknowledgedRole` e bloqueio backend/UI de `viewer`, persistência compartilhada em `.agent-flow/provider-telemetry-alerts/inbox.aftelemetryalerts.json`, sync central opcional por `AGENT_FLOW_PROVIDER_TELEMETRY_ALERTS_CENTRAL_URL`/`AGENT_FLOW_PROVIDER_TELEMETRY_ALERTS_CENTRAL_TOKEN`, roteamento lógico local configurável por `AGENT_FLOW_PROVIDER_TELEMETRY_ALERT_ROUTES`, canal/motivo/regra aplicada por alerta, breakdown visual por rota, escalonamento local por `AGENT_FLOW_PROVIDER_TELEMETRY_ALERT_ESCALATION_POLICY` com nível/prioridade/motivo, política de entrega por `AGENT_FLOW_PROVIDER_TELEMETRY_ALERT_DELIVERY_POLICY` com prioridade mínima, limite de lote por rota e cooldown local por alerta, prontidão governada por `/provider-telemetry-alerts/delivery-readiness` e export `.afproviderdelivery.json`, dispatch externo governado por rota via `AGENT_FLOW_PROVIDER_TELEMETRY_ALERT_ROUTE_SINKS`, token de sink apenas via env/header, ações visuais `Central`/`Sync central`/`Rotas`/`Prontidão`/`Enviar rotas`, status sem URL/token e sem eventos brutos, URLs de webhook ou secrets.
- Studio Local consome `turn/stream` por SSE ou WebSocket no envio de turno e mostra painel `Resposta em streaming`, seletor de transporte, contador de tokens, origem dos chunks, texto incremental, conclusão e erro do stream antes de atualizar transcript, events e run local.
- Studio Local expõe `Idempotency-Key` visível/editável para o envio manual de turno, reutilizando a chave no cliente `turn/stream` e permitindo reproduzir/reexecutar chamadas específicas pelo playground.
- Studio Local possui painel `Jobs pós-finalização`, consultando o runtime local para listar jobs da sessão atual ou todos os jobs, carregar `/metadata.operations.jobs` para mostrar worker, retry/claim multiworker, retenção e schedules, escolher janela de telemetria, filtrar por status, exibir métricas agregadas e operacionais de sucesso/duração/atividade recente, p95, throughput, próxima execução, breakdown por status/tipo, payload/resultado/erro, salvar histórico local e compartilhável por workspace de snapshots de métricas em `.agent-flow/runtime-job-metrics-history/history.json`, exportar pacote `.afjobmetrics.json`, comparar diff governado `.afjobmetrics-diff.json`, consultar/sincronizar central opcional por `AGENT_FLOW_RUNTIME_JOB_METRICS_HISTORY_CENTRAL_URL` com token somente no header/status sem URL ou token e diff sem payload/resultado/erro bruto de jobs, acionar execução/reprocessamento individual ou em lote com atualização da timeline e executar retenção governada por prévia/limpeza de jobs terminais antigos.
- Build Docker pela UI passa a expor progresso incremental por etapa em `docker compose build` e mantém log de progresso persistido no histórico operacional.
- Studio Local ganhou painel `Contexto do nó`, acionado pelo clique/filtro de nó, reunindo status, papel causal, erro relacionado, eventos recentes, metadados do nó/LLM, prompt renderizado, input/output inferidos, estado do nó, métricas de usage/custo/duração, painel de falhas de sandbox/executor customizado com causa provável, boundary/transporte/tentativas/payload, ação direta para filtrar logs, logs estruturados de código customizado com busca, filtro por modo/status/sandbox e exportação JSON, spans estruturados, diffs e logs correlacionados.
- Studio Local infere diagnóstico operacional por nó, com causa provável, próximas ações e evidências derivadas de payload, safety, status, fase, snapshot e cadeia causal, incluindo orientações específicas por tipo de nó para LLM, safety, code, HTTP, banco, arquivo/RAG, approval, scoring e analytics.
- Studio Local exibe comparação contextual do nó selecionado quando há comparação de runs carregada, mostrando run base/candidato, presença do nó em cada run, `seq`, status/fase, contagem de diff de state/output, preview dos diffs e ações para carregar base, carregar candidato, focar o nó ou criar um cenário de debug local a partir do run candidato.
- Studio Local exibe resumo multi-camadas dentro da comparação de runs, agregando fluxo/execução, state/output, nó selecionado, eventos/erros, pins/mocks e checkpoints/cenários com status, contagem, detalhe, próximo passo operacional e ações guiadas por camada para focar nó/falha, criar cenário, exportar replay, usar pins e filtrar/executar checkpoints sem criar outro formato de run.
- A comparação do nó selecionado também exporta artefato `.afdebugreplay.json` no formato `agent-flow-builder.debug-replay-artifact.v1`, com flow/hash, comparação base/candidato, refs de diff sem `before/after`, ação recomendada de replay, checkpoints/pins sem payload bruto, cadeia causal e governança explícita sem payload bruto de nós, pins, checkpoint ou secrets; o Studio possui painel `Artefatos de replay` para importar esses pacotes, revisar recomendação, seguir roteiro visual de pacote/checkpoint/pins/cenário, carregar run base/candidato, focar o nó, promover o replay importado para um cenário local de revisão com checkpoint metadata-only e selecionar o cenário já promovido pelo hash do pacote.
- A ação `Criar cenário de debug` transforma a comparação do nó em um cenário reexecutável com checkpoint do evento candidato, tags de debug/comparação/base/candidato, compatibilidade de flow/projeto/nó e uso automático de pins quando já houver pins ativos no flow, permitindo reexecução em lote ou exportação como fixture.
- O resultado de lote de cenários exibe resumo multi-camadas agregado, cobrindo execução em lote, comparações, regressões, pins, checkpoints e último diff detalhado quando há par de runs carregado, com ações guiadas para executar cenários filtrados, exportar relatório/camadas, revisar pendências, usar pins e filtrar/executar checkpoints.
- Os resumos multi-camadas da comparação de runs e do lote de cenários podem ser exportados como `.afdebuglayers.json` no formato `agent-flow-builder.debug-layer-summary.v1`, com hash do pacote, escopo, flow, resumo, evidência mínima e governança explícita indicando exclusão de payload bruto de nós e valores de secrets; também possuem primeira camada compartilhável file-backed por flow em `.agent-flow/debug-layers/snapshots.afdebuglayers.json`, formato `agent-flow-builder.debug-layer-snapshots.v1`, rotas `GET`/`PUT /flows/:flowId/debug-layer-snapshots`, `POST /flows/:flowId/debug-layer-snapshots/merge`, `POST /flows/:flowId/debug-layer-snapshots/conflicts/:conflictId/curation`, `POST /flows/:flowId/debug-layer-snapshots/conflicts/:conflictId/resolve`, `GET /flows/:flowId/debug-layer-snapshots/central` e `POST /flows/:flowId/debug-layer-snapshots/sync-central`, ações visuais `Compartilhar resumo`, `Central` e `Sync central`, painel visual `Conflitos de camadas` para contexto duplicado com thread/atribuição local de curadoria (`Assumir`/`Liberar`), lease padrão de 24h configurável por `AGENT_FLOW_DEBUG_LAYER_SNAPSHOT_CONFLICT_CURATION_LEASE_HOURS`, liberação automática de lease vencido com `leaseExpired`/`lease_expired` e histórico compacto de eventos com papel local `owner`/`reviewer`/`viewer`, merge por `packageHash`, detecção inicial de conflitos por contexto de comparação/lote, resolução mantendo o snapshot escolhido ou mais recente, histórico governado com `resolvedRole`, bloqueio backend de `viewer` para curadoria/resolução, sync central opcional por `AGENT_FLOW_DEBUG_LAYER_SNAPSHOTS_CENTRAL_URL`/`AGENT_FLOW_DEBUG_LAYER_SNAPSHOTS_CENTRAL_TOKEN`, token somente no header, status sem URL/token e sanitização recursiva de evidências para remover chaves sensíveis e payload bruto de nó antes de salvar ou enviar.
- Replay governado possui curadoria local por revisor nos pacotes `.afreplaygovernance.json`, snapshots `.afreplayhistory.json` e diffs `.afreplayhistorydiff.json`, registrando decisão `approved`/`needs_review`/`monitor`, motivos, próxima ação e flags explícitas sem payload bruto de cenário, pins, checkpoint ou secrets.
- Histórico compartilhado de replay detecta conflitos apenas quando a mesma evidência governada de replay/checkpoint/pins/evaluator possui curadorias divergentes, preservando evolução normal do histórico sem alerta. O pacote `.agent-flow/replay-governance/history.afreplayhistory.json` expõe `conflictCount`, `openConflictCount`, refs sem payload bruto, `curationThread.events` com papel local `owner`/`reviewer`/`viewer`, lease padrão de 24h configurável por `AGENT_FLOW_REPLAY_GOVERNANCE_CONFLICT_CURATION_LEASE_HOURS`, liberação automática de lease vencido com `leaseExpired`/`lease_expired`, `resolutionHistory` com `resolvedRole` e tombstone de refs descartadas, a rota `POST /flows/:flowId/replay-governance-history/conflicts/:conflictId/curation` atualiza atribuição local (`Assumir`/`Liberar`) com bloqueio backend de `viewer`, a rota `POST /flows/:flowId/replay-governance-history/conflicts/:conflictId/resolve` mantém o snapshot escolhido também bloqueando `viewer`, `GET /flows/:flowId/replay-governance-history/conflicts-review` exporta a revisão governada e `POST /flows/:flowId/replay-governance-history/conflicts-review/diff` compara revisões; o Studio mostra `Conflitos de replay`, `Histórico de resolução`, `Exportar revisão` e `Comparar revisão` no roteiro do cenário para curar, escolher e auditar qual snapshot governado manter sem snapshots completos, payload de cenário, pins, checkpoint, input/output ou secrets.
- Studio Local permite criar fork de checkpoint/evento a partir do `State inspector`, salvando um cenário local reexecutável com origem do run, sessão, evento, snapshot, input/output e metadata de execução para rastrear a nova sessão.
- O card do cenário selecionado possui roteiro guiado de replay com etapas Origem, Compatibilidade, Restore, Pins e Execução, além de ações diretas para focar origem, aplicar filtros por origem, executar replay, ativar pins e exportar fixture guiada.
- Studio Local permite fixar input/output por nó no drill-down, persistindo pins locais por flow com origem de run/evento e indicador `atual/stale` quando a definição do nó muda; a primeira camada compartilhável file-backed de pins também existe em `.agent-flow/studio-node-pins/pins.afnodepins.json`, formato `agent-flow-builder.studio-node-pins.v1`, com rotas `GET`/`PUT /flows/:flowId/studio-node-pins`, `POST /flows/:flowId/studio-node-pins/merge`, `POST /flows/:flowId/studio-node-pins/conflicts/:conflictId/curation`, `POST /flows/:flowId/studio-node-pins/conflicts/:conflictId/resolve`, `GET /flows/:flowId/studio-node-pins/central`, `POST /flows/:flowId/studio-node-pins/sync-central`, `GET /flows/:flowId/studio-node-pins/conflicts-review` e `POST /flows/:flowId/studio-node-pins/conflicts-review/diff`, ações visuais `Carregar pins`/`Sincronizar pins`/`Exportar revisão`/`Comparar revisão`/`Central`/`Sync central`, painel `Conflitos de pins` com thread/atribuição local de curadoria (`Assumir`/`Liberar`), lease de atribuição configurável por `AGENT_FLOW_STUDIO_NODE_PIN_CONFLICT_CURATION_LEASE_HOURS`, liberação automática de conflito com lease vencido e histórico compacto de eventos antes de escolher qual candidato divergente fica ativo, export `.afnodepin-conflicts.json` e diff `.afnodepin-conflicts-diff.json` sem candidatos completos, `pins`, input/output bruto ou secrets, sync central opcional por `AGENT_FLOW_STUDIO_NODE_PINS_CENTRAL_URL`/`AGENT_FLOW_STUDIO_NODE_PINS_CENTRAL_TOKEN`, preservação de candidatos por `nodeId`/`id` com candidato mais recente ativo por nó, histórico governado de resolução com revisor, papel local `owner`/`reviewer`/`viewer`, nota, pin mantido e refs descartadas sem input/output bruto, bloqueio backend de `viewer` em curadoria/resolução de conflitos, tombstone por `id`/`nodeId`/`contentHash` para não reabrir conflito quando o mesmo candidato descartado volta por sync, visualização dessa trilha no Studio, token somente no header, status sem URL/token e redaction de chaves sensíveis antes de persistir ou enviar input/output no pacote compartilhado.
- Studio Local permite ativar mock por pins no cenário; a execução envia pins ativos na metadata da sessão e o runtime gerado aplica `pinned/mock` por nó antes de chamar LLM, código, HTTP, banco, arquivos/RAG, approval, scoring, analytics ou handlers de controle.
- Causalidade ao vivo da UI foi alinhada ao backend: usa a falha mais recente, o parent executado mais próximo antes da falha e descendentes realmente observados depois da falha.
- Ordenação de `impactedNodes` passou a seguir ordem causal/de execução, com o nó da falha primeiro e descendentes ordenados pelo primeiro evento impactado, em vez de lista alfabética.
- Topbar do Builder/Studio passou a conter overflow de toolbar localmente, evitando alargar o documento em viewport desktop comum.
- Docker Runtime Manager atualiza progresso de `docker compose build` enquanto o comando ainda está rodando, via callback de streaming no runner padrão e registro em memória consultável por `/docker-runtime/status`.
- Builder UI consulta `/docker-runtime/status` durante `Build`, mesmo com a operação ocupada, para atualizar painel de progresso/logs antes da resposta final do comando.
- Docker Runtime Manager registra operações Docker ativas por runtime, expõe `/docker-runtime/cancel` e aborta builds em andamento por `AbortController`, marcando o resultado final como `canceled` em status, progresso e histórico.
- Builder UI mostra ação `Cancelar` durante `Build`, mantendo polling de progresso até o backend confirmar o cancelamento e registrando o evento no histórico operacional.
- Histórico Docker aceita filtros por nível (`level`: error/warning/info/success), etapa textual do progresso (`progressStage`) e status/severidade do progresso (`progressStatus`: running/done/error/warning/info/canceled); a Builder UI expõe os filtros `Nível`, `Etapa` e `Progresso` no painel `Artefato` e aplica o mesmo recorte à lista de progresso visível.
- Painel `Artefato` exibe alertas operacionais persistentes para `Build`, `Up` e `Smoke`, derivados do histórico Docker, com estados `ok`, `pendente`, `desatualizado` e `erro`, incluindo indicação de regressão quando a última execução falha após sucesso anterior.
- Parser de progresso Docker estima percentuais mesmo quando o output do BuildKit não traz contagem explícita, usando etapa inferida, contexto de linhas `#N DONE` e evento final em 100% para builds concluídos.
- Shell principal da Builder UI ganhou breakpoint responsivo abaixo de 760px: topbar passa a quebrar em duas linhas com toolbar rolável, workspace vira fluxo vertical sem largura mínima desktop, canvas mantém altura útil e inspector fica abaixo do canvas.
- Builder UI possui atalhos globais iniciais: `Ctrl/Cmd+S` salva flow/prompts/schemas sujos, `Ctrl/Cmd+Enter` valida o flow atual, `Esc` limpa a seleção do canvas, `A` foca a paleta, `F` reenquadra o canvas, `I` abre a paleta filtrada para inserir etapa conectada quando há nó/aresta selecionado, `C` conecta os nós selecionados em sequência visual e `Delete`/`Backspace` remove a seleção do canvas quando o foco não está em campo editável.
- Auditoria visual automatizada em Playwright (`npm run test:ui-theme`) prepara workspace isolado em `.tmp/ui-theme-workspace`, sobe API/UI em portas dedicadas, usa runner Docker mockado apenas por `AGENT_BUILDER_DOCKER_RUNNER=ui-audit-mock`, percorre tema claro/escuro em viewport desktop e compacta, abre as abas principais do inspector, valida atalhos iniciais, cobre busca/filtro/foco de nós no canvas, grupos colapsáveis por família sem alterar `agent.flow.json`, catálogo local com salvar prompt atual, aplicar HTTP JSON tool em nó existente, criar flow por template de agente, salvar bloco composto, editar metadados, adicionar/reordenar etapa, criar conexão interna, exportar/importar pacote `.afcatalog.json`, editar prompts/schemas internos de skill local, usar ações guiadas de schema interno, reaplicar a skill curada no nó LLM e validar curadoria antes de salvar, edição visual de `runtime.manifest.json`, mapa de bundle multiagente com endpoints resolvidos, renderiza runs locais persistidos com timeline/cadeia causal/state/transcript, cobre diagnóstico automático de safety, prompt renderizado, usage, custo, logs estruturados, spans e fork de checkpoint no drill-down de nó LLM, cobre status global de loading/erro fora do Docker, estados internos de loading/erro em `Arquivos`, `Runtime` e `Studio`, aprovação desatualizada bloqueando `API Docker` na toolbar e `Gerar runtime final` no gate do Studio, o fluxo `LangGraph` -> `Aprovar` -> `API Docker` até o painel de artefato Docker final e exercita build/loading, inspect running, smoke com erro e inspect stopped.
- Painéis `Arquivos`, `Runtime` e `Studio` exibem estados internos acessíveis de loading/erro, usando `role=status` para carregamento, `role=alert` para falhas e mensagens locais no painel sem depender apenas do status global.
- Canvas da Builder UI possui finder compacto com busca textual, filtro por tipo, contador de resultados, chips de nós, foco/seleção do nó, comando `Organizar grafo`, ações de seleção `Alinhar linha`/`Distribuir`, resumo guiado de debug no painel `Editar` para o nó selecionado e ação `Depurar` para abrir o Studio Local já filtrado no nó/evento selecionado, recalculando posições por ordem de execução ou por seleção sem alterar o contrato do `agent.flow.json` além de `position`.
- Debug guiado por nó possui primeira camada de caminho multi-camadas, combinando topologia do flow e run carregada para mostrar upstream, downstream, condições de rota, tipo/status/último evento do vizinho, cadeia causal/impacto quando houver falha e abertura direta do vizinho no Studio.
- Canvas da Builder UI possui grupos colapsáveis locais por família semântica (`Controle`, `Safety`, `IA/RAG`, `Integrações`, `Lógica`, `Outros`), escondendo nós/arestas do grupo recolhido e reabrindo automaticamente o grupo quando um nó oculto é escolhido pelo finder.
- Canvas da Builder UI possui barra contextual de seleção com ações para focar seleção, inserir uma nova etapa conectada após um nó ou no meio de uma aresta, conectar nós selecionados em sequência pela ordem visual, duplicar nó editável, aplicar/limpar tags em lote nos nós selecionados, abrir prompt/schema vinculados e remover nós/arestas selecionados sem passar pelo JSON; as ações principais também estão disponíveis na paleta de comandos para reduzir dependência de mouse. Quando há multiseleção, o painel `Editar` mostra edição em lote para tags, prompt/schema de nós LLM, stage de safety gates, timeout de nós de execução compatíveis, linguagem/modo/input/result de nós `code`, método/url/body/response de nós `http_request`, input/output de `transform_json`, SQL/table/params/data/result/maxRows de nós de banco, source/content/maxChars de arquivo, collection/query/context/topK/chunkSize de RAG, decision/result/valores de approval e campos de scoring/analytics, além de presets locais por flow, biblioteca local reutilizável entre flows, painel de governança do preset selecionado com origem, escopos, usos, último uso, flow de origem e ações, exportação/importação `.afbatchpresets.json` com formato versionado e merge validado para salvar, reaplicar e remover conjuntos de ações de lote.
- Builder UI possui aba `Visão` no inspector com roteiro de criação do agente em etapas sequenciais, mapa visual do fluxo principal derivado dos nós/arestas, status por etapa clicável, seção de canais/subagentes derivada de `runtime.manifest.json` com rotas, endpoint de sessão e status validado/rascunho, mapa de completude do agente, blocos Entrada/LLM/Ferramentas/Controle/Estado/Evidência com criação direta quando faltam LLM, safety ou ferramenta/code, configuração guiada com CTAs contextuais para criar ou revisar LLM, safety, ferramenta/code e evidência local, lista de ações sugeridas por lacuna e diagnósticos de validação clicáveis, resumo operacional e próxima melhor ação conectada ao Flow, Arquivos, Validação, Runtime ou Studio.
- A seção `Canais e subagentes` da aba `Visão` possui drill-down por agente do manifesto, mostrando flowPath, runtime interno, rota de metadata, endpoint de sessões, contrato, resourceName, checks de rota/flow/endpoint e CTA para Runtime, além de empty state guiado quando não há manifesto ou agentes configurados.
- Builder UI possui paleta de comandos local em `Ctrl/Cmd+K` e botão na topbar, com busca por ações de workspace, navegação de abas, canvas, entrega, execução local, criação de nós soltos e ações contextuais da seleção do canvas como inserir etapa conectada no fluxo, conectar seleção em sequência, focar, duplicar, depurar, alinhar, distribuir, abrir prompt/schema, aplicar/limpar tags e remover seleção, reduzindo dependência de mouse para operações frequentes.
- Aba `Arquivos` possui editor visual recursivo para JSON Schema de objetos: lista propriedades, altera tipo, descrição, obrigatoriedade, enum e `items.type` de arrays, edita objetos aninhados e objetos dentro de arrays, adiciona/remove campos, cria/remove e edita definições em `$defs`, controla `$ref`, `additionalProperties` como booleano ou schema tipado, adiciona/remove entradas de `oneOf`, `allOf` e `anyOf`, exibe validação semântica local para refs quebradas, required inconsistente, arrays sem items, enum duplicado, composições inválidas e estruturas malformadas, navega do diagnóstico para o controle visual afetado, oferece correções guiadas para criar `$defs`, remover/deduplicar/normalizar `required`, preencher ou corrigir `items`, inicializar `properties`, corrigir `additionalProperties`, converter composições em lista, remover composições vazias, normalizar enum e deduplicar enum, inclui biblioteca inicial de padrões reutilizáveis para mensagem de conversa, citação RAG, chamada de tool e erro estruturado, e sincroniza o textarea JSON usado pelo salvamento do schema.
- Editor visual de JSON Schema possui painel `Governança do schema`, com contagens de propriedades, obrigatórios, `$defs`, refs, enums, objetos, arrays, composições, profundidade, `additionalProperties`, padrões reutilizáveis aplicados/pendentes, diagnósticos e export `.afschemagovernance.json` no formato `agent-flow-builder.schema-governance.v1` sem conteúdo bruto do schema ou valores de secrets, além de biblioteca local/exportável/importável de padrões de schema em `.afschemapatterns.json` no formato `agent-flow-builder.schema-pattern-library.v1`, permitindo salvar o schema visual atual como padrão, ver preview/diff local de adições e conflitos antes de aplicar, exportar/importar/revisar diff governado `.afschemapatterndiff.json` com verificação de hash do pacote, salvar/exportar/importar snapshots históricos `.afschemapatternhistory.json` sem schema bruto, comparar/exportar/importar/revisar diff histórico `.afschemapatternhistorydiff.json`, aplicar padrões por merge não destrutivo de propriedades, `$defs`, required e composições, aprovar/deprecar/remover padrões locais, calcular assessment exportável de prontidão/risco por padrão sem schema bruto, registrar revisões colaborativas estruturadas por curador sem schema bruto, assumir/liberar thread de curadoria por responsável local com lease padrão de 24h configurável por `AGENT_FLOW_SCHEMA_PATTERN_CURATION_LEASE_HOURS`, liberação automática de lease vencido e histórico compacto `curationThread.events` sem schema bruto, acompanhar uso/último uso, sincronizar biblioteca/histórico por backend file-backed em `.agent-flow/schema-patterns/` com ações explícitas de carregar/sincronizar compartilhado e executar sync central multiusuário opcional por `AGENT_FLOW_SCHEMA_PATTERN_CENTRAL_URL` com token somente no header, status sem URL/token, limite de payload e resumo governado de ação, hash, origem, contagens de entrada/existente/adicionado/atualizado, conflitos abertos e flags de exclusão de secrets/schema bruto, merge estrutural automático de schemas brutos quando propriedades/`$defs`/`additionalProperties` não colidem, plano assistido sanitizado para colisões reais com nomes de campos/defs sem schema bruto, revisão em colunas Atual/Recebido/Schema visual, diff sanitizado por área com itens novos, somente atuais e colisões, diff textual local lado a lado quando o merge tem os dois schemas brutos na resposta, export `.afschemapatternmergeplan.json`, ação visual `Usar schema visual` para aplicar o schema aberto no editor como merge manual auditado, preservando snapshots compactos de conflito sem o schema bruto, permitindo marcar conflitos como resolvidos por identidade local de curador/papel mantendo a versão atual da biblioteca e evitando reabrir a mesma divergência compacta resolvida quando ela volta por sync. Quando a auth local do Builder está ativa, mutações de biblioteca/histórico de padrões de schema também exigem papel mínimo `reviewer` e área `schemas` no backend.
- Conflitos compartilhados de padrões de schema também possuem pacote governado de revisão em `GET /flows/:flowId/schema-pattern-library/conflicts-review` e comparação em `POST /flows/:flowId/schema-pattern-library/conflicts-review/diff`, com ações visuais `Exportar revisão`/`Comparar revisão`, arquivos `.afschemapattern-conflicts.json`/`.afschemapattern-conflicts-diff.json`, comparação de resumo/conflitos/decisões e bloqueio de pacote importado que contenha schema bruto, diff textual local, `items`, payloads ou secrets.
- Canvas marca nós e arestas alterados no draft em relação ao último flow carregado/salvo, com borda/linha tracejada de aviso para orientar o que ainda está pendente de salvamento.
- Canvas marca nós com pins/checkpoints defasados como `stale` quando o hash salvo diverge da definição atual do nó, e destaca as arestas conectadas para mostrar impacto visual no grafo.

## Verificado

```bash
npm run validate:flow
npm run codegen:reference
npm run codegen:sandbox
npm run codegen:manifest
npm run typecheck
npm run test:baseline
npm run test:generated
npm run test:manifest
npm run test:parity
npm run test:mvp-main-path
npm run test:docker-runtime-smoke
npm run test:portable-runtime
npm run test:portable-runtime-auth
npm run test:portable-runtime-bundle
npm run test:onboarding-docs
npm run test:local-models-runbook
npm run test:release-privacy-audit-contract
npm run test:external-integrations-homologation
npm run test:isolation-levels-runbook
npm run test:multiagent-operations-runbook
npm run test:collaboration-conflict-contract
npm run test:expanded-plan-audit
npm run test:expanded-plan-requirement-audit
npm run test:expanded-plan-gate-matrix
npm run test:expanded-plan-evidence-report
npm run test:desktop
npm run test:builder-auth-corporate
npm run test:multiagent-postgres
npm run test:builder-api
npm run test:ui-theme
npm run test:codegen
npm run build:builder-ui
npm audit --audit-level=moderate
```

Resultado: todos passaram.

Também foi validado localmente:

- `npm run test:mvp-main-path` passou após a inclusão do verificador end-to-end do caminho principal do MVP, cobrindo edição mínima do flow, validação, bloqueio de runtime aprovado sem aprovação, evidência de debug em `studio-runs`, geração de sandbox LangGraph, aprovação por hash com evidência sanitizada do Studio sem payload bruto/secrets, geração de runtime FastAPI/Docker aprovado com approval JSON embarcado, auditoria de ZIP pronta, manifesto/exportação com aprovação e evidência embarcadas, preparo de `.env`, build/up via runner Docker mockado, smoke HTTP do runtime final, bundle multiagente aprovado por manifesto e smoke HTTP do agente no bundle.
- `npm run test:docker-runtime-smoke` passou em 2026-07-03 e novamente em 2026-07-04 com `.env` ausente no runtime gerado, `docker compose config`, `docker compose up -d --build`, health com `db_ok=true` e `cache_ok=true`, metadata `reference-interview`, sessão/start/turn/transcript/events/finish, job final `succeeded` e logs sem padrões críticos.
- `npm run test:portable-runtime` passou em 2026-07-04, gerando o runtime `reference-interview`, copiando o pacote FastAPI para um diretório consumidor fora do workspace do Studio, removendo a origem gerada, validando metadados sem path absoluto do repo, README/Compose operacionais e executando `pytest` a partir do pacote copiado.
- `npm run test:portable-runtime-auth` passou em 2026-07-04, gerando e copiando o runtime para fora do workspace do Studio, ativando `AUTH_ENABLED=true`, validando bloqueio sem chave, scoped keys para metadata/sessões/auth, criação/start de sessão com chave operacional, `/auth/keys`, `/auth/audit` persistido e ausência de valores brutos de chave nos payloads e no JSONL.
- `npm run test:portable-runtime-bundle` passou em 2026-07-04, gerando o bundle multiagente por `runtime.manifest.json`, copiando o pacote para um diretório consumidor fora do workspace do Studio, removendo a origem gerada, validando metadata/`bundle.json` sem path absoluto do repo, Compose com `api`/`worker`, `app/worker.py` raiz com import isolado por agente e executando `pytest` a partir do pacote copiado.
- `npm run test:onboarding-docs` passou em 2026-07-04, validando `docs/user-guide.md`, `docs/operator-guide.md` e `docs/developer-guide.md` como onboarding separado para usuario, operador e desenvolvedor, com MVP principal 100%, plano expandido em andamento, LangSmith Cloud opcional, local-first e sem tokens/headers/payloads brutos.
- `npm run test:local-models-runbook` passou em 2026-07-04, validando `docs/local-models-runbook.md` para Ollama local, variaveis dummy, `model-setup`, GPU NVIDIA, distribuicao de imagens de modelo e a evidencia externa `real-model-gpu-matrix`, sem tratar a matriz real de hardware como concluida.
- `npm run test:release-privacy-audit-contract` passou em 2026-07-04, validando `docs/release-privacy-audit.md` como contrato da auditoria final de privacidade/release, sem executar scan de PII/secrets por rodada e mantendo `final-release-privacy-audit` como evidencia real exigida antes de publicacao/release.
- `npm run test:external-integrations-homologation` passou em 2026-07-04, validando `docs/external-integrations-homologation.md` como contrato de homologacao de IdP, sessao central, auditoria central, diretorio, registries e LangSmith opcional, sem chamar servicos externos e mantendo `real-corporate-idp`/`managed-langsmith-provider` como evidencias reais dependentes do operador.
- `npm run test:isolation-levels-runbook` passou em 2026-07-04, validando `docs/isolation-levels-runbook.md` para escolha entre processo local, workspace efemero, processo dedicado, container, container hardened, VM e microVM, com politica fail-closed quando runner VM nao prova isolamento e smokes reais ainda opt-in.
- `npm run test:multiagent-operations-runbook` passou em 2026-07-04, validando `docs/multiagent-operations-runbook.md` para operacao de bundles complexos, conceitos flow/agent/route/handoff/memory/orchestration step, `debug_trace`, smoke por agente/agregado, `agentIsolation` e gate PostgreSQL compartilhado.
- `npm run test:collaboration-conflict-contract` passou em 2026-07-04, validando `/collaboration/conflicts` e `/collaboration/conflicts/diff` com contrato `sourceActions` para revisão/diff/curadoria/resolução por área, bloqueio de `viewer` e pacotes governados sem schema/prompt/input/output/header/token/payload bruto.
- `npm run test:expanded-plan-audit` passou em 2026-07-04, extraindo 12 frentes de `projeto-final.md`, confirmando itens pendentes e critérios por frente, preservando a separação `MVP principal = 100%` versus plano expandido em andamento, bloqueando claim indevido de 100% do plano total e validando a matriz mínima de gates de evidência.
- `npm run test:expanded-plan-requirement-audit` passou em 2026-07-04, validando `docs/expanded-plan-requirement-audit.md` e emitindo `agent-flow-builder.expanded-plan-requirement-audit.v1` com IDs estaveis, linhas de origem e hashes por requisito das 12 frentes, sem texto bruto, payloads, headers, tokens ou secrets.
- `npm run test:expanded-plan-gate-matrix` passou em 2026-07-04, validando `docs/release-gate-matrix.md` como matriz governada de gates diarios, governanca do plano expandido, release local, VM/microVM opt-in e evidencias externas, mantendo LangSmith Cloud opcional, scan de PII/secrets apenas para release final/pedido explicito e bloqueio de 100% total sem auditoria requisito por requisito.
- `npm run test:expanded-plan-evidence-report` passou em 2026-07-04, validando o contrato `agent-flow-builder.expanded-plan-evidence-report.v1` gerado por `npm run report:expanded-plan-evidence`, com status `in_progress`, matriz de gates, 12 frentes, política de conclusão por auditoria requisito-a-requisito e sem tokens/headers de auth ou payloads brutos.
- `npm run test:desktop` passou em 2026-07-04, validando o empacotamento Electron local com assets relativos da UI, preload `__AGENT_FLOW_DESKTOP__`, contrato de URL da Builder API, binário Electron disponível e health da API sem abrir janela.
- `npx tsx --test apps/builder-api/src/collaboration-conflicts.test.mjs` passou em 2026-07-04, cobrindo a visão agregada de conflitos de colaboração, exportação governada, comparação por `/collaboration/conflicts/diff`, rejeição de pacote com payload/token bruto e ausência de schema/prompt/input/output/header/token/payload/secret/candidatos completos nos pacotes.
- `docs/quickstart-10-min.md` adiciona o primeiro quickstart de onboarding para subir Builder API/UI, validar `reference-interview`, testar sandbox/runtime local, aprovar/gerar API Docker, operar o pacote fora do Studio, usar modelo local Ollama e rodar gates rápidos; `docs/release-gate-matrix.md` separa os gates por uso antes de release. `npm run test:expanded-plan-audit` valida que esse guia mantém LangSmith Cloud opcional e separa MVP principal de plano total expandido.
- `npm run test:builder-auth-corporate` passou em 2026-07-03 e novamente em 2026-07-04, cobrindo status sanitizado, probe corporativo, session service, introspecção central obrigatória/fail-closed, audit sink, diretório de grupos, persistência hash-only e ausência de URL/token/chave bruta nos payloads retornados.
- `npm run typecheck`, `npm run test:builder-api`, `npm run build:builder-ui` e `npm run test:ui-theme -- --grep "builder governance auth policy"` passaram após o login OIDC local do Builder, cobrindo autorização por code + PKCE, callback com sessão local, status visual de OIDC e botão `Entrar OIDC`.
- `npm run typecheck`, `npx tsx --test --test-name-pattern "optional local auth|JWT bearer auth" apps/builder-api/src/workspace-governance.test.mjs`, `npm run build:builder-ui`, `npm run test:ui-theme -- --grep "builder governance auth policy"` e `git diff --check` passaram após renovação/logout local da sessão do Builder, cobrindo rotação do token `afbs_*`, rejeição do token antigo, logout hash-only no backend e botões `Renovar`/`Encerrar sessão` desabilitados quando não existe sessão local.
- `npm run typecheck` e `npx tsx --test --test-name-pattern "optional local auth" apps/builder-api/src/workspace-governance.test.mjs` passaram após a persistência central local de sessões do Builder, cobrindo `AGENT_FLOW_BUILDER_AUTH_SESSION_PATH`, reload de sessão por nova instância da API, arquivo com hash de sessão sem token `afbs_*` bruto/chave bruta, rotação removendo o hash antigo e logout limpando o registro persistido.
- `npm run typecheck`, `npm run test:builder-api`, `npm run build:builder-ui`, `npm run test:ui-theme -- --grep "builder governance auth policy"` e `git diff --check` passaram após o refresh OIDC e logout OIDC federado avançado, cobrindo discovery de `end_session_endpoint`, `/builder-auth/oidc/session/refresh`, refresh token apenas em memória, rotação da sessão local com rejeição do token antigo, `/builder-auth/oidc/logout-url`, `client_id`/`post_logout_redirect_uri`/`state`, modo básico sem hint, modo avançado com `id_token_hint` de sessão, `/builder-auth/oidc/logout-callback`, validação de state, rejeição de replay/estado inválido e ausência de token bruto na resposta de callback.
- `npx tsx --test --test-name-pattern "OIDC|optional local auth|JWT bearer auth" apps/builder-api/src/workspace-governance.test.mjs`, `npm run test:builder-api`, `npm run typecheck`, `npm run build:builder-ui` e `npm run test:ui-theme -- --grep "builder governance auth policy"` passaram após a sincronização inicial de grupos corporativos, cobrindo `AGENT_FLOW_BUILDER_AUTH_JWT_GROUPS_CLAIM`, grupos em JWT/OIDC, propagação para sessão local `afbs_*`, persistência em chaves file-backed e visibilidade do claim `grupos` na aba `Governança`.
- `npx tsx --test --test-name-pattern "JWT bearer auth" apps/builder-api/src/workspace-governance.test.mjs` passou após o diretório local de grupos do Builder, cobrindo `AGENT_FLOW_BUILDER_AUTH_GROUP_DIRECTORY`, status governado de diretório de grupos, enriquecimento de grupo por ator sem claim no JWT e autorização de schema via política local aplicada ao grupo enriquecido.
- `npm run typecheck` e `npm run test:ui-theme -- --grep "studio runs with data render"` passaram após os atalhos de cenário favorito/último uso no Studio, cobrindo renderização e ação em tema claro e escuro.
- `npm run typecheck`, `npm run test:ui-theme -- --grep "studio runs with data render"` e `npm run build:builder-ui` passaram após a faixa `Status global do Studio`, cobrindo o resumo operacional, CTA inicial e abertura da falha em tema claro e escuro.
- `npm run typecheck`, `npm run test:ui-theme -- --grep "studio runs with data render"` e `npm run build:builder-ui` passaram após o destaque do nó selecionado pela timeline no painel `Execução ao vivo`, cobrindo `node_failed` em tema claro e escuro.
- `npm run typecheck`, `npm run test:ui-theme -- --grep "studio runs with data render"` e `npm run build:builder-ui` passaram após o empty state com CTA contextual da timeline, cobrindo filtro sem eventos e ação `Mostrar todos` em tema claro e escuro.
- `npm run typecheck`, `npx tsx --test --test-name-pattern "Studio scenarios" apps\builder-api\src\server.test.ts` e `npm run build:builder-ui` passaram após lease/expiração de curadoria para conflitos compartilhados de cenários/datasets/evaluators, cobrindo prazo padrão de 24h, env `AGENT_FLOW_STUDIO_SCENARIO_CONFLICT_CURATION_LEASE_HOURS`, liberação automática e sinalização visual do vencimento.
- `npm run typecheck`, `npx tsx --test --test-name-pattern "Studio node pins" apps\builder-api\src\server.test.ts`, `npm run build:builder-ui` e `npm run test:ui-theme -- --grep "studio runs with data render"` passaram após lease/expiração de curadoria para conflitos compartilhados de pins de nó, cobrindo prazo padrão de 24h, env `AGENT_FLOW_STUDIO_NODE_PIN_CONFLICT_CURATION_LEASE_HOURS`, liberação automática e sinalização visual do vencimento.
- `npm run typecheck`, `npx tsx --test --test-name-pattern "annotation queue" apps\builder-api\src\server.test.ts`, `npm run build:builder-ui` e `npm run test:ui-theme -- --grep "studio runs with data render"` passaram após lease/expiração de curadoria para conflitos da fila de anotação, cobrindo prazo padrão de 24h, env `AGENT_FLOW_ANNOTATION_CONFLICT_CURATION_LEASE_HOURS`, liberação automática e sinalização visual do vencimento.
- Builder API em `http://127.0.0.1:3333/health`.
- Builder UI servindo HTML em `http://127.0.0.1:5173`.
- Builder API listando e validando `reference-interview` via HTTP.
- Sandbox em `http://127.0.0.1:8090`, com smoke test de `POST /sessions`, `start`, `turn`, `transcript` e `events`.
- Builder API validando a promoção LangSmith/LangGraph: sem aprovação retorna 409, após aprovação gera runtime aprovado com `.agent-flow/langgraph-sandbox-approval.json`.
- Sandbox LangGraph validado em venv isolada `.venv/langgraph`, sem instalar `langgraph-cli` no ambiente FastAPI do runtime final: `pytest` do pacote sandbox passou, `langgraph --version` retornou 0.4.30 e `langgraph dev --no-browser --host 127.0.0.1 --port 2024 --no-reload --allow-blocking` subiu `/docs` em smoke temporário.
- Container final validado com `docker compose up -d --build` em `generated/reference-interview-runtime/`; `/health` respondeu `db_ok=true` e `cache_ok=true`, e o smoke HTTP criou sessão, executou `start`, `turn`, retornou `ECHO` e gravou eventos.
- Dockerfile final com runner Node validado por `docker compose build api` em `generated/reference-interview-runtime/` e `docker run --rm reference-interview-runtime-api node --version`, retornando `v20.19.2`.
- Builder UI validado em navegador local em `http://127.0.0.1:5173`: tema escuro aplicado, painel `Studio` aberto, runtime local ativo em `http://127.0.0.1:8090`, sessão criada, turno enviado, timeline/state inspector atualizados e tema claro reaplicado pelo toggle.
- Builder API validada com runner Docker falso em `npm run test:builder-api`, cobrindo status/configure-ports/prepare-env/build/up/down/inspect/history do runtime aprovado sem executar Docker real.
- Rota `/docker-runtime/status` validada por `app.inject` no workspace real para `generated/reference-interview-runtime`, retornando target `fastapi-runtime`, recurso `sessions`, `Dockerfile`, compose, portas API/Postgres/Redis e links `http://127.0.0.1:8080/docs` e `/openapi.json`.
- Rota `/docker-runtime/status` validada por `app.inject` com `runtimeUrl=http://127.0.0.1:9000`, retornando links customizados para `http://127.0.0.1:9000/docs` e `/openapi.json`.
- Rota `/docker-runtime/history` validada por `app.inject` no workspace real para `generated/reference-interview-runtime`, retornando lista vazia quando ainda não há histórico local.
- Rotas de runs locais do Studio validadas em `npm run test:builder-api`, cobrindo lista vazia, gravação de snapshot, snapshotCount, diff por node, listagem de resumo, carregamento completo e arquivo persistido em `.agent-flow/studio-runs/`.
- Rota `/flows/reference-interview/studio-runs` validada por `app.inject` no workspace real, retornando `200` e lista vazia quando ainda não há snapshot local.
- `npm run typecheck`, `npm run test:builder-api` e `npm --workspace @agent-flow-builder/builder-ui run build` passaram após o painel `Contexto do nó` e a nova ordenação causal.
- Builder UI validado via Playwright em `http://127.0.0.1:5173` com viewport `1440x900`: aba `Studio` abriu, clique no nó `start` preencheu `Contexto do nó` com 4 cards, tema alternou entre escuro/claro preservando o painel e `body.scrollWidth` permaneceu dentro do viewport.
- `npm run test:builder-api` cobre progresso incremental do Docker build: enquanto o `POST /docker-runtime/build` ainda está pendente, `GET /docker-runtime/status` retorna `lastOperation=build`, `lastStatus=running` e `progress` já preenchido.
- `npm run test:builder-api` cobre cancelamento de build Docker: `/docker-runtime/cancel` aborta o runner pendente via signal, o build retorna `lastStatus=canceled`, o progresso termina com status `canceled` e o filtro de histórico por `status=canceled` encontra a entrada cancelada.
- `npm run test:builder-api` cobre o bundle multiagente como artefato Docker: `/docker-runtime/status` reconhece target `runtime-manifest-bundle`, lista agentes gerados, `/docker-runtime/smoke` com `agentId` executa `/metadata`, cria sessão e percorre `start`, `turn`, `transcript` e `events` na rota montada do agente, e `/docker-runtime/smoke-all` executa o mesmo percurso para dois agentes do bundle em sequência.
- `npm run test:builder-api` cobre filtros de histórico/progresso Docker por `level=error`, `level=success`, `progressStage=metadata`, `progressStatus=done`, `progressStatus=canceled` e rejeição de `level`/`progressStatus` inválidos.
- `npm run test:builder-api` cobre percentuais estimados no progresso Docker ao vivo e 100% no evento final de build concluído.
- Builder UI validado por screenshots Playwright temporários em `1440x900` e `390x844`, tema claro e escuro; a correção responsiva removeu o corte lateral mobile do shell principal.
- `npm run typecheck` e `npm run build:builder-ui` passaram após a inclusão dos atalhos globais iniciais.
- `npm run test:codegen` cobre execução real de nó `code` TypeScript por arquivo, com `codeDependencies` materializado em `app/code/package.json`, execução real de `codeExecution: "http"` contra servidor HTTP local, execução real de `codeExecution: "mcp"` contra servidor MCP stdio local e execução real de `codeExecution: "sidecar"` por subprocesso Python no pytest gerado, além dos testes já existentes de Python e JavaScript; os casos customizados validam também `execution_log` e `span`.
- `npm run test:baseline`, `npm run test:generated`, `npm run test:manifest`, `npm run test:parity`, `npm run test:codegen`, `npm run test:builder-api`, `npm run build:builder-ui` e `npm run typecheck` passaram após a inclusão da primeira camada de jobs pós-finalização, cobrindo criação de `post_finish_summary`, endpoints `/jobs`, métricas `/jobs/metrics`, execução manual do job, retry/reprocessamento, lote nativo em `/jobs/run-pending` e `/jobs/retry-failed`, worker CLI, serviço `worker` no compose gerado, eventos de conclusão/falha/retry, paridade OpenAPI/comportamental e bundles multiagente gerados.
- `npm run test:baseline`, `npm run test:generated`, `npm run test:manifest`, `npm run test:parity`, `npm run test:codegen`, `npm run test:builder-api`, `npm run build:builder-ui`, `npm run typecheck`, `npm run validate:flow` e `git diff --check` passaram após a inclusão de autenticação avançada por `AGENT_API_KEYS`, rate limit local e auditoria em memória, cobrindo chave legada de acesso total, múltiplas chaves com scopes, bloqueio por permissão insuficiente, leitura/escrita de sessões, leitura/escrita de jobs, metadata protegida quando auth está ativo, autenticação WebSocket por `api_key`, bloqueio HTTP 429 por credencial/escopo e `GET /auth/audit` sem registrar valores brutos de chave.
- `python -m pytest -q examples/reference-interview-runtime -k "auth_rate_limit_and_audit_log"`, `python -m pytest -q generated/reference-interview-runtime -k "auth_rate_limit_and_audit_log"` e `python -m pytest -q generated/reference-runtime-bundle/agents/reference-interview -k "auth_rate_limit_and_audit_log"` passaram após a persistência local de auditoria de auth, cobrindo `AUTH_AUDIT_PATH`, escrita append-only JSONL, reload no startup, resposta `/auth/audit` com `persistent/path`, preservação de eventos `allowed`/`rejected`/`rate_limited` e ausência do valor bruto da chave no arquivo persistido.
- `python -m pytest -q examples/reference-interview-runtime -k "api_keys_file_supports_local_rotation_without_restart or scoped_api_keys or auth_rate_limit"`, `python -m pytest -q generated/reference-interview-runtime -k "api_keys_file_supports_local_rotation_without_restart"` e `python -m pytest -q generated/reference-runtime-bundle/agents/reference-interview -k "api_keys_file_supports_local_rotation_without_restart"` passaram após `AGENT_API_KEYS_PATH`, cobrindo arquivo JSON local de chaves, item desabilitado, escopo `auth:read`, `/auth/keys` sem valores brutos e rotação de `operator-v1` para `operator-v2` sem recriar o app.
- `python -m pytest -q examples/reference-interview-runtime -k "revocation_metadata or expiration_metadata or api_keys_file"`, `python -m pytest -q generated/reference-interview-runtime -k "revocation_metadata or expiration_metadata or api_keys_file"` e `python -m pytest -q generated/reference-runtime-bundle/agents/reference-interview -k "revocation_metadata or expiration_metadata or api_keys_file"` passaram após a revogação local persistente por `key_id`, cobrindo `AGENT_API_REVOKED_KEY_IDS`, `AGENT_API_REVOKED_KEY_IDS_PATH`, identificador qualificado por origem, rejeição com `Chave de API revogada.`, `/auth/keys` com `revoked=true` e ausência de valores brutos de chave no baseline, runtime gerado e bundle.
- `npm run codegen:reference`, `npm run codegen:sandbox`, `npm run codegen:manifest`, `npm run test:codegen`, `npm run test:baseline`, `npm run test:generated`, `npm run test:manifest`, `npm run test:parity`, `npm run typecheck`, `npm run validate:flow` e `git diff --check` passaram após a propagação da revogação para templates, runtime gerado, sandbox LangGraph e bundle multiagente. `git diff --check` emitiu apenas avisos LF/CRLF do workspace.
- `python -m pytest -q examples/reference-interview-runtime -k "agent_scoped_api_keys or scoped_api_keys or api_keys_file"`, `python -m pytest -q generated/reference-interview-runtime -k "agent_scoped_api_keys or scoped_api_keys or api_keys_file"` e `python -m pytest -q generated/reference-runtime-bundle/agents/reference-interview -k "agent_scoped_api_keys or scoped_api_keys or api_keys_file"` passaram após scopes por agente, cobrindo autorização com `agents:<agent_id>:*`, bloqueio de `agents:other-agent:*`, escopo `auth:read` por agente e `/auth/keys` com `agent_id` sem valores brutos. Também passaram `npm run test:baseline` (18 testes), `npm run test:generated` (19 testes), `npm run test:manifest` (19 testes), `npm run test:parity` (25 paths), `npm run test:codegen`, `npm run typecheck`, `npm run validate:flow` e `git diff --check`.
- `python -m pytest -q examples/reference-interview-runtime -k "expiration_metadata or agent_scoped_api_keys or api_keys_file"`, `python -m pytest -q generated/reference-interview-runtime -k "expiration_metadata or agent_scoped_api_keys or api_keys_file"` e `python -m pytest -q generated/reference-runtime-bundle/agents/reference-interview -k "expiration_metadata or agent_scoped_api_keys or api_keys_file"` passaram após expiração local de chaves, cobrindo `expires_at`, `expiresAt`, rejeição de chave expirada com 403, chave ativa aceita e `/auth/keys` com `expired`/`expires_at` sem valores brutos. Também passaram `npm run test:baseline` (19 testes), `npm run test:generated` (20 testes), `npm run test:manifest` (20 testes), `npm run test:parity` (25 paths), `npm run test:codegen`, `npm run typecheck` e `npm run validate:flow`.
- `npm run typecheck`, `npm run build:builder-ui` e `npm run test:ui-theme -- --grep "studio runs with data render"` passaram após o painel visual de auditoria de auth no Studio, cobrindo tema claro/escuro, `STUDIO_RUNTIME_API_KEY` em `Secrets locais`, envio de `X-Agent-API-Key`, carregamento de `/auth/audit`, contadores por status e ausência do valor bruto da chave na UI.
- `npm run typecheck`, `npm run build:builder-ui` e `npm run test:ui-theme -- --grep "studio runs with data render"` passaram após o painel visual `Chaves de auth`, cobrindo tema claro/escuro, carregamento de `/auth/keys`, contadores de ativas/revogadas/expiradas, origem/escopos sem valores brutos, envio do header `X-Agent-API-Key`, export governado `.afauthkeys.json` com `agent-flow-builder.runtime-auth-keys.v1` sem chave bruta nem caminhos locais e ação `Revogar no próximo start` preenchendo `AGENT_API_REVOKED_KEY_IDS` em Secrets locais. O build emitiu apenas o aviso conhecido de chunk maior que 500 kB.
- `python -m pytest -q examples/reference-interview-runtime`, `npm run test:generated`, `npm run test:manifest`, `npm run test:parity`, `npm run test:codegen` e `npm run typecheck` passaram após a inclusão de `POST /sessions/{session_id}/turn/stream`, cobrindo emissão SSE de `turn_started`, `token`, `turn_completed`, `stream_closed`, reuso da mesma chave de idempotência no endpoint normal e paridade OpenAPI com 18 paths.
- `npm run typecheck`, `npm run build:builder-ui` e `npm run test:ui-theme` passaram após o consumo visual de `turn/stream` no Studio, cobrindo envio de turno por SSE mockado, `Idempotency-Key` editável propagado ao header, painel `Resposta em streaming`, contador de tokens, texto final e conclusão do stream em tema claro e escuro.
- `npm run typecheck`, `npm run build:builder-ui` e `npm run test:ui-theme` passaram após a exportação controlada de `.env` local no Studio, cobrindo download manual, conteúdo com secrets configurados, exclusão de env vazia/protegida, perfis compartilháveis de política de secrets, export/import `.afsecrets.json`, aplicação de perfil no flow, padrão de workspace, proteção de env por política e multisseleção determinística por chips do finder.
- `npm run typecheck`, `npm run build:builder-ui` e `npm run test:ui-theme` passaram após a camada de datasets locais e evaluators compostos no Studio, cobrindo seleção explícita de cenários, salvar dataset, card de resumo/versionamento, exportação `.afdataset.json`, remoção, reimportação por input oculto com histórico preservado, atualização para múltiplos cenários, execução como lote filtrado, histórico local com hash de relatório e versão/hash do flow, agregação experimental, comparação contra execução anterior com `flow mudou/flow igual`, criação de evaluator composto com duas regras, exportação `.afevaluators.json` com `rules` e preservação em fixture/metadata.
- `npm run typecheck`, `npm run test:builder-api`, `npm run build:builder-ui` e `npm run test:ui-theme -- --grep "studio runs with data render"` passaram após o painel/export `.afexperiments.json`, o dashboard `.afexperiment-dashboard.json` e o dashboard histórico dedicado de experimentos, cobrindo tendência, pass/ok médio, drift de flow, melhor/pior execução, dashboard agregado por flow, análise histórica backend em `agent-flow-builder.experiment-dashboard-history.v1`, deltas entre snapshots, melhor/pior snapshot histórico, arquivo versionado `agent-flow-builder.dataset-experiments.v1`/`agent-flow-builder.experiment-dashboard.v1` e persistência/exportação de evaluator estrutural `JSON path existe`.
- `npm run typecheck`, `npm run build:builder-ui`, o cenário Playwright filtrado `studio runs with data render` e `npm run test:ui-theme` com 24 cenários passaram após a fila local de anotação/revisão, cobrindo adição a partir de resultado de lote, responsável, status aprovado, export `.afannotations.json`, import/merge do pacote e filtro por status/responsável no formato `agent-flow-builder.annotation-queue.v1`.
- `npm run typecheck`, `npm run test:builder-api`, `npm run build:builder-ui`, o cenário Playwright filtrado `studio runs with data render` e `npm run test:ui-theme` com 24 cenários passaram após evaluators HTTP/LLM-as-judge no Studio, cobrindo rota `/evaluators/external`, configuração visual do judge, export `.afevaluators.json` com `kind: "llm_judge"`, preservação de endpoint/rubrica/paths e execução do cenário aguardando veredito externo.
- `npm run typecheck`, `npm run build:builder-ui` e `npm run test:ui-theme` passaram após os cards operacionais por agente no bundle Docker, cobrindo rota `/reference-interview`, metadata, endpoint de sessões, link direto de metadata e agente selecionado para smoke.
- `npm run typecheck`, `npm run test:codegen`, `npm run test:builder-api`, `npm run build:builder-ui` e `python -m pytest -q generated/reference-runtime-bundle` passaram após o contrato de storage compartilhado do bundle e o smoke agregado multiagente.
- `npx tsx --test --test-name-pattern "multiagent manifest bundle" packages/codegen-langgraph/src/codegen.test.ts`, `npm run codegen:manifest` e `python -m pytest -q generated/reference-runtime-bundle` passaram após o contrato de isolamento operacional por agente, cobrindo `.runtime-manifest/agent-isolation.json`, `bundle.json#agentIsolation`, metadata raiz, health raiz, `routePrefix` único, idempotência por prefixo de rota, storage por `agent_id`, scopes `agents:<agent_id>:...`, import isolado dos runtimes Python e ausência de secrets/env values/payloads no artefato de isolamento.
- `npm run test:multiagent-postgres` passou gerando um bundle temporário de dois agentes, subindo Postgres real em Docker (`postgres:16-alpine`) e executando o teste de integração com `AGENT_FLOW_TEST_POSTGRES_URL`, provando escrita de ambos os agentes no mesmo banco compartilhado.
- `npm run test:ui-theme` passou com 21 cenários: tema claro/escuro em viewport `1440x900` e `390x844`, cobrindo render inicial, atalhos `A`/`F`, abas `Editar/Visão/Arquivos/Validação/JSON/Artefato/Runtime/Studio/Catálogo`, roteiro de criação do agente na aba `Visão`, mapa visual do fluxo principal, status de canais/subagentes com navegação para Runtime, overview de completude com ações sugeridas, próxima melhor ação e diagnóstico de validação navegável para o nó afetado, busca/filtro/foco de nós no canvas, organização automática do grafo, alinhamento/distribuição de seleção, aplicação/limpeza de tags em lote com busca por tag, edição em lote de schema para LLM, stage para safety gates, code/HTTP e famílias avançadas Transform/Banco/Arquivo/RAG/Approval/Scoring/Analytics com save/apply de preset local, biblioteca local de preset reutilizável, exportação/importação `.afbatchpresets.json`, resumo guiado de debug no inspector do nó com diagnóstico, causalidade, pin e eventos recentes, salto `Depurar` do canvas para o Studio filtrado pelo nó, barra contextual de seleção com foco/duplicação/remoção/abertura de prompt, grupos colapsáveis e autoexpansão pelo finder, marcação dirty de nó editado, marcação stale por pin defasado, catálogo local com busca textual, filtro por origem, versão/revisão visível, histórico/diff de revisão, comparação selecionável de revisão, restauração de revisão, seeds, salvar prompt atual, salvar nó atual como tool/skill local, salvar bloco composto, curar bloco local com metadados, adicionar/reordenar etapa, criar conexão interna, exportação/importação de pacote `.afcatalog.json`, editar prompts/schemas internos de skill local, reaplicar a skill curada no nó LLM, validar antes de salvar, aplicar HTTP JSON tool em nó existente, aplicar skill de perguntas estruturadas no nó LLM e criar flow por template de agente com nós `retrieve_context`/`generate_questions`, edição visual de metadados de prompt/schema, propriedades top-level e propriedades aninhadas de JSON Schema com enum, `items.type`, required, `$defs`, `$ref`, `additionalProperties`, `oneOf`, `anyOf`, aplicação de padrão reutilizável `ConversationMessage` e painel de consistência semântica acusando refs locais quebradas, focando o campo visual afetado e resolvendo por ação guiada na aba `Arquivos`, edição visual e salvamento de `runtime.manifest.json`, mapa de bundle multiagente com rota `/reference-interview/sessions` e runtime `agents/reference-interview`, estados internos de loading/erro nos painéis `Arquivos`, `Runtime` e `Studio`, entrada guiada por schema no Studio com preview do payload incluindo `user_message`, string extra, inteiro e booleano derivados de schema, bloco `Saída do turno` com render e raw, painel `Secrets locais` com mascaramento/revelação e secret customizado, ausência de overflow horizontal/texto cortado, runs locais com dados em tema claro/escuro, origem observada de restore de checkpoint, pin local de input/output por nó, logs estruturados com filtro e exportação JSON, toggle de mock por pins em cenário, thresholds de regressão por cenário, biblioteca local de evaluators com exportação/importação `.afevaluators.json`, dataset local com exportação/importação `.afdataset.json`, importação/exportação de fixture JSON de replay, ação de execução em lote, comparação de runs com veredito de regressão, status global de loading/erro fora do Docker, aprovação desatualizada bloqueando `API Docker`, geração visual `LangGraph` -> `Aprovar` -> `API Docker` com controles `Status`, `Preparar .env`, `Build`, `Up`, `Smoke` e `Down`, alertas operacionais persistentes de Build/Up/Smoke, e estados Docker de loading/progresso, container running, smoke com erro e container stopped.
- `npm run typecheck`, `npm run test:codegen`, `npm run build:builder-ui` e `npm run test:ui-theme` passaram após a primeira execução real de `codeExecution: "runtime_adapter"` por endpoint, com teste Python gerado validando payload `adapter`, `execution_log.mode = runtime_adapter`, `span.name = custom_code.runtime_adapter` e correção do endpoint WebSocket `turn/stream/ws` no template gerado.
- `npm run typecheck`, `npx tsx --test --test-name-pattern "runtime_adapter" packages/codegen-langgraph/src/codegen.test.ts`, `npm run codegen:reference`, `npm run codegen:sandbox` e `npm run codegen:manifest` passaram após adicionar a primeira execução de `runtime_adapter` pelo runner VM local, cobrindo flow sem URL HTTP, fonte por `codeInline`, metadados `adapter` no contrato VM, `sandboxIsolation=vm`, `providesVmIsolation=true`, trace `execution_log.mode=runtime_adapter`, `sandbox_base_isolation=external_endpoint` e runtime gerado atualizado.
- `npm run test:codegen`, `npm run test:generated`, `npm run test:manifest`, `npm run test:baseline`, `npm run test:parity`, `npm run test:builder-api`, `npm run typecheck` e `npm run build:builder-ui` passaram após a migração aditiva de schema; `test:generated`, `test:manifest` e `test:codegen` agora cobrem inicialização contra schema SQLite legado sem `agent_id`.
- Smoke Docker real passou em `generated/reference-interview-runtime` com `docker compose up -d --build` contra volume PostgreSQL persistente antigo: `/health` retornou `ok`, `/metadata` retornou `flow_id=reference-interview`, `POST /sessions`, `start`, `turn`, `transcript`, `events` e `finish` funcionaram, e o serviço `worker` processou o job `post_finish_summary` de `pending` para `succeeded`.
- `npm run typecheck`, `npm run test:codegen`, `npm run build:builder-ui`, `npm run test:generated`, `npm run test:manifest`, `npm run test:parity`, `npm run test:ui-theme` e `git diff --check` passaram após a inclusão da política de segurança dos executores externos de nós `code`; os testes gerados cobrem retry HTTP/`runtime_adapter`, allowlist de estado, redaction antes do envio, limite de payload registrado, `payload_policy` e metadados de tentativas no `execution_log`.
- `npm run typecheck`, `npm run test:codegen`, `npm run build:builder-ui`, `npm run test:generated`, `npm run test:manifest`, `npm run test:parity` e `npm run test:ui-theme` passaram após a inclusão dos metadados `custom.sandbox`/`sandbox_*` no runtime gerado; os testes cobrem `external_endpoint` para HTTP/`runtime_adapter`, `subprocess_stdio` para MCP/sidecar, Tool manager com ambiente de execução e filtro/exportação de logs por sandbox no Studio.
- `npm run typecheck`, `npm run test:codegen`, `npm run codegen:reference`, `npm run codegen:sandbox`, `npm run codegen:manifest`, `npm run build:builder-ui`, `npm run test:generated`, `npm run test:manifest`, `npm run test:parity`, `npm run test:ui-theme` e `git diff --check` passaram após a primeira camada de `sandboxIsolation: "ephemeral_workspace"`; o teste de sidecar cobre contrato, env allowlist, trace `ephemeral_workspace`, `base_isolation=subprocess_stdio` e confirma que a escrita do subprocesso não aparece em `app/code` persistente.
- `npm run typecheck`, `npm run validate:flow`, `npm run test:codegen`, `npm run codegen:reference`, `npm run codegen:sandbox`, `npm run codegen:manifest`, `npm run test:baseline`, `npm run test:generated`, `npm run test:manifest`, `npm run test:parity`, `npm run test:builder-api`, `npm run build:builder-ui`, `npm run test:ui-theme` e `git diff --check` passaram após a primeira camada de Safety Harness configurável; o teste gerado cobre regra custom de privacidade com `default_and_custom`, bloqueio antes do LLM, payload `safe_redirect`, rule id/label, severidade, ação, match e ausência de evento `llm_called`, e a paridade confirma que o runtime manual e o gerado expõem os mesmos metadados de safety.
- `npm run typecheck`, `npm run build:builder-ui`, `npm run test:ui-theme` com 22 cenários e `git diff --check` passaram após a biblioteca local de políticas de Safety no inspector, cobrindo salvar, exportar pacote `agent-flow-builder.safety-policy-profiles.v1`, remover, importar `.afsafety.json`, aplicar política ao nó e ausência de overflow horizontal.
- `npm run typecheck` e o cenário Playwright filtrado `node debug path` passaram após a primeira camada de caminho multi-camadas no debug guiado do nó, cobrindo upstream/downstream, condições `safety.decision`/`safety.blocked`, abertura do vizinho `llm_step` no Studio e ausência de overflow horizontal.
- `npm run typecheck`, `npm run build:builder-ui`, `npm run test:ui-theme` com 23 cenários e `git diff --check` passaram após a comparação contextual do nó selecionado, cobrindo comparação `run-ui-audit-ok` vs `run-ui-audit-error`, status `alterado`, diff de state/output, criação de cenário de debug a partir de `input_safety_check` no run candidato e render sem overflow no painel do Studio em tema claro e escuro.
- `npm run typecheck`, `npm run build:builder-ui`, `npm run test:ui-theme -- --grep "studio runs with data render"` e `git diff --check` passaram após os resumos multi-camadas de debug na comparação de runs e no lote de cenários, cobrindo fluxo/execução, state/output, pins/mocks, checkpoints/cenários, atualização da camada `Nó selecionado` ao escolher `input_safety_check`, agregação de execução/comparações/pins/checkpoints em lote, download/validação de pacotes `.afdebuglayers.json` para comparação/lote, export `.afdebugreplay.json` do nó selecionado sem payload bruto e import do replay no painel governado com foco do nó.
- `npm run typecheck`, `npm run build:builder-ui` e `npm run test:ui-theme -- --grep "assets panel edits prompt and schema metadata visually"` passaram após o painel de governança de schema, cobrindo métricas de governança, padrão `ConversationMessage` aplicado, export `.afschemagovernance.json`, formato `agent-flow-builder.schema-governance.v1`, hash, diagnósticos e flags de exclusão de conteúdo bruto/secrets.
- `npm run typecheck`, `npm run test:builder-api`, `npm run build:builder-ui` e `npm run test:ui-theme -- --grep "catalog panel saves local assets and applies a tool"` passaram após a governança exportável, biblioteca compartilhável e conflitos iniciais do catálogo, cobrindo painel de checks por item, export `.afcataloggovernance.json`, formato `agent-flow-builder.catalog-governance.v1`, governança agregada da biblioteca com export `.afcataloglibrarygovernance.json`, formato `agent-flow-builder.catalog-library-governance.v1`, prontidão portátil, flags de exclusão de conteúdo bruto/secrets, botões `Sincronizar compartilhado`/`Carregar compartilhado`, pacote `agent-flow-builder.catalog-library.v1` em `.afcataloglibrary.json`, hash de sync compartilhado, detecção de conflito por item, snapshots compactos sem conteúdo bruto, curador local com papel, bloqueio de resolução para Viewer e decisão visual/API `Manter biblioteca` com `resolvedBy`, `resolution=keep_library` e nota.
- `npm run typecheck` e `npx tsx --test --test-name-pattern "Builder API saves and applies local catalog items" apps/builder-api/src/server.test.ts` passaram após o sync central multiusuário opcional do catálogo local, cobrindo `AGENT_FLOW_CATALOG_CENTRAL_URL`, `AGENT_FLOW_CATALOG_CENTRAL_TOKEN`, `GET /catalog/central`, `POST /catalog/sync-central`, token somente no header, status sem URL/token, request sem token no corpo, envio apenas da biblioteca compartilhável de itens locais, merge da biblioteca retornada e ações visuais `Central`/`Sync central`.
- `npm run typecheck`, `npx tsx --test --test-name-pattern "Builder API syncs annotation queue with the central service without leaking token" apps/builder-api/src/server.test.ts`, `npm run test:builder-api`, `npm run build:builder-ui` e `npm run test:mvp-main-path` passaram após o sync central multiusuário opcional da fila de anotação, cobrindo `AGENT_FLOW_ANNOTATION_QUEUE_CENTRAL_URL`, `AGENT_FLOW_ANNOTATION_QUEUE_CENTRAL_TOKEN`, `GET /flows/:flowId/annotation-queue/central`, `POST /flows/:flowId/annotation-queue/sync-central`, token somente no header, status sem URL/token, request sem token no corpo, envio de itens/saída observada sem runs brutas, merge da fila retornada e ações visuais `Central`/`Sync central`.
- `npm run typecheck`, `npm run test:builder-api`, `npm run build:builder-ui`, `npm run test:mvp-main-path` e `git diff --check` passaram após fechar a UI/docs do sync central do catálogo local; `git diff --check` emitiu apenas avisos LF/CRLF do workspace.
- `npm run test:baseline`, `npm run test:generated`, `npm run test:manifest`, `npm run test:parity`, `npm run test:codegen`, `npm run typecheck`, `npm run build:builder-ui` e `npm run test:ui-theme -- --grep "studio runs with data render"` passaram após a autenticação do SSE de eventos por query `api_key`, cobrindo bloqueio sem chave, aceite por header, aceite por query para `EventSource`, regeneração dos artefatos e propagação de `STUDIO_RUNTIME_API_KEY` no Studio em tema claro e escuro.
- `npm run test:baseline`, `npm run typecheck`, `npm run build:builder-ui` e `npm run test:ui-theme -- --grep "studio runs with data render"` passaram após o suporte visual ao WebSocket de turno, cobrindo `/turn/stream/ws` no runtime manual, correção do worker síncrono do endpoint, seletor SSE/WebSocket no Studio e manutenção do fluxo padrão SSE em tema claro e escuro.
- `npm run test:baseline`, `npm run test:generated`, `npm run test:manifest`, `npm run test:parity`, `npm run test:codegen`, `npm run typecheck`, `npm run build:builder-ui` e `npm run test:ui-theme -- --grep "studio runs with data render"` passaram após a telemetria configurável de jobs, cobrindo `GET /jobs/metrics?window_hours=...`, p95, throughput, próxima execução pendente, defaults `WORKER_INTERVAL_SECONDS`/`WORKER_LIMIT`/`WORKER_RETRY_DELAY_SECONDS` no compose/worker e seletor de janela no Studio em tema claro e escuro.
- `npm run test:baseline`, `npm run test:generated`, `npm run test:manifest`, `npm run test:parity`, `npm run test:codegen`, `npm run typecheck`, `npm run build:builder-ui` e `npm run test:ui-theme -- --grep "studio runs with data render"` passaram após o streaming SSE de turno passar a usar callback incremental do grafo/LLM, cobrindo `source: "llm_callback"` em tokens SSE/WebSocket, fallback por resposta final quando necessário, regeneração do runtime e bundle e indicador visual de origem dos chunks no Studio em tema claro e escuro.
- `npm run test:baseline`, `npm run test:generated`, `npm run test:manifest`, `npm run test:parity`, `npm run test:codegen`, `npm run typecheck`, `npm run build:builder-ui` e `npm run test:ui-theme -- --grep "studio runs with data render"` passaram após o schedule manual de jobs, cobrindo `POST /jobs/{job_id}/schedule` com `delay_seconds`, evento `post_finish_scheduled`, respeito a `next_run_at` pelo worker, paridade OpenAPI com 20 paths, regeneração do runtime/bundle e botões `Adiar 10 min`/`Adiar 1 h` no Studio em tema claro e escuro.
- `npm run validate:flow`, `npm run test:baseline`, `npm run test:generated`, `npm run test:manifest`, `npm run test:parity`, `npm run test:codegen`, `npm run test:builder-api`, `npm run typecheck`, `npm run build:builder-ui` e `npm run test:ui-theme -- --grep "studio runs with data render"` passaram após a recorrência simples de jobs, cobrindo `agent_job_schedules`, `POST /jobs/{job_id}/recurrence`, `GET /job-schedules`, `POST /job-schedules/run-due`, `POST /job-schedules/{schedule_id}/disable`, eventos `job_schedule_created`/`job_schedule_enqueued`/`job_schedule_disabled`, worker que enfileira agendas vencidas antes de processar jobs, paridade OpenAPI com 24 paths, regeneração do runtime/bundle e ações `Repetir 1 h`/`Enfileirar vencidas`/`Desativar` no Studio em tema claro e escuro.
- `python -m pytest -q examples/reference-interview-runtime -k "schedule or recurring or cron"`, `python -m pytest -q generated/reference-interview-runtime -k "schedule or recurring or cron"`, `python -m pytest -q generated/reference-runtime-bundle/agents/reference-interview -k "schedule or recurring or cron"`, `npm run test:baseline`, `npm run test:generated`, `npm run test:manifest`, `npm run test:parity`, `npm run test:codegen`, `npm run test:builder-api`, `npm run typecheck`, `npm run validate:flow`, `npm run build:builder-ui` e `npm run test:ui-theme -- --grep "studio runs with data render"` passaram após o cron básico de jobs, cobrindo `trigger_type="cron"`, `cron_expression`, validação de cron inválido, cálculo da próxima execução, payload de agenda no job enfileirado, paridade manual/gerado/bundle e ação visual `Cron 9h` no Studio em tema claro e escuro.
- `python -m pytest -q examples/reference-interview-runtime -k "schedule or recurring or cron or event"`, `python -m pytest -q generated/reference-interview-runtime -k "schedule or recurring or cron or event"`, `python -m pytest -q generated/reference-runtime-bundle/agents/reference-interview -k "schedule or recurring or cron or event"`, `npm run test:baseline`, `npm run test:generated`, `npm run test:manifest`, `npm run test:parity`, `npm run test:codegen`, `npm run typecheck`, `npm run build:builder-ui`, `npm run test:ui-theme -- --grep "studio runs with data render"` e `git diff --check` passaram após schedules por evento, cobrindo `trigger_type="event"`, `event_type`, `POST /job-schedules/trigger-event`, bloqueio de execução por `run-due`, redaction de payload externo com chaves sensíveis óbvias, evento `job_schedule_event_triggered`, paridade manual/gerado/bundle, OpenAPI com 26 paths e ações visuais `Evento fim`/`Disparar fim` em tema claro e escuro.
- `python -m pytest -q examples/reference-interview-runtime -k "cleanup"`, `python -m pytest -q generated/reference-interview-runtime -k "cleanup"`, `python -m pytest -q generated/reference-runtime-bundle/agents/reference-interview -k "cleanup"`, `npm run test:baseline`, `npm run test:generated`, `npm run test:manifest`, `npm run test:parity`, `npm run test:codegen`, `npm run typecheck`, `npm run build:builder-ui` e `npm run test:ui-theme -- --grep "studio runs with data render"` passaram após a limpeza governada de jobs, cobrindo `POST /jobs/cleanup`, `dry_run=true` por padrão, bloqueio de status não terminal, remoção explícita de jobs `succeeded`/`failed` antigos, evento `jobs_cleanup_completed`, paridade manual/gerado/bundle, OpenAPI com 27 paths e ações visuais `Prévia limpeza 7d`/`Limpar 7d` em tema claro e escuro.
- `npm run typecheck`, `npm run build:builder-ui`, `npm run test:ui-theme -- --grep "studio runs with data render"` e `git diff --check` passaram após o painel `Execução ao vivo`, cobrindo estado vazio, progresso por nó, spans observados, provider/modelo `openai`/`gpt-4.1-mini`, tokens/custo agregados e render em tema claro e escuro.
- `npm run typecheck`, `npm run test:builder-api`, `npm run build:builder-ui`, `npm run test:ui-theme -- --grep "studio runs with data render"` e `git diff --check` passaram após a telemetria histórica por provider/modelo com janela e alertas, cobrindo a rota backend `/studio-runs/provider-telemetry`, query params `windowHours`, `providerTokenBudget` e `providerCostBudgetUsd`, agregação de run/evento/tokens/custo, alertas por orçamento e renderização do painel no Studio em tema claro e escuro.
- `npm run test:codegen`, `npm run typecheck`, `npm run validate:flow`, `npm run codegen:reference`, `npm run codegen:sandbox`, `npm run codegen:manifest`, `npm run test:generated`, `npm run test:manifest`, `npm run test:parity`, `npm run build:builder-ui`, `npm run test:ui-theme -- --grep "studio runs with data render"` e `git diff --check` passaram após `sandboxIsolation: "dedicated_process"` para Python, cobrindo worker Python separado, workspace temporário, env allowlist, trace `dedicated_process`, `base_isolation=runtime_process`, saída não persistida em `app/code` e opção visual no inspector/perfis de sandbox.
- `npm run typecheck` e `npm run test:ui-theme -- --grep "studio runs with data render"` passaram após o painel `Falhas de sandbox` no contexto do nó, cobrindo erro `custom_code_failed` em `dedicated_process`, causa provável de entry point, boundary `process_workspace`, transporte `stdin_stdout_json`, executor Python, erro resumido e botão `Ver logs` aplicando filtro por status/sandbox/busca em tema claro e escuro.
- `npm run typecheck` e `npm run test:ui-theme -- --grep "studio runs with data render"` passaram após o painel agregado `Falhas de sandbox`, cobrindo agrupamento por `deterministic_gate`/`file`/`dedicated_process`, executor Python, transporte `stdin_stdout_json`, erro resumido e ação `Abrir nó` que seleciona o nó/evento em tema claro e escuro.
- `npm run typecheck`, `npx tsx --test --test-name-pattern "runtime job metrics history" apps/builder-api/src/server.test.ts`, `npm run build:builder-ui` e `npm run test:ui-theme -- --grep "studio runs with data render"` passaram após o histórico compartilhável/exportável/comparável/sincronizável de métricas de jobs pós-finalização, cobrindo botões `Salvar snapshot`/`Exportar histórico`/`Comparar histórico`/`Central`/`Sync central`, storage por flow e por workspace, formato `agent-flow-builder.runtime-job-metrics-history.v1`, diff `agent-flow-builder.runtime-job-metrics-history-diff.v1`, central sync sem token no corpo/status e render no Studio em tema claro e escuro.
- `npm run typecheck` e `npm run test:ui-theme -- --grep "agent overview summarizes"` passaram após o `Roteiro de criação` na aba `Visão`, cobrindo as etapas Entrada/IA/Ferramentas/Safety/Estado/Teste, CTA derivado do flow e atualização do bloqueio após validação com erro.
- `npm run typecheck` e `npm run test:ui-theme -- --grep "canvas batch editor updates advanced node families"` passaram após a governança inicial de presets de edição em lote, cobrindo origem biblioteca/flow, escopos, flow de origem, estado `nunca`, incremento de uso e contagem de nós no último uso após aplicar preset importado.
- `npm run test:codegen`, `npm run typecheck`, `npm run validate:flow`, `npm run build:builder-ui`, `npm run codegen:reference`, `npm run codegen:sandbox`, `npm run codegen:manifest`, `npm run test:ui-theme -- --grep "code sandbox policy library"`, `npm run test:generated`, `npm run test:manifest` e `npm run test:parity` passaram após a primeira camada de `sandboxIsolation: "container"` para Python/JavaScript/TypeScript, cobrindo Flow Spec, opção visual `Container dedicado`, perfis de sandbox com imagem/engine, runtime gerado com falha rastreável por imagem ausente para Python e JavaScript, runner Node em container e paridade dos artefatos regenerados.
- `npm run typecheck`, `npm run test:builder-api` e `npm run test:ui-theme -- --grep "studio runs with data render"` passaram após a telemetria histórica de sandbox, cobrindo a rota backend `/studio-runs/sandbox-telemetry`, query params `windowHours` e `onlyFailures`, agregação por nó/modo/isolamento/executor/transporte/imagem/engine/rede, falhas de container, renderização do painel `Histórico de sandbox` no Studio em tema claro e escuro e preservação do painel atual de falhas agregadas por nó.
- `npm run typecheck`, `npx tsx --test --test-name-pattern "studio run persistence" apps/builder-api/src/server.test.ts` e `npm run build:builder-ui` passaram após ampliar a telemetria histórica de sandbox para VM/microVM, cobrindo agregação por orquestração, hardening, `providesVmIsolation`, assurance `guest_vm`, política de rootfs read-only/workspace mount, contadores de VM, microVM, hardening e isolamento VM verificado, além de renderização/export no painel `Histórico de sandbox`.
- `npm run typecheck`, `npm run validate:flow` e `npm run test:ui-theme -- --grep "code sandbox policy library"` passaram após a primeira camada de imagens gerenciadas de container por executor, cobrindo `sandboxContainerImageId` no Flow Spec/Builder UI, presets Python/Node no inspector, aplicação atômica de isolamento/imagem/engine/linguagem, card de detalhe da imagem e preservação do preset em perfis `.afcodesandbox.json`.
- `npm run test:codegen`, `npm run typecheck` e `npm run build:builder-ui` passaram após a inclusão de Bash/Shell como linguagem nativa de nós `code`, cobrindo Flow Spec, opção visual no inspector e edição em lote, preset de container `bash:5.2`, Dockerfile final com `bash`, execução real por arquivo com JSON stdin/stdout, trace `shell_process`, `ephemeral_workspace` descartável e ausência de escrita persistida em `app/code`.
- `npm run test:builder-api`, `npm run typecheck`, `npm run build:builder-ui` e `npm run test:ui-theme -- --grep "builder shell is usable"` passaram após a primeira camada central de governança do workspace, cobrindo rotas `/workspace-governance`, `/workspace-governance/merge`, `/workspace-governance/conflicts/:conflictId/resolve`, persistência em `.agent-flow/governance/workspace.afgovernance.json`, conflitos de papel, auditoria, export governado e aba `Governança` em tema claro/escuro nos viewports desktop e compacto.
- `npx tsx --test apps/builder-api/src/workspace-governance.test.mjs`, `npm run typecheck`, `npm run build:builder-ui` e `npm run test:ui-theme -- --grep "builder shell is usable"` passaram após a checagem consultiva de autorização da governança do workspace, cobrindo `/workspace-governance/authorize`, formato `agent-flow-builder.workspace-governance-decision.v1`, decisão permitida, decisão `would_block`, papel mínimo por ação/política, enforcement opcional com `AGENT_FLOW_WORKSPACE_GOVERNANCE_ENFORCE=true` bloqueando `viewer` nas mutações de governança, catálogo e Docker/runtime delivery, e painel visual de checagem por ator/área/ação na aba `Governança`.
- `npx tsx --test apps/builder-api/src/workspace-governance.test.mjs`, `npm run test:builder-api`, `npm run typecheck`, `npm run build:builder-ui`, `npm run test:ui-theme -- --grep "builder shell is usable"` e `git diff --check` passaram após a primeira auth local opcional do Builder API, cobrindo `/builder-auth/status` público sem valores brutos, `/builder-auth/audit` sem valores brutos ou headers, bloqueio 401 sem chave, aceite por `X-Agent-Flow-Builder-Key`, injeção do ator autenticado na governança, bloqueio de spoof por `x-agent-flow-actor` quando a chave pertence a viewer, geração de chave por `/builder-auth/keys`, arquivo rotacionável sem valor bruto, autenticação com a chave gerada, desativação por `/builder-auth/keys/:keyId/disable`, envio de chave local pela Builder UI e painel de inventário/auditoria/rotação na aba `Governança`.
- `npm run typecheck` e `npx tsx --test --test-name-pattern "JWT bearer auth|optional local auth" apps/builder-api/src/workspace-governance.test.mjs` passaram após a primeira camada JWT local do Builder, cobrindo JWT `HS256` em `Authorization: Bearer`, validação de issuer/audience/expiração, mapeamento de claims para ator/papel/áreas/scopes, enforcement de área por claim, criação de sessão local a partir de JWT, auditoria com `source=jwt`/`source=session` e ausência de segredo, token JWT e token de sessão nos relatórios/JSONL.
- `npm run typecheck` e `npx tsx --test --test-name-pattern "JWT bearer auth|JWKS file|optional local auth" apps/builder-api/src/workspace-governance.test.mjs` passaram após JWKS local do Builder, cobrindo token `RS256` por `AGENT_FLOW_BUILDER_AUTH_JWT_JWKS_PATH`, seleção de chave por `kid`, status JWKS sem chave pública bruta, rejeição de `kid` desconhecido e enforcement de áreas/papéis derivados dos claims.
- `npm run typecheck` e `npx tsx --test --test-name-pattern "JWT bearer auth|JWKS file|OIDC issuer|optional local auth" apps/builder-api/src/workspace-governance.test.mjs` passaram após discovery OIDC inicial do Builder, cobrindo `AGENT_FLOW_BUILDER_AUTH_OIDC_ISSUER_URL`, leitura de `.well-known/openid-configuration`, resolução de `jwks_uri`, validação de issuer descoberto, status OIDC sem material bruto e rejeição de issuer divergente.
- `npm run typecheck`, `npx tsx --test apps/builder-api/src/workspace-governance.test.mjs`, `npm run build:builder-ui` e `npm run test:ui-theme -- --grep "builder governance auth policy"` passaram após sessão local, filtros/resumo/reload persistente da auditoria local do Builder, cobrindo criação de `/builder-auth/session`, autenticação por `Authorization: Bearer afbs_*` sem reenviar a chave bruta, presets `Workspace`, `7 dias`, `Sem expiração`, campo `Expiração da chave do Builder`, resumo `Política ampla`/`Política limitada`, filtros por status/rota/busca, `summary` agregado, reload de `AGENT_FLOW_BUILDER_AUTH_AUDIT_PATH` após nova instância da API, export governado `.afbuilderauthaudit.json` com formato `agent-flow-builder.builder-auth-audit.v1` e ausência de erros em navegador.
- `npm run typecheck`, `npm run validate:flow`, `npm run test:codegen` e `npm run test:ui-theme -- --grep "code sandbox policy library"` passaram após a primeira camada de orquestração avançada de container, cobrindo os campos canônicos `sandboxContainerProfile`, memória, CPU, PIDs, rootfs read-only, cap-drop e no-new-privileges no Flow Spec, Builder UI, perfis `.afcodesandbox.json` e codegen Python/Node.
- `npm run typecheck`, `npm run validate:flow`, `npm run test:codegen` 26/26 e `npx tsx --test --test-name-pattern "VM-isolated|VM runner" packages/codegen-langgraph/src/codegen.test.ts` passaram após a primeira camada de `sandboxIsolation: "vm"`, cobrindo Flow Spec, codegen Python, contrato `agent-flow-vm-runner.v1`, falha rastreável por runner ausente, execução Bash/Shell via runner VM, `custom.sandbox`/`execution_log` com `vm`/`microvm`, runner/imagem/perfil/política, executor `bash`/`sh` quando aplicável e preservação dos campos VM no contrato do nó.
- `npm run typecheck`, `npm run validate:flow`, `npm run test:codegen` e `npm run test:ui-theme -- --grep "code sandbox policy library"` passaram após a primeira camada de presets gerenciados de imagem VM, cobrindo `sandboxVmImageId` no Flow Spec/Builder UI/codegen, presets Python/Node no inspector, aplicação atômica de isolamento/runner/args/imagem/perfil/memória/vCPU, card de detalhe da imagem VM, preservação em perfis `.afcodesandbox.json` e trace do image id no runtime gerado.
- `npm run typecheck`, `npx tsx --test --test-name-pattern "VM runner readiness" apps/builder-api/src/server.test.ts` e `npm run test:ui-theme -- --grep "code sandbox policy library"` passaram após o checker local de runner VM, cobrindo a rota `/flows/:flowId/code-vm-runner/check`, resolução de `sandboxVmRunner`/`AGENT_FLOW_CODE_VM_RUNNER`, args, imagem relativa ao flow, formato `agent-flow-builder.vm-runner-check.v1`, flag `executesUserCode=false`, estado bloqueado para runner/imagem ausentes, painel visual `Verificação VM` no inspector e export governado `.afvmreadiness.json` com `agent-flow-builder.vm-runner-readiness.v1`, sem paths locais resolvidos e sem executar código do usuário.
- `npm run test:builder-api`, `npm run typecheck`, `npm run build:builder-ui`, `npm run validate:flow` e `npm run test:ui-theme -- --grep "studio runs with data render"` passaram após a primeira camada de conflitos/auditoria/policy local da fila de anotação e identidade local de revisor, cobrindo merge concorrente do mesmo item por revisores diferentes, item vencedor pelo `updatedAt` mais recente, persistência de `conflictCount`/`openConflictCount`/`conflicts`, `existingSnapshot`/`incomingSnapshot` completos, `auditEntries`/`auditCount`, `permissionPolicy` com modo e papéis locais, resolução inicial de conflito, export/import `.afannotations.json` e renderização do Studio em tema claro/escuro.
- `npm run typecheck`, `npm run test:builder-api`, `npm run build:builder-ui`, `npm run test:mvp-main-path`, `npm run test:ui-theme -- --grep "studio runs with data render"` e `git diff --check` passaram após a thread de curadoria dos conflitos da fila de anotação, cobrindo rota `POST /flows/:flowId/annotation-queue/conflicts/:conflictId/curation`, ações `assign`/`release`, auditoria `conflict_curated`, normalização de `curationThread`, histórico compacto `curationThread.events`, bloqueio de conflito já resolvido, aplicação automática do status `resolved` quando o conflito é finalizado e renderização do Studio em tema claro/escuro. O `git diff --check` emitiu apenas avisos LF/CRLF do workspace e o build da UI manteve apenas o aviso conhecido de chunk acima de 500 kB.
- `npm run typecheck`, `npx tsx --test --test-name-pattern "lists, validates, reads and generates" apps/builder-api/src/server.test.ts`, `npx tsx --test --test-name-pattern "Builder API syncs annotation queue" apps/builder-api/src/server.test.ts`, `npm run test:builder-api`, `npm run build:builder-ui`, `npm run test:mvp-main-path` e `npm run test:ui-theme -- --grep "studio runs with data render"` passaram após o histórico/tombstone de resolução da fila de anotação, cobrindo `resolutionHistory` no pacote `agent-flow-builder.annotation-queue.v1`, refs compactas com hash de saída observada em vez de output bruto, painel `Histórico de resolução` no Studio e supressão de reabertura quando a mesma revisão descartada retorna por merge. O build da UI manteve apenas o aviso conhecido de chunk acima de 500 kB.
- `npx tsx --test --test-name-pattern "annotation queue conflict review" apps/builder-api/src/server.test.ts`, `npm run typecheck`, `npm run build:builder-ui` e `npm run test:ui-theme -- --grep "studio runs with data render"` passaram após o export/diff governado de conflitos da fila de anotação, cobrindo `agent-flow-builder.annotation-queue-conflict-review.v1`, `agent-flow-builder.annotation-queue-conflict-review-diff.v1`, rotas `/flows/:flowId/annotation-queue/conflicts-review` e `/flows/:flowId/annotation-queue/conflicts-review/diff`, rejeição de pacote bruto no diff, refs/hashes para campos sensíveis, botões `Exportar revisão`/`Comparar revisão` em tema claro/escuro e ausência de itens completos, `existingSnapshot`/`incomingSnapshot`, vereditos, razões, notas e saídas observadas no pacote de revisão.
- `npm run test:builder-api` e `npm run typecheck` passaram após o enforcement backend da policy da fila de anotação, cobrindo `viewer` bloqueado, reviewer bloqueado fora do próprio item em `assignee_only`, reviewer autorizado no item atribuído, alteração de policy bloqueada para reviewer e liberada para owner quando `x-agent-flow-actor` está presente.
- `npm run typecheck`, `npm run build:builder-ui`, `npm run test:ui-theme -- --grep "studio runs with data render"` e `git diff --check` passaram após o histórico local/exportável da fila de anotação, cobrindo botões `Salvar snapshot`/`Exportar histórico`, storage por flow, formato `agent-flow-builder.annotation-queue-history.v1`, export `.afannotationhistory.json`, resumo por status/responsável/revisor/conflitos e ausência de payload bruto dos itens no snapshot.
- `npm run typecheck`, `npm run build:builder-ui` e `npm run test:ui-theme -- --grep "assets panel edits prompt and schema metadata visually"` passaram após a biblioteca local/exportável/importável de padrões de JSON Schema, cobrindo salvar padrão a partir do schema atual, storage `agent-flow-builder.schema-pattern-library.v1`, export `.afschemapatterns.json`, import de pacote `agent-flow-builder.schema-pattern-library.v1` e aplicação por merge não destrutivo de propriedades/`$defs`.
- `npm run typecheck`, `npm run build:builder-ui` e `npm run test:ui-theme -- --grep "assets panel edits prompt and schema metadata visually"` passaram após a curadoria local de padrões de JSON Schema, cobrindo status `draft`/`approved`, `reviewedBy`, `reviewedAt`, export `.afschemapatterns.json` preservando curadoria, aplicação de padrão importado, incremento de `usageCount` e `lastUsedAt`.
- `npm run typecheck` e `npm run test:ui-theme -- --grep "assets panel edits prompt and schema metadata visually"` passaram após o preview/diff local de padrões de JSON Schema, cobrindo contagem de adições, lista dos primeiros campos/`$defs` novos e ausência de conflitos antes de aplicar padrão built-in e padrão importado.
- `npm run typecheck` e `npm run test:ui-theme -- --grep "assets panel edits prompt and schema metadata visually"` passaram após o export governado de diff de padrão de JSON Schema, cobrindo download `.afschemapatterndiff.json`, formato `agent-flow-builder.schema-pattern-application-diff.v1`, hash do schema atual, metadados do padrão, preview de campos/`$defs`, flags de exclusão de conteúdo bruto/secrets e `packageHash`.
- `npm run typecheck`, `npm run build:builder-ui` e `npm run test:ui-theme -- --grep "assets panel edits prompt and schema metadata visually"` passaram após a importação/revisão visual de diff de padrão de JSON Schema, cobrindo input `.afschemapatterndiff.json`, normalização do pacote, status de compatibilidade com o hash do schema atual, contagem de adições/conflitos, hash do pacote, hash verificado, hash divergente com hash recalculado visível e fechamento do painel de revisão sem aplicar alteração no schema.
- `npm run typecheck`, `npm run build:builder-ui` e `npm run test:ui-theme -- --grep "assets panel edits prompt and schema metadata visually"` passaram após o histórico local/exportável/importável de padrões de JSON Schema, cobrindo botão `Salvar snapshot`, painel `Histórico`, formato `agent-flow-builder.schema-pattern-library-history.v1`, export `.afschemapatternhistory.json`, `snapshotHash`, `packageHash`, flags de exclusão de schema bruto/secrets e importação do pacote histórico.
- `npm run typecheck`, `npm run build:builder-ui` e `npm run test:ui-theme -- --grep "assets panel edits prompt and schema metadata visually"` passaram após a comparação governada entre snapshots de padrões de JSON Schema, cobrindo resumo de mudanças entre os dois snapshots mais recentes, export/import/revisão `.afschemapatternhistorydiff.json`, formato `agent-flow-builder.schema-pattern-library-history-diff.v1`, contagens de adicionados/removidos/alterados/status/uso/tags/schemaHash, `usageDelta`, `packageHash`, hash verificado, hash divergente com hash recalculado visível e ausência de schema bruto.
- `npm run test:builder-api`, `npm run typecheck` e `npm run test:ui-theme -- --grep "assets panel edits prompt and schema metadata visually"` passaram após a sincronização compartilhada de padrões de JSON Schema, cobrindo rotas `GET`/`PUT`/`POST merge` de `/flows/:flowId/schema-pattern-library` e `/flows/:flowId/schema-pattern-history`, persistência file-backed em `.agent-flow/schema-patterns/`, recomputação de `packageHash`, merge por ID/hash, ausência de schema bruto no histórico compartilhado, ações visuais `Carregar compartilhado`/`Sincronizar compartilhado`, resumo `sharedSync` com ação, hash, storage, contagens de merge e conflitos, além de detecção/visibilidade/resolução inicial de conflitos compartilhados por `POST /flows/:flowId/schema-pattern-library/conflicts/:conflictId/resolve`, com snapshots compactos sem schema bruto e `resolvedBy` vindo da identidade local de curador.
- `npm run typecheck` e `npx tsx --test --test-name-pattern "Builder API lists, validates, reads and generates the reference flow" apps/builder-api/src/server.test.ts` passaram após o sync central multiusuário opcional de padrões de JSON Schema, cobrindo `AGENT_FLOW_SCHEMA_PATTERN_CENTRAL_URL`, `AGENT_FLOW_SCHEMA_PATTERN_CENTRAL_TOKEN`, `GET /flows/:flowId/schema-pattern-library/central`, `POST /flows/:flowId/schema-pattern-library/sync-central`, status sem URL/token, token somente no header, request sem token no corpo, push da biblioteca/histórico local, merge da biblioteca/histórico retornados e ações visuais `Central`/`Sync central`.
- `npm run typecheck`, `npm run test:builder-api`, `npm run build:builder-ui`, `npm run test:mvp-main-path` e `git diff --check` passaram após fechar a UI/docs do sync central de padrões de JSON Schema; `git diff --check` emitiu apenas avisos LF/CRLF do workspace.
- `npm run typecheck`, `npm run build:builder-ui`, `npx tsx --test apps/builder-api/src/provider-telemetry-alerts.test.mjs`, `npm run test:ui-theme -- --grep "studio runs with data render"` e `git diff --check` passaram após a inbox local/exportável/compartilhada file-backed de alertas de telemetria por provider, cobrindo retenção selecionável, registro local em `agent-flow-builder.provider-telemetry-alerts.{flowId}`, formato `agent-flow-builder.provider-telemetry-alerts.v1`, rotas `GET`/`PUT`/`POST merge` em `/flows/:flowId/provider-telemetry-alerts`, persistência compartilhada em `.agent-flow/provider-telemetry-alerts/inbox.aftelemetryalerts.json`, resumo `sharedSync`, export `.aftelemetryalerts.json`, flags de exclusão de eventos brutos/secrets, reabertura por nova ocorrência, reconhecimento propagado para o arquivo compartilhado, `acknowledgedBy`/`acknowledgedRole` e bloqueio de `viewer` em tema claro e escuro.
- `npm run typecheck`, `npm run build:builder-ui`, `npx tsx --test apps/builder-api/src/approved-runtime-manifest.test.mjs apps/builder-api/src/provider-telemetry-alerts.test.mjs` e `npm run test:ui-theme -- --grep "runtime manifest editor saves visual changes"` passaram após o bundle aprovado por manifesto, cobrindo bloqueio sem aprovação, rota `/runtime-manifest/generate-approved`, aprovação agregada em `.agent-flow/langgraph-sandbox-approval.json`, aprovação por agente, auditoria de ZIP `ready=true`, target `runtime-manifest-bundle`, export `.agent-flow/export-manifest.json` e botão visual `Gerar bundle aprovado`.
- `npm run typecheck`, `npm run build:builder-ui` e `npm run test:ui-theme -- --grep "command palette navigates and creates nodes|canvas finder searches"` passaram após a ampliação das ações contextuais de canvas na paleta de comandos e a remoção por `Delete`/`Backspace`, cobrindo duplicação/remoção de nó selecionado pela paleta/teclado, abertura de prompt vinculado pela paleta, finder do canvas, barra contextual e ausência de overflow horizontal.
- `npm run typecheck`, `npm run build:builder-ui` e `npm run test:ui-theme -- --grep "command palette navigates and creates nodes"` passaram após a inserção conectada de etapas pelo canvas, cobrindo botão `Inserir etapa`, paleta filtrada em `Inserir`, ação `Inserir HTTP no fluxo`, criação do nó selecionado e conexão automática após `llm_step`.
- `npm run typecheck`, `npm run build:builder-ui`, `npm run test:ui-theme -- --grep "canvas finder searches"` e `npm run test:ui-theme -- --grep "command palette navigates and creates nodes"` passaram após a conexão sequencial da seleção, cobrindo botão `Conectar sequência`, criação de aresta faltante entre `deterministic_gate` e nó HTTP recém-criado, preservação do batch editor e renderização da paleta de comandos.
- `npm run typecheck`, `npm run build:builder-ui` e `npm run test:ui-theme -- --grep "command palette navigates"` passaram após os comandos contextuais de seleção de grafo por teclado, cobrindo `Selecionar downstream do nó`, seleção multi-nó incluindo o HTTP recém-inserido, feedback no status e resumo `5 nós selecionados` na barra contextual.
- `npm run typecheck`, `npm run test:builder-api`, `npm run build:builder-ui`, `npm run test:ui-theme -- --grep "runtime manifest editor saves visual changes"` e `git diff --check` passaram após o runbook JSON por agente e o atalho de histórico por agente no painel Docker/bundle, cobrindo botão `Runbook`, download `.afagentrunbook.json`, formato `agent-flow-builder.runtime-agent-runbook.v1`, URLs reais de metadata/sessões, evidência pendente de smoke, comandos manuais, flags sem `.env`/secrets, botão `Histórico`, filtro visual `operation=smoke`/`agentId` e busca backend em campos estruturados de `smoke`/`smokeAll`. O `git diff --check` emitiu apenas avisos LF/CRLF do workspace.
- `npm run typecheck`, `npm run build:builder-ui` e `npm run test:ui-theme -- --grep "safety policy library"` passaram após o export governado do histórico de Safety Harness, cobrindo botão `Exportar histórico`, download `.afsafetyhistory.json`, formato `agent-flow-builder.safety-harness-history.v1`, contadores de run/revisão, `inputPreview` e flags `includesRawInput=false`/`includesSecrets=false`.
- `npx tsx --test apps/builder-api/src/workspace-governance.test.mjs` passou após o enforcement backend de revisão do Safety Harness para atores autenticados locais, cobrindo bloqueio de `viewer`, autorização de `reviewer` na área `safety_harness`, decisão `agent-flow-builder.safety-harness-permission-decision.v1` sem input bruto/secrets e substituição do `reviewer` do payload pelo ator autenticado real.
- `npm run typecheck`, `npm run build:builder-ui` e `npm run test:ui-theme -- --grep "safety policy library"` passaram após alinhar a revisão visual do Safety Harness à identidade local de revisor, cobrindo exibição `revisor qa-safety`, gravação `Revisor: qa-safety` no run revisado e export `.afsafetyhistory.json` preservando o reviewer sem input bruto/secrets.
- `npx tsx --test apps/builder-api/src/workspace-governance.test.mjs`, `npm run typecheck` e `npm run test:builder-api` passaram após o enforcement backend central por papel/área da auth local do Builder nas rotas protegidas, cobrindo bloqueio de `viewer`, bloqueio de `reviewer` fora da área `schemas`, autorização de `reviewer` com área `schemas`, decisão `agent-flow-builder.builder-auth-area-decision.v1` sem payload bruto/secrets, preservação da decisão especializada do Safety Harness e persistência de biblioteca/merge de padrões.
- `npm run typecheck`, `npm run build:builder-ui` e `npm run test:ui-theme -- --grep "studio runs with data render"` passaram após o export governado da telemetria histórica de sandbox, cobrindo botão `Exportar telemetria`, download `.afsandboxtelemetry.json`, formato `agent-flow-builder.studio-sandbox-telemetry-export.v1`, relatório original `agent-flow-builder.studio-sandbox-telemetry.v1`, query aplicada e flags `includesRawEvents=false`/`includesRawState=false`/`includesSecrets=false`/`includesEnvValues=false`/`includesSandboxWorkspaceFiles=false`.
- `npm run typecheck`, `npm run build:builder-ui`, `npx tsx --test --test-name-pattern "Builder API lists, validates, reads and generates the reference flow" apps/builder-api/src/server.test.ts`, `npm run test:ui-theme -- --grep "studio runs with data render"` e `git diff --check` passaram após a inbox local/exportável/compartilhada file-backed de alertas de regressão por comparação de runs, cobrindo retenção, registro de alerta, storage local por flow em `agent-flow-builder.regression-alerts.{flowId}`, rotas `GET`/`PUT`/`POST merge` em `/flows/:flowId/regression-alerts`, persistência compartilhada em `.agent-flow/regression-alerts/inbox.afregressionalerts.json`, export `.afregressionalerts.json`, formato `agent-flow-builder.regression-alerts.v1`, thresholds aplicados, métricas resumidas, reabertura por nova ocorrência, reconhecimento de abertos propagado para o arquivo compartilhado, `acknowledgedBy`/`acknowledgedRole`, bloqueio de `viewer` e flags `excludesRawRuns=true`/`excludesRawNodePayloads=true`/`excludesSecretValues=true`.
- `npm run typecheck`, `npx tsx --test --test-name-pattern "regression alerts with the central service" apps/builder-api/src/server.test.ts`, `npm run test:builder-api`, `npm run build:builder-ui`, `npm run test:mvp-main-path`, `npm run test:ui-theme -- --grep "studio runs with data render"` e `git diff --check` passaram após o sync central opcional da inbox de alertas de regressão, cobrindo `AGENT_FLOW_REGRESSION_ALERTS_CENTRAL_URL`, `AGENT_FLOW_REGRESSION_ALERTS_CENTRAL_TOKEN`, `GET /flows/:flowId/regression-alerts/central`, `POST /flows/:flowId/regression-alerts/sync-central`, token somente no header, status sem URL/token, request sem token no corpo, merge da inbox retornada e flags sem runs brutas, payloads de nó ou secrets. O `git diff --check` emitiu apenas avisos LF/CRLF do workspace.
- `npm run typecheck`, `npx tsx --test --test-name-pattern "provider telemetry alerts with the central service" apps/builder-api/src/provider-telemetry-alerts.test.mjs`, `npm run test:builder-api`, `npm run build:builder-ui`, `npm run test:mvp-main-path`, `npm run test:ui-theme -- --grep "studio runs with data render"` e `git diff --check` passaram após o sync central opcional da inbox de alertas de telemetria por provider, cobrindo `AGENT_FLOW_PROVIDER_TELEMETRY_ALERTS_CENTRAL_URL`, `AGENT_FLOW_PROVIDER_TELEMETRY_ALERTS_CENTRAL_TOKEN`, `GET /flows/:flowId/provider-telemetry-alerts/central`, `POST /flows/:flowId/provider-telemetry-alerts/sync-central`, token somente no header, status sem URL/token, request sem token no corpo, merge da inbox retornada, ações visuais `Central`/`Sync central` e flags sem eventos brutos de run ou secrets. O `git diff --check` emitiu apenas avisos LF/CRLF do workspace.
- `npx tsx --test apps/builder-api/src/provider-telemetry-alerts.test.mjs` e `npm run typecheck` passaram após adicionar roteamento lógico local para alertas de provider por `AGENT_FLOW_PROVIDER_TELEMETRY_ALERT_ROUTES`, cobrindo regra por métrica/uso, aplicação de `route`, `routeLabel`, `routingRuleId`, `routingReason`, `routeBreakdown`, preservação de sync central e flags sem eventos brutos, URLs de webhook ou secrets.
- `npx tsx --test apps/builder-api/src/provider-telemetry-alerts.test.mjs`, `npm run typecheck` e `npm run build:builder-ui` passaram após adicionar dispatch externo governado por rota para alertas de provider, cobrindo `GET/POST /flows/:flowId/provider-telemetry-alerts/dispatch`, `AGENT_FLOW_PROVIDER_TELEMETRY_ALERT_ROUTE_SINKS`, token de sink via env enviado só no header, status/resultado sem URL/token, envio apenas de alertas abertos da rota configurada, skip explícito de rota sem sink, payload `agent-flow-builder.provider-telemetry-alert-route-dispatch.v1` sem eventos brutos ou secrets e ações visuais `Rotas`/`Enviar rotas`.
- `npm run typecheck`, `npm run build:builder-ui`, `npx tsx --test --test-name-pattern "Builder API lists, validates, reads and generates the reference flow" apps/builder-api/src/server.test.ts`, `npm run test:builder-api`, `npm run test:mvp-main-path`, `npm run test:ui-theme -- --grep "studio runs with data render"` e `git diff --check` passaram após adicionar o dashboard histórico dedicado de telemetria por provider, cobrindo `GET/POST /flows/:flowId/provider-telemetry-dashboard-history`, persistência `.agent-flow/provider-telemetry-dashboard-history/history.json`, formato `agent-flow-builder.provider-telemetry-dashboard-history.v1`, deduplicação por flow/hash/janela, tendência/deltas/maior custo/maior uso, export `.afprovidertelemetryhistory.json`, painel visual em tema claro/escuro e flags sem eventos brutos, state bruto, secrets, sink URLs ou tokens. O `git diff --check` emitiu apenas avisos LF/CRLF do workspace.
- `npm run typecheck` e `npx tsx --test apps/builder-api/src/provider-telemetry-alerts.test.mjs` passaram após adicionar a primeira política local de escalonamento de alertas de provider, cobrindo `AGENT_FLOW_PROVIDER_TELEMETRY_ALERT_ESCALATION_POLICY`, regras por rota/provider/modelo/métrica/uso/ocorrências/idade, aplicação de `escalationLevel`, `escalationRuleId`, `escalationReason`, `dispatchPriority`, `escalationBreakdown`, ordenação de payload por prioridade, status visual com regra de escalonamento, chip por alerta no Studio e flags sem eventos brutos, URLs de webhook, tokens ou secrets.
- `npm run typecheck` e `npx tsx --test apps/builder-api/src/provider-telemetry-alerts.test.mjs` passaram após adicionar a política local de entrega dos alertas de provider, cobrindo `AGENT_FLOW_PROVIDER_TELEMETRY_ALERT_DELIVERY_POLICY`, `maxAlertsPerRoute`, `minDispatchPriority`, `cooldownMinutes`, regras por rota, `availableOpenCount`, `skippedByPolicyCount`, `deliveryPolicy` no status/resultado/payload, persistência de `lastDispatchedAt`/`dispatchCount`, reenvio controlado por cooldown e status visual sem URL/token/secrets.
- `npm run typecheck`, `npm run build:builder-ui` e `npx tsx --test apps/builder-api/src/provider-telemetry-alerts.test.mjs` passaram após adicionar o relatório governado de prontidão da entrega de alertas de provider, cobrindo `GET /flows/:flowId/provider-telemetry-alerts/delivery-readiness`, formato `agent-flow-builder.provider-telemetry-alert-delivery-readiness.v1`, status `ready`/`partial`/`blocked`/`idle`, contadores por rota, sinks configurados sem URL/token, políticas de roteamento/escalonamento/entrega, export visual `.afproviderdelivery.json` e flags sem eventos brutos, URLs de webhook, tokens ou secrets.
- `npm run typecheck`, `npm run build:builder-ui`, `npm run test:ui-theme -- --grep "studio runs with data render"` e `git diff --check` passaram após o histórico local/exportável de governança de replay e o diff histórico de snapshots, cobrindo botão `Salvar snapshot`, habilitação de `Exportar histórico`, download `.afreplayhistory.json`, formato `agent-flow-builder.replay-governance-history.v1`, painel `Comparação histórica`, download `.afreplayhistorydiff.json`, formato `agent-flow-builder.replay-governance-history-diff.v1`, vínculo por origem/run, comparação de checkpoint/pins/restore/evaluator e flags `excludesRawScenarioPayload=true`/`excludesRawPinPayloads=true`/`excludesRawCheckpointState=true`/`excludesSecretValues=true`. O `git diff --check` emitiu apenas avisos LF/CRLF do workspace.
- `npm run typecheck`, `npm run test:builder-api`, `npm run build:builder-ui` e `npm run test:ui-theme -- --grep "studio runs with data render"` passaram após a curadoria governada de replay por revisor local, cobrindo painel `Curadoria governada do replay`, reviewer local, decisão no snapshot `.afreplayhistory.json`, flags sem payload bruto/secrets e `reviewStatus`/`reviewer` no diff `.afreplayhistorydiff.json` em tema claro e escuro.
- `npm run typecheck`, `npm run build:builder-ui`, `npx tsx --test --test-name-pattern "Builder API lists, validates, reads and generates the reference flow" apps/builder-api/src/server.test.ts`, `npm run test:ui-theme -- --grep "studio runs with data render"` e `git diff --check` passaram após a sincronização compartilhada file-backed do histórico de replay, cobrindo rotas `GET`/`PUT`/`POST merge` em `/flows/:flowId/replay-governance-history`, persistência em `.agent-flow/replay-governance/history.afreplayhistory.json`, deduplicação por `snapshotHash`, contagens `sharedSync`, recomputação de hash, carregamento/merge na UI em tema claro/escuro e remoção de payload bruto de cenário, pins e checkpoint.
- `npm run typecheck`, `npx tsx --test --test-name-pattern "Builder API syncs replay governance history with the central service without raw payloads" apps/builder-api/src/server.test.ts`, `npm run test:builder-api`, `npm run build:builder-ui`, `npm run test:mvp-main-path` e `git diff --check` passaram após o sync central opcional do histórico governado de replay, cobrindo `AGENT_FLOW_REPLAY_GOVERNANCE_HISTORY_CENTRAL_URL`, `AGENT_FLOW_REPLAY_GOVERNANCE_HISTORY_CENTRAL_TOKEN`, `GET /flows/:flowId/replay-governance-history/central`, `POST /flows/:flowId/replay-governance-history/sync-central`, token somente no header, status sem URL/token, request sem token no corpo, merge do histórico retornado e flags sem payload bruto de cenário, pins, checkpoint ou secrets. O `git diff --check` emitiu apenas avisos LF/CRLF do workspace.
- `npm run typecheck`, `npx tsx --test --test-name-pattern "Builder API lists, validates, reads and generates the reference flow" apps/builder-api/src/server.test.ts`, `npm run test:builder-api`, `npm run build:builder-ui`, `npm run test:mvp-main-path`, `npm run test:ui-theme -- --grep "studio runs with data render"` e `git diff --check` passaram após a detecção/resolução de conflitos do histórico compartilhado de replay, cobrindo `conflictCount`/`openConflictCount`, refs sanitizadas, rejeição de hash que não pertence ao conflito, rota `POST /flows/:flowId/replay-governance-history/conflicts/:conflictId/resolve`, ação `resolve_conflict`, painel visual `Conflitos de replay` e regra de conflito por mesma evidência governada com curadoria divergente. O `git diff --check` emitiu apenas avisos LF/CRLF do workspace.
- `npm run typecheck`, `npx tsx --test --test-name-pattern "replay governance" apps/builder-api/src/server.test.ts`, `npx tsx --test --test-name-pattern "lists, validates, reads and generates" apps/builder-api/src/server.test.ts`, `npm run test:builder-api`, `npm run build:builder-ui`, `npm run test:mvp-main-path`, `npm run test:ui-theme -- --grep "studio runs with data render"` e `git diff --check` passaram após a thread de curadoria dos conflitos de replay, cobrindo `POST /flows/:flowId/replay-governance-history/conflicts/:conflictId/curation`, ações `assign`/`release`, `sharedSync.action=curate_conflict`, preservação de `curationThread.events` ao recarregar o pacote, botões `Assumir`/`Liberar` no painel `Conflitos de replay`, render em tema claro/escuro e ausência de payload bruto de cenário, pins, checkpoint ou secrets. O build da UI manteve apenas o aviso conhecido de chunk acima de 500 kB e o `git diff --check` emitiu apenas avisos LF/CRLF do workspace.
- `npm run typecheck` e `npx tsx --test --test-name-pattern "replay governance" apps/builder-api/src/server.test.ts` passaram após adicionar lease/expiração à curadoria de conflitos do histórico de replay, cobrindo prazo padrão de 24h, env `AGENT_FLOW_REPLAY_GOVERNANCE_CONFLICT_CURATION_LEASE_HOURS`, governança `autoReleasesExpiredAssignments`, limpeza de lease ao liberar, normalização de pacote persistido antigo com lease vencido para `unassigned`, sinalização `leaseExpired` e evento compacto `lease_expired` sem payload bruto.
- `npm run typecheck`, `npx tsx --test --test-name-pattern "replay governance" apps/builder-api/src/server.test.ts`, `npm run test:builder-api`, `npm run build:builder-ui`, `npm run test:mvp-main-path`, `npm run test:ui-theme -- --grep "studio runs with data render"` e `git diff --check` passaram após o histórico governado de resolução/tombstone dos conflitos de replay, cobrindo `resolutionHistory` no pacote `agent-flow-builder.replay-governance-history.v1`, registro de revisor/nota/snapshot mantido/refs descartadas sem cenário/pins/checkpoint bruto, aplicação do histórico antes da detecção de conflitos para evitar reabertura quando a mesma ref descartada volta por sync e painel `Histórico de resolução` no roteiro do replay. O build da UI manteve apenas o aviso conhecido de chunk acima de 500 kB e o `git diff --check` emitiu apenas avisos LF/CRLF do workspace.
- `npm run typecheck`, `npx tsx --test --test-name-pattern "lists, validates, reads and generates" apps/builder-api/src/server.test.ts`, `npm run build:builder-ui`, `npm run test:ui-theme -- --grep "studio runs with data render"` e `git diff --check` passaram após a revisão governada exportável/comparável dos conflitos de replay, cobrindo `GET /flows/:flowId/replay-governance-history/conflicts-review`, `POST /flows/:flowId/replay-governance-history/conflicts-review/diff`, botões `Exportar revisão`/`Comparar revisão`, downloads `.afreplay-conflicts.json`/`.afreplay-conflicts-diff.json`, flags de governança e rejeição de payload bruto de replay.
- `npm run typecheck`, `npm run build:builder-ui`, `npm run test:ui-theme -- --grep "studio runs with data render"` e `git diff --check` passaram após os templates visuais de cenário no Studio, cobrindo cards `Templates de cenário`, aplicação de `Caminho feliz`, preenchimento de critério textual e preservação do fluxo de cenários em tema claro e escuro. O `git diff --check` emitiu apenas avisos LF/CRLF do workspace.
- `npm run typecheck`, `npx tsx --test --test-name-pattern "Safety Harness history with the central service" apps/builder-api/src/server.test.ts`, `npm run test:builder-api`, `npm run build:builder-ui`, `npm run test:mvp-main-path`, `npm run test:ui-theme -- --grep "safety policy library"` e `git diff --check` passaram após o sync central opcional do Safety Harness e a resolução governada de colisões do mesmo run, cobrindo `AGENT_FLOW_SAFETY_HARNESS_CENTRAL_URL`, `AGENT_FLOW_SAFETY_HARNESS_CENTRAL_TOKEN`, `GET /flows/:flowId/safety-harness/central`, `POST /flows/:flowId/safety-harness/sync-central`, token somente no header, status sem URL/token, request sem token no corpo, sanitização de `inputPreview`/`matchedText` no envio e no merge recebido, `resolutionHistory` compacta com refs descartadas sem input bruto/matched text e painel visual `Histórico de resolução`. O build da UI manteve apenas o aviso conhecido de chunk acima de 500 kB e o `git diff --check` emitiu apenas avisos LF/CRLF do workspace.
- `npm run typecheck`, `npx tsx --test --test-name-pattern "Safety Harness history with the central service" apps/builder-api/src/server.test.ts`, `npm run build:builder-ui`, `npm run test:ui-theme -- --grep "safety policy library"` e `git diff --check` passaram após o diff governado do histórico de Safety Harness, cobrindo `POST /flows/:flowId/safety-harness/diff`, botão visual `Comparar histórico`, download `.afsafetyhistory-diff.json`, preservação de `resolutionHistory` ao recarregar histórico e pacote de comparação sem `inputPreview`, `matchedText`, headers externos, payloads brutos de provider, token central ou secrets.
- `npm run typecheck`, `npx tsx --test --test-name-pattern "prepares local Ollama models" apps/builder-api/src/server.test.ts`, `npm run test:builder-api`, `npm run build:builder-ui`, `npm run test:ui-theme -- --grep "approval and Docker artifact|Docker operations"`, `npm run test:mvp-main-path` e `git diff --check` passaram após a ação visual e exportável de setup de modelos locais, cobrindo detecção de `ollama-pull-*` no `docker-compose.yml`, exposição de `modelSetup` no status, execução de `/docker-runtime/setup-models` como `docker compose --profile model-setup up ...`, filtro de histórico por `operation=setup_models`, bloqueio 409 quando o runtime não declara modelos locais, checklist visual `Modelos locais`, relatório de entrega com `docker.modelSetup`, runbook/export manifest com passo `model_setup`, verificador MVP usando adapter `ollama` e executando setup de modelos antes do build/smoke, estados claro/escuro do painel Docker e correção do teste de merge do catálogo para usar pacote remoto explicitamente mais novo. `git diff --check` emitiu apenas avisos LF/CRLF do workspace.
- `python -m pytest -q examples/reference-interview-runtime -k "ollama_missing_model"`, `python -m pytest -q generated/reference-interview-runtime -k "ollama_missing_model"`, `python -m pytest -q generated/reference-runtime-bundle/agents/reference-interview -k "ollama_missing_model"` e `npx tsx --test --test-name-pattern "local Ollama adapter" packages/codegen-langgraph/src/codegen.test.ts` passaram após o fallback prescritivo de modelo Ollama ausente, cobrindo baseline manual, runtime gerado, agente gerado em bundle e codegen do cliente LLM/trace de nó.
- `npm run typecheck`, `npm run test:baseline`, `npm run test:generated`, `npm run test:manifest`, `npm run test:parity`, `npm run test:codegen`, `npm run test:mvp-main-path` e `git diff --check` passaram após a estabilização final do fallback de Ollama ausente e do fixture Bash LF no codegen; `git diff --check` emitiu apenas avisos LF/CRLF do workspace.
- `npm run typecheck` e `npx tsx --test --test-name-pattern "local Ollama adapter|node-level local adapter|multiagent bundle includes Ollama|prepares local Ollama models" packages/codegen-langgraph/src/codegen.test.ts apps/builder-api/src/server.test.ts` passaram após os overrides locais de modelo, cobrindo `OLLAMA_IMAGE`, `OLLAMA_MODEL_NAMES`, envs de capacidade/concurrency, `docker-compose.gpu.yml`, `docker-compose.model-image.yml`, `ollama-models/Dockerfile` e passos opcionais `model_image`/`model_gpu` no runbook exportado.
- `npm run typecheck`, `npm run test:codegen`, `npm run test:builder-api`, `npm run test:mvp-main-path`, `npm run validate:flow`, `npm run test:baseline`, `npm run test:generated`, `npm run test:manifest`, `npm run test:parity`, `npm run build:builder-ui` e `git diff --check` passaram na revisão de status do MVP. O `test:codegen` passou 25/25 após corrigir o caso Bash para rodar o teste gerado dedicado de shell code em vez do smoke genérico que assume resposta `ECHO`; `git diff --check` emitiu apenas avisos LF/CRLF do workspace e o build da UI manteve apenas o aviso conhecido de chunk acima de 500 kB.
- `npm run typecheck`, `npx tsx --test --test-name-pattern "schema pattern structural merge leaves raw schema collisions as conflicts" apps/builder-api/src/schema-patterns.test.ts`, `npm run test:builder-api`, `npm run build:builder-ui`, `npm run test:mvp-main-path` e `git diff --check` passaram após o diff sanitizado detalhado de conflitos de padrões de schema, cobrindo campos/`$defs`/`required` novos, somente atuais e em colisão, composições, `additionalProperties`, export `.afschemapatternmergeplan.json` sem schema bruto e renderização em grade Atual/Recebido/Decisão. `git diff --check` emitiu apenas avisos LF/CRLF do workspace e o build da UI manteve apenas o aviso conhecido de chunk acima de 500 kB.
- `npm run typecheck`, `npm run build:builder-ui`, `npm run test:builder-api` e `npm run test:mvp-main-path` passaram após o assessment local/exportável de curadoria dos padrões de schema, cobrindo score `Pronto`/`Revisar`/`Bloqueado`, motivos de bloqueio/aviso/pontos fortes sem schema bruto, inclusão do assessment em `.afschemapatterns.json` e `.afschemapatternhistory.json`, recomputação ao importar/sincronizar pacotes antigos e uso da identidade local do curador ao aprovar/deprecar padrões.
- `npm run typecheck`, `npx tsx --test --test-name-pattern "schema pattern structural merge leaves raw schema collisions as conflicts" apps/builder-api/src/schema-patterns.test.ts`, `npm run test:builder-api`, `npm run build:builder-ui`, `npm run test:mvp-main-path` e `git diff --check` passaram após o diff textual local lado a lado de colisões reais de schema, cobrindo retorno `agent-flow-builder.schema-pattern-raw-text-diff.local.v1` no merge quando os dois schemas brutos estão disponíveis, renderização expansível Atual/Recebido, flags `localOnly`/`excludedFromSharedStorage`/`excludedFromExportsByDefault`, e prova de que o diff bruto não é persistido no arquivo compartilhado. O `git diff --check` emitiu apenas avisos LF/CRLF do workspace e o build da UI manteve apenas o aviso conhecido de chunk acima de 500 kB.
- `npm run typecheck`, `npm run test:builder-api`, `npm run build:builder-ui` e `npm run test:mvp-main-path` passaram após as revisões colaborativas estruturadas de padrões de schema, cobrindo decisões `approve`/`request_changes`/`deprecate`, resumo visual da última revisão, deduplicação/merge por ID no frontend/backend, inclusão em `.afschemapatterns.json` e `.afschemapatternhistory.json`, diff histórico sensível a mudanças de revisão e governança sem schema bruto/secrets. O build da UI manteve apenas o aviso conhecido de chunk acima de 500 kB.
- `npm run typecheck`, `npm run test:builder-api`, `npm run build:builder-ui`, `npm run test:mvp-main-path` e `git diff --check` passaram após a atribuição/thread de curadoria de padrões de schema, cobrindo ações visuais `Assumir`/`Liberar`, normalização frontend/backend de `curationThread`, histórico compacto `curationThread.events`, preservação de contagens e última decisão em snapshots compactos sem schema bruto, detecção de mudanças de curadoria em conflitos/histórico e aplicação de metadados de thread ao resolver conflitos. O `git diff --check` emitiu apenas avisos LF/CRLF do workspace e o build da UI manteve apenas o aviso conhecido de chunk acima de 500 kB.
- `npm run typecheck`, `npm run test:builder-api`, `npm run build:builder-ui`, `npm run test:mvp-main-path` e `git diff --check` passaram após a atribuição/thread de curadoria de conflitos do catálogo compartilhado, cobrindo rota `POST /catalog/shared-library/conflicts/:conflictId/curation`, ações visuais `Assumir`/`Liberar`, normalização frontend/backend de `curationThread`, histórico compacto `curationThread.events`, atualização `sharedSync.action=curate_conflict`, preservação de snapshots compactos sem conteúdo bruto e aplicação do status `resolved` ao resolver o conflito. A thread agora também tem lease padrão de 24h, configurável por `AGENT_FLOW_CATALOG_CONFLICT_CURATION_LEASE_HOURS`, com liberação automática de lease vencido e evento `lease_expired`. O `git diff --check` emitiu apenas avisos LF/CRLF do workspace e o build da UI manteve apenas o aviso conhecido de chunk acima de 500 kB.
- `npm run typecheck`, `npx tsx --test --test-name-pattern "prepares local Ollama models" apps/builder-api/src/server.test.ts`, `npx tsx --test --test-name-pattern "local Ollama adapter|multiagent bundle includes Ollama" packages/codegen-langgraph/src/codegen.test.ts` e `npm run build:builder-ui` passaram após o seletor visual CPU/GPU e o build gerenciado de imagem pré-carregada para modelos locais, cobrindo `modelSetup.execution`, `modelSetup.distribution`, detecção injetável de GPU local, recomendação `gpu`, comando `docker compose -f docker-compose.yml -f docker-compose.gpu.yml up -d --build`, operação `/docker-runtime/build-model-image`, histórico `operation=build_model_image`, comando `docker compose -f docker-compose.yml -f docker-compose.model-image.yml build ollama`, bloqueio 409 quando o artefato não possui override GPU/model-image e renderização/build do painel.
- `npm run typecheck`, `npm run build:builder-ui`, `npm run test:builder-api`, `npm run test:codegen`, `npm run test:mvp-main-path`, `npx tsx --test --test-name-pattern "prepares local Ollama models" apps/builder-api/src/server.test.ts`, `npx tsx --test --test-name-pattern "local Ollama adapter|node-level local adapter|multiagent bundle includes Ollama" packages/codegen-langgraph/src/codegen.test.ts` e `git diff --check` passaram após a exportação versionada da imagem Ollama pré-carregada, cobrindo `OLLAMA_MODEL_IMAGE`, tag/arquivo de distribuição em `modelSetup.distribution`, operação `/docker-runtime/export-model-image`, histórico `operation=export_model_image`, comando `docker image save -o model-distribution/...tar ...`, comando de `docker image load` no status/runbook e separação entre imagem base `OLLAMA_IMAGE` e imagem distribuível. `git diff --check` emitiu apenas avisos LF/CRLF do workspace.
- `npm run typecheck` e `npx tsx --test --test-name-pattern "prepares local Ollama models" apps/builder-api/src/server.test.ts` passaram após a publicação gerenciada da imagem Ollama em registry, cobrindo operação `/docker-runtime/push-model-image`, histórico `operation=push_model_image`, comando `docker image push ...`, bloqueio 409 para runtime sem tag publicável, botão visual `Publicar imagem` e passo `model_image_push` no runbook sem credenciais.
- `npm run typecheck`, `npm run build:builder-ui` e `npx tsx --test --test-name-pattern "prepares local Ollama models" apps/builder-api/src/server.test.ts` passaram após o catálogo local de imagens Ollama, cobrindo `/model-image-catalog`, `/model-image-catalog/register-runtime`, `/model-image-catalog/merge`, arquivo `.agent-flow/model-images/catalog.afmodelimages.json`, deduplicação por tag, metadados de modelos/comandos/origem, botão visual `Registrar catálogo` e governança sem credenciais Docker/env values.
- `npm run typecheck` e `npm run build:builder-ui` passaram após a superfície visual do catálogo de imagens, cobrindo tipos e renderização da lista local, estado de carregamento, export `.afmodelimages.json`, import/merge de pacote via `/model-image-catalog/merge`, destaque da imagem atual catalogada e mensagens de governança sem credenciais.
- `npm run typecheck` e `npx tsx --test --test-name-pattern "prepares local Ollama models" apps/builder-api/src/server.test.ts` passaram após a descoberta/sync file-backed de catálogos de imagens, cobrindo `/model-image-catalog/discovery`, `/model-image-catalog/sync-discovered`, pasta `.agent-flow/model-images/imports`, pacote `.afmodelimages.json` descoberto, merge por tag e governança sem credenciais/env values.
- `npm run typecheck`, `npm run build:builder-ui`, `npx tsx --test --test-name-pattern "prepares local Ollama models" apps/builder-api/src/server.test.ts`, `npm run test:ui-theme` e `git diff --check` passaram após o sync remoto de catálogos de imagens por URL, cobrindo `AGENT_FLOW_MODEL_IMAGE_CATALOG_REMOTE_URLS`, `/model-image-catalog/remote`, `/model-image-catalog/sync-remote`, redaction de URL, ausência de header `authorization`, merge por tag, botões `Remoto`/`Sync remoto`, estados claro/escuro e governança sem credenciais/env values. O `git diff --check` emitiu apenas avisos LF/CRLF do workspace.
- `npm run typecheck`, `npm run build:builder-ui`, `npx tsx --test --test-name-pattern "prepares local Ollama models" apps/builder-api/src/server.test.ts`, `npm run test:ui-theme` e `git diff --check` passaram após o registry remoto curado salvo no workspace, cobrindo `.agent-flow/model-images/remote-registries.afmodelregistry.json`, `GET/POST/DELETE /model-image-catalog/remote-registry`, rejeição de URL com usuário/senha, uso do registry salvo por `/model-image-catalog/remote`, atualização de `lastStatusCode`, `lastItemCount` e `lastSyncedAt` após `/model-image-catalog/sync-remote`, renderização do formulário visual em tema claro/escuro e ausência de whitespace errors. O `git diff --check` emitiu apenas avisos LF/CRLF do workspace.
- `npm run typecheck`, `npx tsx --test --test-name-pattern "prepares local Ollama models" apps/builder-api/src/server.test.ts`, `npm run build:builder-ui`, `npm run test:builder-api`, `npm run test:mvp-main-path` e `git diff --check` passaram após o sync central multiusuário do catálogo de imagens, cobrindo `AGENT_FLOW_MODEL_IMAGE_CATALOG_CENTRAL_URL`, `AGENT_FLOW_MODEL_IMAGE_CATALOG_CENTRAL_TOKEN`, `GET /model-image-catalog/central`, `POST /model-image-catalog/sync-central`, token apenas no header, status sem URL/token, request sem credenciais Docker/env values, push do catálogo local e merge do catálogo retornado. O `git diff --check` emitiu apenas avisos LF/CRLF do workspace.
- `npm run typecheck`, `npm run build:builder-ui`, `npx tsx --test --test-name-pattern "prepares local Ollama models" apps/builder-api/src/server.test.ts` e `npm run test:ui-theme` passaram após a detecção de compatibilidade Docker GPU, cobrindo campos `dockerGpuRuntimeAvailable`, `dockerGpuRuntimeDetails`, `dockerGpuRuntimeError`, recomendação `gpu` quando GPU local e runtime `nvidia` estão disponíveis, recomendação `cpu` quando há GPU local mas o Docker não confirma runtime NVIDIA, renderização `Docker GPU ok`/`Docker pendente` na UI e temas claro/escuro.
- `npm run typecheck`, `npx tsx --test --test-name-pattern "prepares local Ollama models" apps/builder-api/src/server.test.ts`, `npm run build:builder-ui`, `npm run test:ui-theme` e `git diff --check` passaram após o probe prático Docker GPU, cobrindo `POST /docker-runtime/check-gpu`, operação/histórico `check_gpu`, comando `docker run --rm --gpus all nvidia/cuda:12.4.1-base-ubuntu22.04 nvidia-smi -L`, parser de dispositivos `nvidia-smi -L`, bloqueio 409 quando o artefato não possui `docker-compose.gpu.yml`, botão visual `Testar GPU`, renderização do resultado com dispositivos/erro, passo `model_gpu_probe` no runbook/ZIP exportado e temas claro/escuro. O `git diff --check` emitiu apenas avisos LF/CRLF do workspace.
- `npm run typecheck`, `npm run test:builder-api`, `npm run test:mvp-main-path`, `npm run build:builder-ui`, `npm run test:ui-theme -- --grep "builder governance auth policy"` e `git diff --check` passaram após o sink HTTP central opcional de auditoria do Builder, cobrindo `AGENT_FLOW_BUILDER_AUTH_AUDIT_SINK_URL`, token de sink fora de status/export/payload, payload `agent-flow-builder.builder-auth-audit-sink-event.v1` sem chave bruta, token de sessão ou headers, status visual `sink central HTTP`, relatório local com `externalSinkConfigured` e falha não bloqueante.
- `npm run typecheck`, `npm run test:builder-api`, `npm run test:mvp-main-path`, `npm run build:builder-ui`, `npm run test:ui-theme -- --grep "builder governance auth policy"` e `git diff --check` passaram após o diretório corporativo HTTP opcional do Builder, cobrindo `AGENT_FLOW_BUILDER_AUTH_GROUP_DIRECTORY_URL`, token de diretório fora de status/auditoria, merge com diretório local, aplicação de política por grupo enriquecido e indicação visual de fonte externa.
- `npm run typecheck`, `npx tsx --test --test-name-pattern "optional local auth" apps/builder-api/src/workspace-governance.test.mjs`, `npm run test:builder-api`, `npm run test:mvp-main-path`, `npm run build:builder-ui`, `npm run test:ui-theme -- --grep "builder governance auth policy"` e `git diff --check` passaram após o sync HTTP opcional de sessão corporativa do Builder, cobrindo `AGENT_FLOW_BUILDER_AUTH_SESSION_SERVICE_URL`, eventos `created`/`refreshed`/`revoked`, token de serviço fora de status/auditoria/payload, hash de sessão sem token `afbs_*` bruto e indicação visual de serviço corporativo de sessão.
- `npm run typecheck` e `npx tsx --test --test-name-pattern "optional local auth" apps/builder-api/src/workspace-governance.test.mjs` passaram após a introspecção central obrigatória opcional de sessão do Builder, cobrindo `AGENT_FLOW_BUILDER_AUTH_SESSION_INTROSPECTION_URL`, modo fail-closed, decisão central `allowed:false`, override de identidade para `central-session`, token de introspecção fora de status/auditoria/payload e ausência do token `afbs_*` bruto na consulta central.
- `npm run typecheck`, `npx tsx --test --test-name-pattern "external Builder auth integrations" apps/builder-api/src/workspace-governance.test.mjs`, `npm run test:builder-api`, `npm run build:builder-ui`, `npm run test:mvp-main-path`, `npm run test:ui-theme -- --grep "builder governance auth policy"` e `git diff --check` passaram após o probe governado de integrações corporativas do Builder, cobrindo `POST /builder-auth/external-probe`, `AGENT_FLOW_BUILDER_AUTH_SESSION_SERVICE_URL`, `AGENT_FLOW_BUILDER_AUTH_SESSION_INTROSPECTION_URL`, `AGENT_FLOW_BUILDER_AUTH_AUDIT_SINK_URL`, `AGENT_FLOW_BUILDER_AUTH_GROUP_DIRECTORY_URL`, envio de tokens somente no header, `HEAD` sem corpo para serviços/sinks, `GET` sem corpo para diretório, contagens de atores/grupos, ação visual `Testar integrações` e relatório sem URL, porta, tokens ou headers. O `git diff --check` emitiu apenas avisos LF/CRLF do workspace.
- `npm run typecheck`, `npx tsx --test --test-name-pattern "Studio node pins" apps/builder-api/src/server.test.ts`, `npm run test:builder-api`, `npm run build:builder-ui`, `npm run test:mvp-main-path`, `npm run test:ui-theme -- --grep "studio runs with data render"` e `git diff --check` passaram após a primeira camada compartilhável file-backed, central opcional e thread/atribuição de curadoria de pins de nó, cobrindo `GET`/`PUT /flows/:flowId/studio-node-pins`, `POST /flows/:flowId/studio-node-pins/merge`, `POST /flows/:flowId/studio-node-pins/conflicts/:conflictId/curation`, `POST /flows/:flowId/studio-node-pins/conflicts/:conflictId/resolve`, `GET /flows/:flowId/studio-node-pins/central`, `POST /flows/:flowId/studio-node-pins/sync-central`, persistência `.agent-flow/studio-node-pins/pins.afnodepins.json`, formato `agent-flow-builder.studio-node-pins.v1`, request central `agent-flow-builder.studio-node-pins-central-sync-request.v1`, detecção de conflitos por nó, preservação de múltiplos candidatos divergentes, ações `assign`/`release`, histórico compacto `curationThread.events`, resolução escolhendo o pin mantido no Studio, histórico governado de resolução com revisor/nota/ref mantida/refs descartadas, prevenção de reabertura quando uma ref descartada idêntica volta por sync, token somente no header, status sem URL/token, redaction de chaves sensíveis em input/output compartilhado e ações visuais `Carregar pins`/`Sincronizar pins`/`Central`/`Sync central`/`Conflitos de pins`/`Assumir`/`Liberar`/`Histórico de resolução`. O `git diff --check` emitiu apenas avisos LF/CRLF do workspace.
- `npm run typecheck`, `npx tsx --test --test-name-pattern "debug layer snapshots" apps/builder-api/src/server.test.ts`, `npm run test:builder-api`, `npm run build:builder-ui`, `npm run test:mvp-main-path`, `npm run test:ui-theme -- --grep "studio runs with data render"` e `git diff --check` passaram após a primeira camada compartilhável file-backed e central opcional dos snapshots de camadas de debug, cobrindo `GET`/`PUT /flows/:flowId/debug-layer-snapshots`, `POST /flows/:flowId/debug-layer-snapshots/merge`, `POST /flows/:flowId/debug-layer-snapshots/conflicts/:conflictId/resolve`, `GET /flows/:flowId/debug-layer-snapshots/central`, `POST /flows/:flowId/debug-layer-snapshots/sync-central`, persistência `.agent-flow/debug-layers/snapshots.afdebuglayers.json`, formato `agent-flow-builder.debug-layer-snapshots.v1`, request central `agent-flow-builder.debug-layer-snapshots-central-sync-request.v1`, merge por `packageHash`, detecção de conflito por contexto de comparação/lote, painel visual `Conflitos de camadas`, resolução escolhendo snapshot específico no Studio, token somente no header, status sem URL/token, sanitização recursiva de evidências e ações visuais `Compartilhar resumo`/`Central`/`Sync central`. O `git diff --check` emitiu apenas avisos LF/CRLF do workspace.
- `npm run typecheck`, `npx tsx --test --test-name-pattern "debug layer snapshots" apps/builder-api/src/server.test.ts`, `npm run test:builder-api`, `npm run build:builder-ui`, `npm run test:mvp-main-path`, `npm run test:ui-theme -- --grep "studio runs with data render"` e `git diff --check` passaram após a thread de curadoria dos conflitos de camadas de debug, cobrindo `POST /flows/:flowId/debug-layer-snapshots/conflicts/:conflictId/curation`, ações `assign`/`release`, `sharedSync.action=curate_conflict`, preservação de `curationThread.events` ao recarregar o pacote, botões `Assumir`/`Liberar` no painel `Conflitos de camadas` e renderização em tema claro/escuro sem payload bruto de nó ou secrets. O `git diff --check` emitiu apenas avisos LF/CRLF do workspace e o build da UI manteve apenas o aviso conhecido de chunk acima de 500 kB.
- `npm run typecheck` e `npx tsx --test --test-name-pattern "debug layer snapshots" apps/builder-api/src/server.test.ts` passaram após adicionar lease/expiração à curadoria de conflitos de camadas de debug, cobrindo prazo padrão de 24h, env `AGENT_FLOW_DEBUG_LAYER_SNAPSHOT_CONFLICT_CURATION_LEASE_HOURS`, governança `autoReleasesExpiredAssignments`, preservação de `resolutionHistory` ao curar conflito, limpeza de lease ao liberar, normalização de pacote persistido antigo com lease vencido para `unassigned`, sinalização `leaseExpired` e evento compacto `lease_expired` sem payload bruto.
- `npm run typecheck`, `npm run test:builder-api`, `npm run build:builder-ui`, `npm run test:mvp-main-path`, `npm run test:ui-theme -- --grep "studio runs with data render"` e `git diff --check` passaram após o histórico governado de resolução/tombstone dos conflitos de camadas de debug, cobrindo `resolutionHistory` no pacote `agent-flow-builder.debug-layer-snapshots.v1`, registro de revisor/nota/snapshot mantido/refs descartadas sem payload bruto, aplicação do histórico antes da detecção de conflitos para evitar reabertura quando a mesma ref descartada volta por sync e painel `Histórico de resolução` no Studio. O `git diff --check` emitiu apenas avisos LF/CRLF do workspace e o build da UI manteve apenas o aviso conhecido de chunk acima de 500 kB.
- `npm run typecheck`, `npm run validate:flow`, `npx tsx --test --test-name-pattern "VM runner" apps/builder-api/src/server.test.ts`, `npx tsx --test --test-name-pattern "VM-isolated|VM runner" packages/codegen-langgraph/src/codegen.test.ts`, `npm run test:vm-image-manifest`, `npm run test:vm-image-bundle`, `npm run test:vm-image-scaffold`, `npm run test:vm-microvm-image-scaffold`, `npm run test:vm-image-real-smoke`, `npm run test:vm-microvm-real-smoke`, `npm run test:vm-reference-runner`, `npm run test:vm-qemu-runner`, `npm run test:vm-microvm-runner`, `npm run test:vm-guest-agent`, `npm run build:builder-ui` e `git diff --check` passaram após a camada de manifestos VM, scaffold de imagem, smoke real opt-in e runner-kit, cobrindo `sandboxVmRunnerManifest`, `sandboxVmImageManifest`, `sandboxVmEngine`, leitura de manifestos JSON pequenos sem executar código do usuário, validação de protocolo `agent-flow-vm-runner.v1`, engine, imageId, tamanho declarado, SHA-256 correto, bloqueio por SHA-256 divergente, capabilities hardened, scaffold QEMU com cloud-init/guest agent/scripts build-boot/transportador SSH sem baixar imagem nem iniciar VM, scaffold direct-kernel Firecracker/Cloud Hypervisor com preparo rootfs/kernel ou firmware/seed e preflights sem baixar imagem nem iniciar VM, readiness dry-run do smoke real, gate microVM real em dry-run com readiness para Firecracker/Cloud Hypervisor, fail-closed do runtime quando runner VM não prova `providesVmIsolation=true`, empacotamento `.afvmimagebundle` com manifestos sanitizados, imagem e artefatos de boot copiados/verificados, ausência de paths locais de origem, variáveis portáveis para `AGENT_FLOW_CODE_VM_BOOT_ARTIFACTS` e `AGENT_FLOW_CODE_VM_SEED_IMAGE`, `runner-kit` portátil, execução de `runner-kit/check-bundle.mjs`, runner de referência Python inline/file com bloqueio de `sourcePath` externo e declaração `providesVmIsolation=false`, runner QEMU de preflight com plano Q35/microVM e `seed.iso`, runner Firecracker/Cloud Hypervisor de preflight com rootfs, kernel/firmware, seed, plano Firecracker por config/API socket e plano Cloud Hypervisor por kernel ou firmware/disk/API socket, falha fechada sem transporte, contrato de transporte externo explícito com `providesVmIsolation=false` em modo simulado, e guest agent Python embutível na imagem com contrato inline/file, bloqueio de `sourcePath` externo e falha fechada para protocolo/linguagem não suportados. Neste host, o dry-run apontou ausência de QEMU/cloud-localds no Windows, mas o smoke real passou com `AGENT_FLOW_VM_REAL_SMOKE=1`, `AGENT_FLOW_VM_REAL_SMOKE_BACKEND=docker` e `AGENT_FLOW_VM_DOWNLOAD_BASE_IMAGE=1`, baixando/cacheando cloud image Debian, construindo a imagem QEMU, bootando VM real em container Docker, aguardando SSH e executando o guest agent com `providesVmIsolation=true`. O `git diff --check` emitiu apenas avisos LF/CRLF do workspace.
- `npm run test:vm-image-bundle` passou após preservar `policyManifest` em `.afvmimagebundle`, cobrindo cópia/verificação SHA-256 de `manifests/microvm.policy.json`, manifesto de imagem sanitizado com `policyManifest: "microvm.policy.json"`, `bundle.image.policyManifest`, governança `includesPolicyManifest`, scripts portáveis com `AGENT_FLOW_MICROVM_POLICY_MANIFEST`, `runner-kit/check-bundle.mjs` validando formato/tamanho/hash da política e preflight Firecracker lendo a política empacotada com `readOnlyRootfs=true`, rede `none`, sem workspace mount, sem host device passthrough e assurance exigida `guest_vm`, sem executar código do usuário.
- `npm run typecheck`, `npm run validate:flow`, `npm run test:mvp-main-path`, `npm run test:builder-api`, `npm run test:codegen`, `npm run test:baseline`, `npm run test:generated`, `npm run test:manifest`, `npm run test:parity`, `npm run test:multiagent-postgres`, `npm run test:docker-runtime-smoke`, `npm run test:ui-theme`, `npm run build:builder-ui`, `npm run test:vm-image-manifest`, `npm run test:vm-image-bundle`, `npm run test:vm-image-scaffold`, `npm run test:vm-microvm-image-scaffold`, `npm run test:vm-image-real-smoke`, `npm run test:vm-microvm-real-smoke`, `npm run test:vm-reference-runner`, `npm run test:vm-qemu-runner`, `npm run test:vm-microvm-runner`, `npm run test:vm-guest-agent`, `python -m py_compile tools/agent-flow-vm-runner-microvm.py` e `git diff --check` passaram nesta revisão final. O gate visual cobriu tema claro/escuro em desktop/compacto, corrigiu espera de carregamento do flow antes de atalhos, evitou crash do inspector quando a resposta de VM readiness vem sem manifestos e estabilizou a verificação do catálogo usando o chip visível do canvas em vez de depender do enquadramento interno do React Flow. O smoke Docker subiu o runtime final em `http://127.0.0.1:8080`, validou health DB/cache, metadata, sessão, turn, transcript, events, finish e job pós-finalização `succeeded`.
- `npm run test:vm-microvm-homologation` passou após adicionar o contrato `.afvmhomologation.json`, cobrindo promoção de imagem microVM real fornecida pelo operador em três estados: `blocked` sem preflight/boot evidence, `preflight_verified` com evidência `agent-flow-vm-runner-microvm-preflight.v1` e `homologated` com evidência `agent-flow-builder.vm-microvm-real-smoke-gate.v1` em `mode: real-boot`. O manifesto valida runner, imagem, SHA-256, artefatos de boot, política hardened, rede `none`, rootfs read-only, bloqueio de workspace mount/host device passthrough, assurance `guest_vm`, não inclui paths locais resolvidos e declara `executesUserCodeDuringManifestBuild=false`.
- `npm run test:vm-microvm-official-recipe` passou após adicionar `vm-image:microvm-recipe`, cobrindo a receita oficial local para imagem microVM kernel-direct: geração de scaffold, scripts de build/preflight/homologação/bundle/publicação local, templates de evidência, checklist, preparo com artefatos falsos sem download/boot, preflight real do runner, homologação com evidência sintética, pacote `.afvmimagebundle`, `runner-kit/check-bundle.mjs` e `release/microvm-image-release.json`, sem paths locais resolvidos ou secrets nos manifestos finais.
- `npm run test:vm-microvm-release-registration`, `npm run test:vm-image-bundle`, `npm run test:vm-image-manifest` e o recorte `npx tsx --test --test-name-pattern "VM runner readiness" apps/builder-api/src/server.test.ts` passaram após adicionar `vm-image:microvm-register`, cobrindo o registro consumível de release microVM em `.afvmrelease.json`, `microvm-runtime-config.json` e scripts de ambiente, validação de bundle/homologação/check-bundle, readiness `ready` pelo checker VM sem executar código do usuário, e correção da semântica hardened para exigir `supportsWorkspaceMount=false` como condição positiva.
- `npm run test:builder-auth-corporate`, `npm run typecheck`, `npm run test:builder-api` e `npm run build:builder-ui` passaram após adicionar a homologação corporativa local do Builder, cobrindo `POST /builder-auth/corporate-homologation`, artefato `.agent-flow/builder-auth/corporate-homologation.afbuilderauthhomologation.json`, status `homologated` quando session service, introspecção central obrigatória, audit sink, diretório externo e políticas de grupo estão ok, e ausência de URL, token, header, chave bruta, token de sessão, token de provedor ou path absoluto no retorno e no arquivo salvo.
- `python -m pytest -q examples/reference-interview-runtime/tests/test_reference_runtime.py -k "worker or job_batch or schedule or cron or lease"`, `python -m pytest -q generated/reference-interview-runtime/tests/test_generated_runtime.py -k "worker or job_batch or schedule or cron or lease"` e `npm run test:docker-runtime-smoke` passaram após adicionar claim/lease multiworker nos jobs, cobrindo bloqueio de duplicate claim, `locked_by`/`locked_until`, métrica de lease ativo/expirado, retomada por outro worker após expiração, compatibilidade com services antigos sem kwargs, limpeza de lock ao concluir e smoke real do serviço `worker` no Compose final.
- `python -m pytest -q examples/reference-interview-runtime/tests/test_reference_runtime.py -k "session_flow_idempotency_transcript_and_events or turn_stream"`, `python -m pytest -q generated/reference-interview-runtime/tests/test_generated_runtime.py -k "metadata_flow_and_idempotency or streaming_turn"` e o mesmo recorte em `generated/reference-runtime-bundle/agents/reference-interview` passaram após adicionar spans nativos `span_started`/`span_completed`, cobrindo persistência em `/events`, `payload.span.operation=graph_node`, `node_type`, `duration_ms`, `source=runtime_native_span` e correlação `source_message_id` em spans de turno.
- `npm run typecheck`, `npx tsx --test --test-name-pattern "lists, validates, reads and generates" apps/builder-api/src/server.test.ts` e `npx tsx --test apps/builder-api/src/schema-patterns.test.ts` passaram após a resolução guiada de conflitos da biblioteca compartilhada de padrões de schema, cobrindo estratégia explícita, nota de resolução, plano sanitizado sem schema bruto, aplicação de metadados quando o schema hash selecionado já corresponde ao item atual, ação `resolve_conflict` na auditoria compartilhada, prevenção de reabertura quando a mesma divergência compacta já resolvida retorna por sync, merge estrutural automático de schemas brutos sem colisão, permanência de conflito aberto quando há colisão real de propriedade, `schemaMergePlan` sanitizado sem schema bruto e resolução `apply_manual_schema_merge` aplicando o schema visual como merge manual auditado.
- `npm run typecheck`, `npx tsx --test apps/builder-api/src/schema-patterns.test.ts`, `npm run test:builder-api`, `npm run build:builder-ui`, `npm run test:mvp-main-path`, `npm run test:ui-theme -- --grep "studio runs with data render"` e `git diff --check` passaram após o histórico compacto da thread de curadoria de padrões de schema, cobrindo normalização/merge de `curationThread.events`, derivação de eventos a partir de `curationReviews`, truncamento de notas, descarte de ações inválidas, exibição dos três eventos recentes no Studio em tema claro/escuro e preservação sem schema bruto. O `git diff --check` emitiu apenas avisos LF/CRLF do workspace e o build da UI manteve apenas o aviso conhecido de chunk acima de 500 kB.
- `npm run typecheck`, `npx tsx --test apps/builder-api/src/schema-patterns.test.ts`, `npm run build:builder-ui` e `npm run test:ui-theme -- --grep "studio runs with data render"` passaram após adicionar lease/expiração à thread de curadoria de padrões de schema, cobrindo prazo padrão de 24h, env `AGENT_FLOW_SCHEMA_PATTERN_CURATION_LEASE_HOURS`, governança `autoReleasesExpiredAssignments`, liberação automática para `unassigned`, sinalização `leaseExpired`, evento compacto `lease_expired` e renderização do vencimento/estado expirado no Studio.
- `npm run typecheck`, `npx tsx --test apps/builder-api/src/schema-patterns.test.ts`, `npx tsx --test --test-name-pattern "lists, validates, reads and generates" apps/builder-api/src/server.test.ts`, `npm run build:builder-ui` e `npm run test:ui-theme -- --grep "studio runs with data render"` passaram após adicionar revisão/diff governados de conflitos compartilhados de padrões de schema, cobrindo formato `agent-flow-builder.schema-pattern-conflict-review.v1`, diff `agent-flow-builder.schema-pattern-conflict-review-diff.v1`, comparação de resumo/conflitos/decisões, rejeição de revisão importada com schema bruto, ausência de `items`/`rawSchemaTextDiff`/payload bruto/secrets e botões `Exportar revisão`/`Comparar revisão` no Studio.
- `npm run typecheck`, `npx tsx --test --test-name-pattern "saves and applies local catalog" apps/builder-api/src/server.test.ts`, `npm run test:builder-api`, `npm run build:builder-ui`, `npm run test:mvp-main-path`, `npm run test:ui-theme -- --grep "catalog panel saves local assets and applies a tool"` e `git diff --check` passaram após a resolução guiada de conflitos do catálogo compartilhado e o histórico governado explícito da biblioteca, cobrindo `keep_library`/`use_incoming`/`restore_existing_snapshot`, plano sanitizado sem conteúdo bruto, aceite do snapshot recebido quando ele já está materializado na biblioteca atual, `resolutionHistory` compacta com snapshot mantido/descartado sem conteúdo bruto, prevenção de reabertura quando a mesma divergência compacta já resolvida retorna por sync, separação visual entre conflito aberto e histórico resolvido e bloqueio preservado para Viewer na UI. O `git diff --check` emitiu apenas avisos LF/CRLF do workspace.
- `npm run typecheck` e `npx tsx --test --test-name-pattern "local catalog" apps/builder-api/src/server.test.ts` passaram após adicionar lease/expiração à curadoria de conflitos do catálogo compartilhado, cobrindo prazo padrão de 24h, env `AGENT_FLOW_CATALOG_CONFLICT_CURATION_LEASE_HOURS`, governança `autoReleasesExpiredAssignments`, liberação automática para `unassigned`, sinalização `leaseExpired` e evento compacto `lease_expired` ao recarregar pacote persistido antigo.
- `npm run typecheck`, `npm run test:ui-theme -- --grep "catalog panel saves local assets and applies a tool"`, `npm run build:builder-ui` e `git diff --check` passaram após a edição guiada de schemas internos do catálogo, cobrindo o painel `Schema guiado`, métricas por schema interno, ações `Campo`, `$defs`, `oneOf` e `Bloquear extras`, persistência do schema curado no item local e validação semântica antes de salvar. O build manteve apenas o aviso conhecido de chunk acima de 500 kB e o `git diff --check` emitiu apenas avisos LF/CRLF do workspace.
- `npm run typecheck`, `npm run test:ui-theme -- --grep "runtime manifest editor saves visual changes"`, `npm run build:builder-ui` e `git diff --check` passaram após a composição assistida multiagente no Runtime Manifest, cobrindo o painel `Composição assistida`, checklist visual de prontidão, recomendações de flows/agentes, templates iniciais `triage-agent`/`support-agent`, ação `Adicionar recomendados` e preservação da edição visual/aprovação do manifesto. O build manteve apenas o aviso conhecido de chunk acima de 500 kB e o `git diff --check` emitiu apenas avisos LF/CRLF do workspace.
- `npm run typecheck`, `npx tsx --test --test-name-pattern "multiagent manifest bundle" packages/codegen-langgraph/src/codegen.test.ts`, `npx tsx --test --test-name-pattern "runtime manifest|schema" apps/builder-api/src/server.test.ts`, `npm run test:ui-theme -- --grep "runtime manifest editor saves visual changes"`, `npm run codegen:manifest`, `npm run test:manifest`, `npm run build:builder-ui` e `git diff --check` passaram após a orquestração declarativa multiagente, cobrindo `runtime.manifest.json#orchestration`, validação de `entryAgentId`/handoffs contra agentes existentes, painel visual `Orquestração`, ação `Sugerir handoffs`, edição de modo/entrada/handoff, `.runtime-manifest/orchestration.json`, `bundle.json#orchestration`, metadata raiz do bundle e teste Python gerado para `/metadata.orchestration`. O build manteve apenas o aviso conhecido de chunk acima de 500 kB e o `git diff --check` emitiu apenas avisos LF/CRLF do workspace.
- `npm run typecheck`, `npx tsx --test --test-name-pattern "multiagent manifest bundle" packages/codegen-langgraph/src/codegen.test.ts`, `npm run codegen:manifest`, `python -m pytest -q generated/reference-runtime-bundle/tests/test_multiagent_bundle.py` e `git diff --check` passaram após a primeira rota executável de orquestração multiagente no bundle raiz, cobrindo `POST /orchestration/run`, seleção de entrada/handoffs a partir de `orchestration`, criação de sessões nos agentes montados, execução `start`/`turn` por child app em memória, chaves de idempotência por etapa/agente/operação, metadata de orquestração por sessão e retorno governado `agent-flow-builder.runtime-orchestration-run.v1`.
- `npm run typecheck`, `npx tsx --test --test-name-pattern "multiagent manifest bundle" packages/codegen-langgraph/src/codegen.test.ts`, `npm run codegen:manifest`, `python -m pytest -q generated/reference-runtime-bundle/tests/test_multiagent_bundle.py` e `git diff --check` passaram após adicionar roteamento condicional simples e memória compartilhada governada à orquestração do bundle, cobrindo condições explícitas `input contains:`/`output contains:`, execução dinâmica de handoffs após a saída intermediária, `shared_memory` no retorno `agent-flow-builder.runtime-orchestration-run.v1`, previews compactos de saída, decisões de roteamento e metadata de memória compartilhada entregue aos próximos agentes sem secrets/env values.
- `npm run typecheck`, `npx tsx --test --test-name-pattern "multiagent manifest bundle" packages/codegen-langgraph/src/codegen.test.ts`, `npm run codegen:manifest`, `python -m pytest -q generated/reference-runtime-bundle/tests/test_multiagent_bundle.py` e `git diff --check` passaram após adicionar roteamento por caminho estruturado e persistência governada da memória de orquestração, cobrindo condições como `output.assistant_message.code == ECHO`, resolução de `input.*`/`output.*`/`memory.*`, comparação `==`/`!=`/`contains`, `memory_path`, `persist_memory`, `ORCHESTRATION_MEMORY_PATH`, arquivo JSONL `agent-flow-builder.runtime-orchestration-memory-record.v1` sem path local no payload de resposta e retorno `shared_memory.persistence` sem secrets/env values.
- `npm run typecheck`, `npx tsx --test --test-name-pattern "multiagent manifest bundle" packages/codegen-langgraph/src/codegen.test.ts`, `npm run codegen:manifest`, `python -m pytest -q generated/reference-runtime-bundle/tests/test_multiagent_bundle.py` e `git diff --check` passaram após adicionar a primeira camada de debug step-by-step da orquestração executável, cobrindo `debug_trace` no formato `agent-flow-builder.runtime-orchestration-debug-trace.v1`, capabilities `debugTrace`/`structuredConditions` em `bundle.json` e `.runtime-manifest/orchestration.json`, eventos `plan_created`, `step_started`, `step_completed`, `handoff_decision`, `handoff_enqueued`, falha sanitizada, resumo por quantidade de etapas/memória/handoffs e persistência desse trace no JSONL sem secrets/env values.
- `npm run typecheck`, `npm run build:builder-ui` e `git diff --check` passaram após adicionar o painel visual `Debug orquestração` ao Docker/bundle, cobrindo chamada direta ao runtime final em `POST /orchestration/run`, uso de `STUDIO_RUNTIME_API_KEY`/`AGENT_API_KEY` quando configurado, input visual de mensagem, status de execução, erro de chamada, erro sanitizado retornado pelo runtime e timeline compacta de `debug_trace` em tema responsivo.
- `npm run typecheck`, `npm run build:builder-ui`, `npm run test:ui-theme -- --grep "runtime manifest editor saves visual changes"` e `git diff --check` passaram após evoluir o painel `Debug orquestração` com histórico local por artefato, carregamento do último trace ao reabrir o artefato, filtros por tipo/status/agente, comparação com a execução anterior por status/etapas/eventos/handoffs e export `.aforchdebug.json` com governança local/preview-only sem secrets/env values.
- `npm run typecheck` e `npx tsx --test --test-name-pattern "orchestration debug history" apps/builder-api/src/server.test.ts` passaram após a primeira camada compartilhável file-backed do histórico de debug de orquestração, cobrindo `GET`/`PUT /flows/:flowId/orchestration-debug-history`, `POST /flows/:flowId/orchestration-debug-history/merge`, `GET /flows/:flowId/orchestration-debug-history/central`, `POST /flows/:flowId/orchestration-debug-history/sync-central`, persistência `.agent-flow/orchestration-debug/history.aforchdebug.json`, formato `agent-flow-builder.orchestration-debug-history.v1`, request central `agent-flow-builder.orchestration-debug-history-central-sync-request.v1`, merge por `debug_trace.run_id`, sanitização de `runtimeUrl`, redaction recursiva de chaves sensíveis em `debug_trace`, `shared_memory` e timeline, token central somente no header, status sem URL/token e ações visuais `Carregar compartilhado`/`Sincronizar compartilhado`/`Central`/`Sync central` no painel `Debug orquestração`.
- `npm run typecheck`, `npx tsx --test --test-name-pattern "orchestration debug history" apps/builder-api/src/server.test.ts`, `npm run build:builder-ui` e `npm run test:ui-theme -- --grep "runtime manifest editor saves visual changes"` passaram após a primeira camada de diff governado entre execuções de orquestração, cobrindo `POST /flows/:flowId/orchestration-debug-history/diff`, formato `agent-flow-builder.orchestration-debug-history-diff.v1`, comparação por `debug_trace.run_id`, refs esquerda/direita, deltas de status/etapas/eventos/handoffs, seções agregadas de tipos de evento, agentes, handoffs e erros, export visual `.aforchdiff.json`, ausência de payload bruto de runtime e redaction de chaves sensíveis antes de montar o pacote.
- `npm run typecheck`, `npx tsx --test --test-name-pattern "generated multiagent manifest bundle" packages/codegen-langgraph/src/codegen.test.ts`, `npm run codegen:manifest`, `python -m pytest -q generated/reference-runtime-bundle/tests/test_multiagent_bundle.py`, `npm run build:builder-ui` e `npm run test:ui-theme -- --grep "runtime manifest editor saves visual changes"` passaram após adicionar `orchestration.memoryPolicy` ao Runtime Manifest, cobrindo schema/metadata/bundle, edição visual da política de memória compartilhada, persistência `disabled`/`optional_jsonl`/`always_jsonl`, persistência default por manifesto, caminho padrão, limite de entradas, retenção de JSONL, tamanho de preview, redaction de chaves configuradas, inclusão controlada de saídas/decisões e política resumida em `shared_memory`/`debug_trace` sem expor valores de secrets.
- `npm run typecheck`, `npx tsx --test --test-name-pattern "Studio scenarios" apps/builder-api/src/server.test.ts`, `npm run build:builder-ui`, `npm run test:ui-theme -- --grep "studio runs with data render"` e `git diff --check` passaram após a primeira camada compartilhável de cenários/datasets/evaluators do Studio, cobrindo rotas `GET`/`PUT`/`merge` por flow em `/flows/:flowId/studio-scenarios`, persistência file-backed em `.agent-flow/studio-scenarios/scenarios.afscenarios.json`, formato `agent-flow-builder.studio-scenarios.v1`, campos `evaluatorCount`/`evaluators` no pacote, resumo multiagente por agente/run/sessão/origem/cobertura de datasets, redaction recursiva de chaves sensíveis, sync central opcional por `AGENT_FLOW_STUDIO_SCENARIOS_CENTRAL_URL` com token somente no header, status sem URL/token e contagens separadas de cenários/datasets/evaluators enviados e recebidos, detecção de conflitos em cenário/dataset/evaluator por hash de conteúdo, preservação de candidatos, diff compacto sanitizado por campo divergente sem input/payload bruto ou secret, rotas `POST /flows/:flowId/studio-scenarios/conflicts/:conflictId/curation` e `POST /flows/:flowId/studio-scenarios/conflicts/:conflictId/resolve`, thread local `Assumir`/`Liberar`, curador visual com identidade local e papel `owner`/`reviewer`/`viewer`, bloqueio visual/handler para `viewer` não assumir nem resolver conflito, histórico governado de resolução com refs mantida/descartadas e painel visual `Conflitos de cenários compartilhados`, além das ações `Carregar compartilhados`/`Salvar compartilhado`/`Sincronizar compartilhados`/`Central`/`Sync central` junto da biblioteca de datasets. O `git diff --check` emitiu apenas avisos LF/CRLF do workspace.
- `npm run typecheck`, `npx tsx --test apps/builder-api/src/studio-scenarios-conflict-review.test.mjs`, `npm run build:builder-ui` e `npm run test:ui-theme -- --grep "studio runs with data render"` passaram após adicionar `GET /flows/:flowId/studio-scenarios/conflicts-review` e o botão visual `Exportar revisão`, gerando `.afscenario-conflicts.json` no formato `agent-flow-builder.studio-scenarios-conflict-review.v1` para revisão governada de conflitos compartilhados, com refs, diff compacto, thread de curadoria, histórico de resolução e hashes de candidatos, mas sem `candidates`, input bruto de cenários, payload bruto ou valores de secrets. O teste visual validou o download em tema claro e escuro.
- `npx tsx --test apps/builder-api/src/studio-scenarios-conflict-review.test.mjs`, `npm run typecheck`, `npm run build:builder-ui` e `npm run test:ui-theme -- --grep "studio runs with data render"` passaram após adicionar comparação governada de revisões de conflitos compartilhados de cenários/datasets/evaluators: `POST /flows/:flowId/studio-scenarios/conflicts-review/diff` recebe uma revisão `.afscenario-conflicts.json`, rejeita pacotes com `candidates`/payload bruto, compara estado atual contra revisão importada e gera `.afscenario-conflicts-diff.json` por botão visual `Comparar revisão`, com deltas de resumo, conflitos, thread de curadoria e histórico de resolução sem candidatos completos, input bruto, payload bruto ou secrets. O build manteve apenas o aviso conhecido de chunk acima de 500 kB.

- `npm run typecheck`, `npx tsx --test --test-name-pattern "Studio scenarios" apps/builder-api/src/server.test.ts`, `npm run build:builder-ui`, `npm run test:ui-theme -- --grep "studio runs with data render"` e `git diff --check` passaram após adicionar enforcement backend aos conflitos compartilhados de cenários/datasets/evaluators: `viewer` recebe 403 ao tentar curar/resolver, `reviewer` fica registrado nos eventos de curadoria e `resolvedRole` entra no histórico governado. O `git diff --check` emitiu apenas avisos LF/CRLF do workspace.
- `npm run typecheck` e `npx tsx --test --test-name-pattern "saves and applies local catalog" apps/builder-api/src/server.test.ts` passaram após adicionar enforcement backend aos conflitos da biblioteca compartilhada do catálogo: `viewer` recebe 403 em curadoria/resolução, `role` é registrado nos eventos compactos e `resolvedRole` entra no histórico governado de resolução.
- `npx tsx --test --test-name-pattern "catalog" apps/builder-api/src/server.test.ts`, `npm run typecheck`, `npm run build:builder-ui` e `npm run test:ui-theme -- --grep "catalog panel saves local assets"` passaram após adicionar revisão governada e comparação de conflitos da biblioteca compartilhada do catálogo: `GET /catalog/shared-library/conflicts-review` gera `.afcatalog-conflicts.json`, `POST /catalog/shared-library/conflicts-review/diff` gera `.afcatalog-conflicts-diff.json`, e a UI expõe `Exportar revisão`/`Comparar revisão`, validando que os pacotes não incluem itens completos, conteúdo bruto, `nodePatch`, input/output ou secrets. O build manteve apenas o aviso conhecido de chunk acima de 500 kB.
- `npm run typecheck`, `npx tsx --test --test-name-pattern "Studio node pins" apps/builder-api/src/server.test.ts`, `npm run build:builder-ui`, `npm run test:ui-theme -- --grep "studio runs with data render"` e `git diff --check` passaram após adicionar enforcement backend aos conflitos compartilhados de pins de nó: `viewer` recebe 403 ao tentar assumir/resolver, `role` fica registrado em `curationThread.events` e `resolvedRole` entra em `resolutionHistory`. O `git diff --check` emitiu apenas avisos LF/CRLF do workspace.
- `npm run typecheck`, `npx tsx --test --test-name-pattern "Studio node pins" apps/builder-api/src/server.test.ts`, `npm run build:builder-ui` e `npm run test:ui-theme -- --grep "studio runs with data render"` passaram após adicionar revisão governada e comparação de conflitos compartilhados de pins de nó: `GET /flows/:flowId/studio-node-pins/conflicts-review` gera `.afnodepin-conflicts.json`, `POST /flows/:flowId/studio-node-pins/conflicts-review/diff` gera `.afnodepin-conflicts-diff.json`, e a UI expõe `Exportar revisão`/`Comparar revisão`, validando em tema claro e escuro que os pacotes não incluem candidatos completos, `pins`, input/output bruto ou secrets. O build manteve apenas o aviso conhecido de chunk acima de 500 kB.
- `npx tsx --test --test-name-pattern "experiment dashboard history with central" apps/builder-api/src/server.test.ts`, `npm run typecheck`, `npm run build:builder-ui` e `npm run test:ui-theme -- --grep "studio runs with data render"` passaram após adicionar histórico experimental compartilhável/centralizável: `POST /flows/:flowId/experiment-dashboard-history/merge`, `GET /flows/:flowId/experiment-dashboard-history/central`, `POST /flows/:flowId/experiment-dashboard-history/sync-central` e `POST /flows/:flowId/experiment-dashboard-history/diff`, com token central somente no header, status sem URL/token, merge de snapshots, export/comparação visual `.afexperiment-dashboard-history.json`/`.afexperiment-dashboard-history-diff.json` e diff governado sem `dashboard` bruto, datasets brutos, runs brutas, saídas observadas ou secrets. O build manteve apenas o aviso conhecido de chunk acima de 500 kB.
- `npx tsx --test --test-name-pattern "studio run persistence" apps/builder-api/src/server.test.ts`, `npm run typecheck`, `npm run build:builder-ui` e `npm run test:ui-theme -- --grep "studio runs with data render"` passaram após adicionar merge, sync central e diff governado ao histórico de dashboard de telemetria por provider: `POST /flows/:flowId/provider-telemetry-dashboard-history/merge`, `GET /flows/:flowId/provider-telemetry-dashboard-history/central`, `POST /flows/:flowId/provider-telemetry-dashboard-history/sync-central` e `POST /flows/:flowId/provider-telemetry-dashboard-history/diff`, com token central somente no header, status sem URL/token, export/comparação visual `.afprovidertelemetryhistory.json`/`.afprovidertelemetryhistory-diff.json` e diff sem payload de telemetria bruto, eventos brutos, state bruto, sink URLs/tokens ou secrets. O build manteve apenas o aviso conhecido de chunk acima de 500 kB.
- `npm run typecheck`, `npx tsx --test --test-name-pattern "debug layer snapshots" apps/builder-api/src/server.test.ts`, `npm run build:builder-ui`, `npm run test:ui-theme -- --grep "studio runs with data render"` e `git diff --check` passaram após adicionar enforcement backend aos conflitos compartilhados de camadas de debug: `viewer` recebe 403 ao tentar assumir/resolver, `role` fica registrado em `curationThread.events` e `resolvedRole` entra em `resolutionHistory`.
- `npm run typecheck`, `npx tsx --test --test-name-pattern "debug layer snapshots" apps/builder-api/src/server.test.ts`, `npm run build:builder-ui` e `npm run test:ui-theme -- --grep "studio runs with data render"` passaram após adicionar revisão governada e comparação de conflitos compartilhados de camadas de debug: `GET /flows/:flowId/debug-layer-snapshots/conflicts-review` gera `.afdebuglayer-conflicts.json`, `POST /flows/:flowId/debug-layer-snapshots/conflicts-review/diff` gera `.afdebuglayer-conflicts-diff.json`, e a UI expõe `Exportar revisão governada de conflitos de camadas`/`Comparar revisão governada de conflitos de camadas`, validando em tema claro e escuro que os pacotes não incluem snapshots completos, evidências, payload bruto, input/output ou secrets. O build manteve apenas o aviso conhecido de chunk acima de 500 kB.
- `npm run typecheck`, `npx tsx --test --test-name-pattern "lists, validates, reads and generates" apps/builder-api/src/server.test.ts`, `npx tsx --test --test-name-pattern "syncs replay governance history" apps/builder-api/src/server.test.ts`, `npm run build:builder-ui`, `npm run test:ui-theme -- --grep "studio runs with data render"` e `git diff --check` passaram após adicionar enforcement backend aos conflitos compartilhados do histórico de replay: `viewer` recebe 403 ao tentar assumir/resolver, `role` fica registrado em `curationThread.events` e `resolvedRole` entra em `resolutionHistory`.
- `npm run typecheck`, `npm run build:builder-ui`, `npm run test:ui-theme -- --grep "studio runs with data render"` e `git diff --check` passaram após ligar o painel `Jobs pós-finalização` do Studio Local ao `/metadata.operations.jobs`, cobrindo o estado inicial `não verificado` e, após atualização, o resumo visual de worker, retry/claim multiworker, retenção e schedules em tema claro e escuro. O build manteve apenas o aviso conhecido de chunk acima de 500 kB e o `git diff --check` emitiu apenas avisos LF/CRLF do workspace.
- `npm run typecheck`, `npm run build:builder-ui`, `npm run test:ui-theme -- --grep "runtime manifest editor saves visual changes"` e `git diff --check` passaram após propagar retry/claim multiworker de `operations.jobs` para os cards operacionais Docker/bundle e para o runbook `.afagentrunbook.json`, mantendo `jobsOperations` bruto para automação e adicionando `jobsOperationsLabels` para leitura humana sem valores de `.env`.
- `npm run typecheck`, `npm run build:builder-ui`, `npm run test:ui-theme -- --grep "studio runs with data render"` e `git diff --check` passaram após unificar o componente visual de política operacional de jobs e reutilizá-lo no Studio Local, no resultado imediato do smoke Docker individual e no smoke multiagente.
- `npx tsx --test --test-name-pattern "runtime manifest|multiagent manifest bundle" apps/builder-api/src/server.test.ts` passou após reforçar que o smoke Docker individual, o smoke multiagente e o histórico de smoke preservam `agentMetadata.operations.jobs`, incluindo worker, retry delay, claim multiworker, retenção e schedules por agente.
- `npm run typecheck`, `npm run build:builder-ui`, `npm run test:ui-theme -- --grep "approval and Docker artifact render"`, `npm run test:ui-theme -- --grep "runtime manifest editor saves visual changes"`, `npm run test:mvp-main-path`, `npm run test:builder-auth-corporate`, `npm run test:docker-runtime-smoke` e `git diff --check` passaram após adicionar `agentOperations` ao relatório JSON de entrega para runtime monoagente/bundle e o resumo sanitizado `orchestration` para bundles, consolidando status, rota, última evidência de smoke, `jobsOperations` bruto, labels humanas de worker/retry/retenção/schedules e última execução de orquestração sem input bruto, memória compartilhada bruta, payloads de etapa ou timeline bruta. O Docker smoke real validou `generated/reference-interview-runtime` em `http://127.0.0.1:8080`, com health OK, Postgres/cache OK, sessão, turn, transcript, events, finish e job `succeeded`. O build manteve apenas o aviso conhecido de chunk acima de 500 kB e o `git diff --check` emitiu apenas avisos LF/CRLF do workspace.

## Ainda não implementado

- Ergonomia refinada do canvas já possui finder, grupos, organização automática inicial, seleção por teclado de upstream/downstream/vizinhos diretos do nó, alinhamento/distribuição de seleção, aplicação/limpeza de tags em lote por seleção, edição em lote de prompt/schema/stage/timeout, primeira camada específica para `code`/`http_request`, lote visual para Transform/Banco/Arquivo/RAG/Approval/Scoring/Analytics, presets locais por flow, biblioteca local inicial de presets reutilizáveis entre flows com governança visual de origem/escopos/uso, exportação/importação versionada `.afbatchpresets.json`, busca por tags, resumo de debug guiado por nó, salto para debug no Studio, primeira camada de caminho multi-camadas no debug do nó, comparação contextual do nó entre runs, resumo multi-camadas de comparação e de lote com export `.afdebuglayers.json`, compartilhamento file-backed/central opcional desses resumos em `.agent-flow/debug-layers/snapshots.afdebuglayers.json` com detecção/resolução inicial de conflitos, thread/atribuição local de curadoria de conflitos de camadas, exportação/comparação governada `.afdebuglayer-conflicts.json`/`.afdebuglayer-conflicts-diff.json`, histórico governado de resolução e prevenção de reabertura quando ref descartada idêntica volta por sync, artefato governado de replay por nó/candidato com export/import `.afdebugreplay.json`, painel de artefatos de replay com foco no nó e carregamento de run base/candidato, criação de cenário de debug por nó a partir do run candidato, primeira camada de execução guiada no cenário com próxima ação, preparação de fixture, restore, pins, execução/observação, avaliação do resultado, comparação governada de checkpoint/pins/restore/evaluator, export `.afreplaygovernance.json`, histórico local/exportável `.afreplayhistory.json`, sincronização file-backed em `.agent-flow/replay-governance/`, sync central opcional do histórico de replay com token só no header, diff histórico `.afreplayhistorydiff.json` sem payload bruto, primeira camada compartilhável file-backed e central opcional de pins de nó em `.agent-flow/studio-node-pins/pins.afnodepins.json`, painel dedicado de conflitos de pins com thread/atribuição local, lease/expiração, eventos compactos de curadoria e histórico governado de resolução, dirty/stale, barra contextual, ações contextuais na paleta de comandos, remoção por `Delete`/`Backspace`, overview de completude, roteiro de criação do agente e drill-down de canais/subagentes; ainda precisa evoluir para colaboração distribuída mais profunda entre múltiplos workspaces/usuários e políticas de resolução mais granulares além dessa curadoria local com lease. O editor visual de JSON Schema já cobre a primeira camada de `$defs`, `oneOf`, `allOf`, `anyOf`, `$ref`, `additionalProperties`, validação semântica local, navegação por diagnóstico, correção guiada ampliada, padrões reutilizáveis iniciais, biblioteca local/exportável/importável `.afschemapatterns.json`, preview/diff local de padrões, export/import/revisão `.afschemapatterndiff.json`, histórico `.afschemapatternhistory.json`, export/import/revisão de diff histórico `.afschemapatternhistorydiff.json`, curadoria local por status/uso com assessment exportável de prontidão/risco, revisões colaborativas estruturadas e atribuição/thread de curadoria por responsável local com lease/expiração e eventos compactos, sincronização compartilhada file-backed por flow, auditoria compacta de sync, detecção/visibilidade/resolução guiada de conflitos compartilhados com estratégia `accept_current_library`/`accept_existing_snapshot`/`accept_incoming_snapshot`/`apply_manual_schema_merge`, merge estrutural automático de schemas brutos sem colisão, plano assistido sanitizado para colisões reais sem schema bruto, revisão visual por colunas, diff sanitizado por área com itens novos/somente atuais/colisões, diff textual local lado a lado quando a resposta de merge tem os dois schemas brutos, aplicação manual auditada pelo schema visual, aplicação de metadados quando o schema hash selecionado já está presente, prevenção de reabertura da mesma divergência compacta resolvida quando ela volta por sync, enforcement backend por ator autenticado local para mutações de padrões de schema e governança exportável `.afschemagovernance.json`, e o Studio já gera forms de payload top-level, grupos aninhados de objetos tipados e arrays guiados por schema; ainda precisa evoluir para revisão distribuída avançada entre autores/workspaces.
- O catálogo local já possui a primeira camada de tools/skills compostas por bundle de nós/arestas, permite salvar subgrafos selecionados como blocos reutilizáveis, mostra resumo visual de blocos/templates, permite curadoria visual estrutural de itens locais com prompts/schemas internos editáveis, painel guiado para schemas internos, exporta/importa pacotes reutilizáveis, gera governança local/exportável por item e por biblioteca e já possui compartilhamento file-backed inicial entre workspaces por `.afcataloglibrary.json`, sync central opcional por `AGENT_FLOW_CATALOG_CENTRAL_URL`, thread/atribuição visual de curadoria de conflitos com lease/expiração e histórico compacto de eventos, detecção/resolução guiada de conflitos por item com estratégia explícita, plano sanitizado, aceite de snapshot recebido quando já materializado, `resolutionHistory` compacta, prevenção de reabertura idêntica de conflito resolvido e revisão/diff governado `.afcatalog-conflicts.json`/`.afcatalog-conflicts-diff.json`; ainda precisa evoluir para colaboração distribuída mais profunda.
- Sandbox dedicado avançado por nó com artefato binário microVM kernel-direct pronto em ambiente real e políticas de orquestração ainda mais fortes; executores nativos Python/JavaScript/TypeScript/Bash-Shell, HTTP, MCP stdio, sidecar local, `runtime_adapter` por endpoint e primeira camada de `runtime_adapter` por runner VM local, política inicial de payload/retry/redaction, perfis reutilizáveis de sandbox/payload, catálogo inicial de imagens gerenciadas por executor, presets gerenciados iniciais de VM, checker local de runner/imagem/manifestos VM sem executar código do usuário, export governado `.afvmreadiness.json` sem paths locais resolvidos, manifesto opcional de runner/imagem com validação de protocolo, engine, imageId, tamanho declarado, SHA-256 e capabilities hardened, scaffold QEMU com cloud-init/build/boot/transportador SSH, scaffold microVM direct-kernel com preparo rootfs/kernel ou firmware/seed e preflights, smoke real Docker/QEMU com cloud image Debian, gate microVM real opt-in para Firecracker/Cloud Hypervisor com dry-run, preflight real e boot launch smoke quando binários/artefatos são fornecidos, contrato de homologação `.afvmhomologation.json`, receita oficial local `vm-image:microvm-recipe`, registro consumível `vm-image:microvm-register`, empacotador `.afvmimagebundle` com manifestos sanitizados, imagem e artefatos obrigatórios de boot copiados/verificados, `runner-kit` auto-verificável, runner de referência de contrato Python local, runner QEMU de preflight com plano Q35/microVM, runner Firecracker/Cloud Hypervisor de preflight direct-kernel, transporte externo explícito para guest e guest agent Python embutível na imagem, perfil inicial de orquestração hardened de container, ponte local `sandboxIsolation: "vm"` para runner externo em Python/Node/Bash-Shell, rastreabilidade de isolamento por execução, workspace temporário descartável para executores process-backed, worker Python dedicado por processo, primeira camada de container Python/JavaScript/TypeScript/Bash-Shell, painel de falhas de sandbox por nó, visão agregada por executor no run atual, telemetria histórica persistida de sandbox por flow, contadores de VM/microVM/hardening/isolamento VM verificado e export governado `.afsandboxtelemetry.json` já existem; executar/publicar imagens microVM kernel-direct reais e políticas ainda mais fortes continuam pendentes para ambientes que exigirem isolamento próprio mais rígido.
- Evoluir a orquestração multiagente executável em bundles complexos; composição assistida no Runtime Manifest, recomendações de flows/agentes, checklist visual de prontidão, templates iniciais de papéis `triage`/`support`, contrato declarativo `orchestration` com modo/entrada/handoffs, primeira execução por `POST /orchestration/run`, roteamento simples por condição explícita/textual/estruturada, memória compartilhada governada com previews, `orchestration.memoryPolicy`, persistência JSONL opcional/default, retenção, redaction e limites de preview/entradas, `debug_trace` step-by-step da orquestração, painel visual `Debug orquestração` com histórico local, filtros, comparação, export, histórico compartilhável file-backed em `.agent-flow/orchestration-debug/history.aforchdebug.json`, sync central opcional e diff governado `.aforchdiff.json` entre execuções, contrato explícito `agentIsolation` por rota/agente, visão de bundle, cards operacionais por agente com status/evidência de smoke, detecção de smoke desatualizado, seleção direta, histórico filtrado por agente, smoke direto por agente, smoke agregado, runbook JSON por agente, verificação com PostgreSQL real compartilhado via `npm run test:multiagent-postgres`, primeira camada compartilhável/centralizável de cenários/datasets/evaluators em `.agent-flow/studio-scenarios/scenarios.afscenarios.json`, curadoria de conflitos de cenários com lease de atribuição configurável por `AGENT_FLOW_STUDIO_SCENARIO_CONFLICT_CURATION_LEASE_HOURS`, liberação automática de leases vencidos e histórico compacto `lease_expired`, e histórico experimental compartilhável/centralizável com diff governado já existem; ainda falta colaboração distribuída mais profunda em cenários multiusuário avançados.
- Safety Harness avançado com avaliação contínua e governança multiusuário; a primeira camada local configurável por nó, a biblioteca local exportável/importável de políticas, o provider HTTP externo no Builder/runtime final, histórico backend por workspace, dashboard local por flow/nó, revisão humana simples/rápida com identidade local e papel visíveis/exportados, bloqueio backend de revisão por `viewer`, `resolvedRole` em colisões automáticas do mesmo run, export governado `.afsafetyhistory.json`, diff governado `.afsafetyhistory-diff.json`, sync central opcional com token só no header e pacote sanitizado, resolução automática governada de colisões do mesmo run com refs compactas, governança central file-backed do workspace, auth local opcional do Builder, checagem consultiva de autorização local e enforcement opcional nas rotas críticas do Builder já existem.
- Políticas operacionais mais ricas para jobs pós-finalização, escala e operação contínua refinada; schedule manual por job, recorrência simples por intervalo, cron ou evento, endpoint `POST /job-schedules/trigger-event`, endpoints nativos de lote, métricas agregadas e operacionais com janela configurável/p95/throughput, histórico local/compartilhável/exportável/comparável/sincronizável de snapshots de métricas, retry, reprocessamento manual, limpeza governada com prévia, ações em lote via Studio, primeira camada persistente/manual/recorrente, worker CLI opcional, serviço `worker` configurável no Docker Compose, claim/lease multiworker com retomada após expiração e painel visual inicial no Studio já existem.
- Dashboards históricos adicionais e políticas ainda mais avançadas de entrega/escalonamento de alertas ainda podem evoluir; streams de eventos por SSE/WebSocket, spans nativos `span_started`/`span_completed`, callback incremental de tokens no grafo, `turn/stream` SSE/WebSocket, consumo visual de eventos/resposta incremental, painel de progresso por nó/uso por provider, telemetria histórica local com janela/alertas por orçamento, dashboard histórico dedicado por provider com snapshots/export, merge, sync central opcional e diff governado, e inbox local/exportável/compartilhada file-backed com retenção, reconhecimento, roteamento lógico, escalonamento local, política de entrega com lote/cooldown/prioridade, prontidão exportável e dispatch externo governado por rota de alertas de provider no Studio já existem.
- Modelos locais já possuem primeira camada via adapter `ollama` OpenAI-compatible local, healthcheck dedicado no Builder API/UI, detecção visual de modelos instalados, presets iniciais por perfil de hardware, troca guiada para modelo já instalado, orientação de `ollama pull`, empacotamento Compose inicial do servidor Ollama, pull governado por profile `model-setup`, execução visual desse profile no painel da API Docker, fallback prescritivo no runtime quando o modelo local não está baixado, imagem local pré-carregável por override Compose, build local gerenciado dessa imagem pelo Builder, tag `OLLAMA_MODEL_IMAGE`, export local versionado `.tar` via Builder, publicação em registry externo via `docker image push` quando a tag é publicável, catálogo local/mergeável de imagens e comandos em `.agent-flow/model-images/catalog.afmodelimages.json`, lista visual do catálogo no painel Docker, export `.afmodelimages.json`, import/merge visual entre workspaces, descoberta/sync file-backed de pacotes em `.agent-flow/model-images/imports` e `AGENT_FLOW_MODEL_IMAGE_CATALOG_PATHS`, registry remoto curado salvo no workspace em `.agent-flow/model-images/remote-registries.afmodelregistry.json`, sync remoto simples de catálogos HTTP(S) por UI ou `AGENT_FLOW_MODEL_IMAGE_CATALOG_REMOTE_URLS`, sync central multiusuário por `AGENT_FLOW_MODEL_IMAGE_CATALOG_CENTRAL_URL` com token só no header e sem credenciais no corpo/status, comandos de load/push no runbook/status quando aplicável, perfil NVIDIA GPU por override Compose, detecção de GPU local por `nvidia-smi`, inspeção do runtime `nvidia` do Docker por `docker info`, recomendação CPU/GPU no status, seletor visual `CPU/GPU` para subir o runtime sem editar `.env` e probe manual `Testar GPU` em container CUDA; ainda falta validação prática ampla em múltiplas combinações reais de driver, Docker Desktop/Linux, NVIDIA Container Toolkit e imagens CUDA.
- Integração opcional com LangSmith Cloud pelo Builder UI agora possui handoff local governado, registro de evidência externa, deploy por endpoint governado e sync central opcional desses registros: o Builder gera `.agent-flow/langsmith-handoff.aflangsmithhandoff.json` com checklist, comandos, nomes de env sem valores, status de sandbox/aprovação e flags explícitas de que não chama cloud, não salva token e não inclui secrets/payloads/prompts/schemas brutos; também salva `.agent-flow/langsmith-cloud-deployments.aflangsmithdeployments.json` com status `prepared`/`deployed`/`verified`/`failed`, hash do handoff, ator/papel, URLs sanitizadas sem query/hash e bloqueio de `viewer`; dispara deploy opcional por `AGENT_FLOW_LANGSMITH_CLOUD_DEPLOY_URL`/`AGENT_FLOW_LANGSMITH_CLOUD_DEPLOY_TOKEN`, enviando handoff e registros sanitizados para endpoint do operador com token somente no header e salvando a resposta como evidência local; e expõe status/sync central por `AGENT_FLOW_LANGSMITH_CLOUD_DEPLOYMENTS_CENTRAL_URL`/`AGENT_FLOW_LANGSMITH_CLOUD_DEPLOYMENTS_CENTRAL_TOKEN`, com token somente no header, corpo/status sem token, status sem URL e merge local dos registros recebidos. Ainda falta deploy gerenciado acoplado diretamente a um provedor específico, caso o operador queira essa integração fora do modo 100% local.

## Próximos Passos (ciclo atual)

Para chegar ao objetivo completo de "studio local + aprovação + API Docker" sem regressão de capacidade, a sequência recomendada é:

1. **Canvas/produtos de trabalho refinados (Média prioridade)**
   - aprofundar comandos contextuais avançados de canvas para fluxos longos de edição por teclado, além da camada atual de seleção upstream/downstream/vizinhos, inserção conectada por nó/aresta, conexão sequencial de multiseleção, foco, duplicação, debug, alinhamento/distribuição, prompt/schema, tags e remoção pela paleta.
   - ampliar o editor visual de JSON Schema para governança multiusuário e diffs entre versões de schemas complexos; o preview/diff local antes de aplicar, export/import/revisão `.afschemapatterndiff.json`, histórico `.afschemapatternhistory.json`, export/import/revisão de diff histórico `.afschemapatternhistorydiff.json`, curadoria local por status/uso com assessment exportável e eventos compactos, sincronização file-backed por flow, sync central opcional com token no header, auditoria compacta de sync, resolução inicial de conflitos compartilhados compactos, merge estrutural automático de schemas brutos sem colisão, plano assistido sanitizado para colisões reais, revisão visual por colunas, diff sanitizado por área, diff textual local lado a lado em respostas de merge com schemas brutos, aplicação manual auditada pelo schema visual e prevenção de reabertura idêntica de conflitos resolvidos já existem.

2. **Cenários + datasets avançados (Média prioridade)**
   - consolidar a camada avançada de cenários por agente/run além do `sourceContext` inicial, filtros e execução/seleção filtrada por tipo/agente/run já persistidos/expostos em cenário/fixture/metadata/UI;
   - aprofundar replay por pins além das ações guiadas por camada, da primeira execução guiada do cenário selecionado, do export governado `.afreplaygovernance.json`, do histórico local/exportável, compartilhado file-backed e sync central opcional `.afreplayhistory.json`, do diff histórico `.afreplayhistorydiff.json`, da curadoria local por revisor e da thread/atribuição local com lease para conflitos de pins/replay; ainda falta colaboração multiusuário avançada de checkpoints/pins;
   - evoluir datasets/evaluators para experiment suite avançada com colaboração distribuída mais profunda; a camada atual já possui evaluators textuais/JSON path, evaluators HTTP/LLM-as-judge, histórico experimental compacto, dashboard agregado por flow com snapshots backend, dashboard histórico dedicado com análise de tendência/deltas/melhor-pior snapshot, export/comparação governada do histórico em `.afexperiment-dashboard-history.json`/`.afexperiment-dashboard-history-diff.json`, merge e sync central opcional do histórico experimental com token só no header, fila de revisão com backend por workspace, identidade local de revisor, policy local por responsável e papéis locais com enforcement backend por ator, trilha de auditoria, snapshots históricos compactos/exportáveis da fila, sync central opcional da annotation queue com token só no header e sem runs brutas, curadoria atribuível de conflitos com lease/expiração em cenários compartilhados, pins e annotation queue, detecção/resolução/tombstone de conflitos de revisão, biblioteca compartilhável file-backed/central opcional de cenários, datasets e evaluators do Studio e exports/imports `.afevaluators.json`/`.afexperiments.json`/`.afexperiment-dashboard.json`/`.afannotations.json`/`.afannotationhistory.json`.
   - evoluir Safety Harness para governança contínua multiusuário distribuída mais profunda; a camada atual já possui avaliação local, provider HTTP externo opcional no Builder/runtime final, histórico backend por workspace, dashboard local por flow/nó, revisão humana simples/rápida com identidade local visível/exportada, bloqueio backend de revisão por `viewer` quando há auth local/ator do Builder, export governado `.afsafetyhistory.json`, diff governado `.afsafetyhistory-diff.json`, sync central opcional com payload sanitizado e resolução automática governada de colisões do mesmo run.

3. **Adapters de código não nativos (Média/Longo prazo)**
   - evoluir adapters dedicados, linguagens e políticas de container/VM mais fortes quando endpoint/runtime_adapter por VM/sidecar/MCP/workspace temporário/processo dedicado/container Python/JavaScript/TypeScript/Bash-Shell com imagens gerenciadas, presets VM, empacotador `.afvmimagebundle`, perfil hardened e ponte VM local nao forem suficientes;
   - ampliar a política inicial de segurança para perfis reutilizáveis, auditoria e isolamento por executor quando necessário;
   - evoluir políticas de orquestração de VM/microVM quando a telemetria atual de sandbox por executor/orquestração/hardening/assurance e export governado não forem suficientes para ambientes com isolamento próprio mais rígido.

4. **Multiagente operacional**
   - rota/agent_id estável no runtime e no Studio já possui primeira implementação;
   - aprofundar isolamento e orquestração executável por agente dentro do bundle; composição assistida no manifesto, orquestração declarativa, cards de rota/metadata, smoke por agente, histórico filtrado, smoke agregado e runbook JSON exportável por agente já existem;
   - manter `npm run test:multiagent-postgres` como gate repetível para PostgreSQL real compartilhado.

## Regras de bloqueio até fechamento de uma fase

- Nenhuma etapa pode ser marcada "concluída" enquanto não cumprir:
  - paridade da capacidade equivalente à de um fluxo de referência real (ProUp/semântica de conversa);
  - aprovação por hash válida para geração do runtime final;
  - evidência local de debug (run+timeline+state diff+node IO);
  - tema claro e escuro testado no fluxo alvo;
  - fluxo completo sem troca para ferramentas externas no caminho principal.
