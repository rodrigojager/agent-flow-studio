# Schemas estruturados em JSON Schema versionável

Schemas de entrada, saída estruturada e fragmentos de estado devem ser arquivos JSON Schema separados e referenciados pelo fluxo de agente. O codegen pode gerar modelos Pydantic ou outros artefatos a partir desses schemas, mantendo a especificação independente do runtime Python.
