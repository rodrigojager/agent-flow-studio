# Usar LangGraph no baseline manual

O baseline manual deve usar LangGraph desde o primeiro runtime de referência. Como o produto pretende gerar LangGraph + FastAPI, o baseline precisa exercitar o risco central da arquitetura em vez de simular grafo com uma camada própria temporária.
